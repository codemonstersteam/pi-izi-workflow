# Журнал кругов правки плана — eddi / DOS-535

Что здесь: ход прогонов шага 10в (критик → фиксер → гардрейлы) шаг за шагом, с тем, к чему каждый
шаг привёл. Пишется по журналу прогона (`~/.pi/workflows/.../journal.json`), а не по печати
запускающей модели. Цель — увидеть картину целиком и назвать проблемы.

Как читать: `→` результат хост-функции, `⟹` следствие для артефактов, `✘` дефект.

---

## Прогон 4f938cfe · 20.08.2026 11:16 · со слепка `eddi-step9-start`

Вход `from 9` (PLAN.md нет). Испытывается: критик получил адресную книгу; правка кладётся в карточку.

| # | шаг | результат |
|---|---|---|
| 1 | `design` ворота шага 9 | → `reused: ["values","chains"]` ⟹ роль `router` не звалась, ~40k токенов сэкономлено |
| 2 | роль `core-designer` | наряд 48 515 симв → карточка партии, 11 модулей |
| 3 | `planbook` | → 11 модулей = 11 разделов, 7 use case, 2 блока `raises:` |
| 4 | `tickets{dry}` | → 16 нарядов, волны 4·1·3·5·2·1 |
| 5 | `planFeedback{nfrs}` | → не хватает 1: `glossary-cache-ttl = 5 minutes` |
| 6 | `planCard` → роль `plan-fixer` → `planFix judge:"card"` | → ok ⟹ величина в КАРТОЧКЕ, план пересобран |
| 7 | роль `plan-critic`, круг 1 | → 5 находок, **каждая с путём** (адресная книга сработала) |
| 8 | `planRoute` | → план 5 · дизайн 0 · требование 0 |

**✘ Дефект 1.** R11 («модуль конфигурации агента») уехал в `plan`, хотя раздела у модуля нет.
Причина: маршрут считал «путь есть в плане» по вхождению строки, а путь стоял в плане как
`sample:` — образец стиля соседнего раздела. Фиксер закрыл бы находку строкой ВНУТРИ чужого раздела.
**Починено:** «в плане» = есть заголовок раздела `## <путь>` (с нумерацией или без). Проверено на том
же вердикте: было `план 5`, стало `план 4 · требование 1`.

---

## Прогон 00d07e75 · 11:27 · со слепка, с фиксом маршрута

| # | шаг | результат |
|---|---|---|
| 1 | `design` ворота | → `reused: ["values","chains"]` |
| 2 | роль `core-designer` | карточка, 11 модулей |
| 3 | `planbook` | → 11 = 11, 2 блока `raises:` |
| 4 | `tickets{dry}` | → 16 нарядов |
| 5 | величина | → `planFix ok` ⟹ `5 minutes` в карточке; отметка шага 9 переставлена |
| 6 | роль `plan-critic`, круг 1 | → 5 находок с путями (конверт соврал: `gaps: 2` при 5 строках) |
| 7 | `planRoute` | → **план 4 · требование 1** ⟹ фикс маршрута работает |
| 8 | `nodeFacts` | → объявления `AgentConfiguration.java` (151 шт.) поданы фиксеру |
| 9 | `planCard` ×4 | → все находки в одну карточку (партия одна) |
| 10 | роль `plan-fixer` → `planFix judge:"card"` | → ok, 12 785 симв ⟹ 4 находки закрыты В ИСТОЧНИКЕ |
| 11 | **`frdAdopt`** | → ok, 14 115 б, **без роли** ⟹ `<delta op="r11" node="…/AgentConfiguration.java">` + узел в `nodes` сценария S5 |
| 12 | `return {replay}` | ⟹ `from = 9`, план переигрывается; правки круга не потеряны — они в карточке |
| 13 | `design` ворота | → `reused: []` ⟹ требование моложе словаря, проход A пересчитывается — верно |
| 14 | роль `valuer`, круг 1 | → 50 значений |
| 15 | гардрейл `design` | → КРАСНО: «строки v40, v43, v44, v49 нет в файле» |

**✘ Дефект 2.** Строки БЫЛИ в файле. Скелет нёс `text="create(List&lt;T&gt; configs, …)"`, роль
вернула `List<T>` — раскодировала при копировании. Сырой `<` заканчивает элемент на себе, и строка
исчезает для читателя, оставаясь в файле. Роль видит `v40` глазами, а ей говорят «её нет» — петля
до конца бюджета в 5 кругов.
**Починено:** блокер различает «строки нет» и «строка есть, но не читается» и называет замену
(`&lt;`, `&gt;`, `&amp;`); правило экранирования переехало из текста роли **в наряд**.

**✘ Дефект 3 (текст лога).** Полоса печатает «полный суд зелен» после `frdAdopt`, тогда как судится
ДИФФ блокеров: старый долг артефакта (F13, F14) правке не вменяется. Формулировка врёт наблюдателю.
Не починено.

---

## Прогон 67a33170 · 11:53 · продолжение с точки «требование усыновило модуль»

Снимок точки: `sandbox/runbox/prev-runs/eddi-after-adopt`. Отметка шага 6 переставлена на
усыновлённый `frd.xml` (журнал хранит sha, не вердикт), вход `from 9`.

| # | шаг | результат |
|---|---|---|
| 1 | `design` ворота | → `reused: []` — требование моложе, словарь и цепочки пересчитываются |
| 2 | `design{skeleton}` | → 50 строк, 35 заполнил скрипт, 15 назвать роли |
| 3 | роль `valuer`, круг 1 | → 50 значений; **гардрейл `design` ЗЕЛЁН с первого круга** |
| 4 | | ⟹ правило про `&lt;`, переехавшее из роли В НАРЯД, сработало: прошлый прогон здесь встал |
| 5 | `design{skeleton chains}` | → 13 маршрутов, все пустые |
| 6 | роль `router` | наряд 27 048 симв — идёт |

Замечание к дефекту 2: роль читает НАРЯД, а не только свою роль. Правило, лежавшее в `valuer.md`,
она игнорировала два прогона подряд; то же правило в наряде сработало сразу.

| 7 | скелет цепочек | → `AgentConfiguration.java` в нём 3 раза ⟹ усыновление доехало до цепочек |
| 8 | роль `router` | **ПОВИСЛА**: 9 минут без единого токена, `pi` жив, экран чист |

**✘ Дефект 4 — поломка, которой нет в `standards/live-run.md`.** Вызов роли повис у провайдера
БЕЗ обрыва и БЕЗ модалки: сторожу нечего нажимать, полоса не знает, что стоит, наблюдателю не
отличить от долгого ответа. Распознаётся единственным способом: `usage.tokens` в `summary.json`
не растёт, а `updatedAt` стоит. Лечение — погасить и перезапустить с точки.
Заведён сторож зависаний: раз в минуту читает `usage.tokens`, N минут без роста — кричит.
**Порог важен:** первый вариант с N=5 дал ЛОЖНУЮ тревогу на `router` — роль думала 5 минут и
продолжила (125 566 → 190 681 токенов). Порог поднят до 8 минут: `router` — самый длинный вызов
конвейера, и гасить его по нетерпению дороже, чем ждать.

---

## Прогон 6e12237b · 12:09 · перезапуск с той же точки

Восстановление из снимка `eddi-after-adopt` заняло 15 секунд: содержимое формы вычищено, `.agent`
и `task` положены из снимка, отметка шага 6 переставлена, вход снова `from 9`.

Ожидается: цепочки → карточка на 12 модулей → `planbook` 12 разделов → нарезка ~17 нарядов →
круг 2 критика. **Ключевая проверка: получил ли `AgentConfiguration` собственный раздел и наряд.**
