# Как работает workflow izi-pi

Реализация концепта (`docs/concept.md`) в текущем репозитории. Здесь — раскладка, программа и
карточка каждого шага. Обоснования не повторяются: они живут в концепте, этот документ на них ссылается.

---

## 1. Два шага, никакой обвязки (S11)

Оператор сказал прямо: «кто тебя просил универсальный харнес под двенадцать, убирай всю
универсальность, делай конкретные два шага». `pipeline.json`, `step.json`, манифест
(`bin/steps.mjs`), квитанции (`bin/receipt.mjs`), генерический промоут (`bin/promote.mjs`) и
headless-раннер (`bin/run.mjs`) — упразднены целиком, не переписаны. Их работу теперь делают пять
именованных функций хоста и программа в полсотни строк, которая знает оба шага по имени, а не по
данным из файла.

```
ext/
  index.mjs         pi-extension: readText · answers · checkTask · checkBrd · brdForm · promote · setPending ·
                     clearPending · survey (S15) · budgets · herdrStatus (S16) · cells · digest · reuse ·
                     remember · checkPart (S17) · buildGraph (S20) · graphMap · checkFrd · frdForm (S21) ·
                     weight (S22) · ripple (S23) · design (S24) · plan + gitTrunk (S29)
                     — глобалы izi.js (roleDirectories → steps/brd/, steps/scope/, steps/intake/,
                     steps/design/);
                     ПЛЮС (S13) tool izi_answer, зарегистрированный через pi.registerTool на самой
                     интерактивной сессии — export default function extension(pi) одновременно
                     обычный ExtensionFactory pi (`(pi: ExtensionAPI) => void`,
                     @earendil-works/pi-coding-agent) И фабрика pi-extensible-workflows
  package.json      МИНИМАЛЬНЫЙ package.json — не пайплайну, а расширению: pi.extensions,
                     зависимость на pi-extensible-workflows (контракт хоста, объяснено в файле)

steps/task/
  task.mjs          ЧИСТОЕ правило входа: непуст, ≤300 строк — checkTaskText(text)
  task.test.mjs     тест по формуле — 1 happy + Σ ветвей антецедента

steps/brd/
  gilb.md           роль (имя файла = имя роли — так резолвит pi-extensible-workflows,
                     ext/index.mjs::roleDirectories, см. ниже)
  order.tpl         наряд роли: плейсхолдеры {TASK}, {ANSWERS}, {FEEDBACK}, {STAGING}, {CHECK}
  brd.mjs           ЧИСТОЕ ядро приёмки: newBrd(text, sources) — источники чисел: TASK.md + ЗНАЧЕНИЯ
                     ответов оператора, не текст его вопросов
  brd.test.mjs      тест по формуле

steps/survey-plan/  (S15) шаг-СКРИПТ: роли нет, значит нет ни .md-роли, ни order.tpl, ни staging —
  plan.mjs           ЧИСТОЕ ядро раскладки: newPlan({files, spine, subjects}) → клетки роя,
                      CELL_FILES=20 · CELL_BYTES=200 КБ (что раньше)
  plan.test.mjs      тест по формуле: happy · шов по байтам · no-files

steps/design/       (S24) шаг 9: две проекции изменения, роль designer + рельса вопроса
  designer.md        роль (имя файла = имя роли; roleDirectories += steps/design/)
  order.tpl          наряд: {FRD}, {RIPPLE}, {ANSWERS}, {MODE}, {DELTA_FORMS}, {FEEDBACK}, {STAGING},
                      {CHECK} — словарь дельт подставляется функцией хоста frdForm, как на шаге 6
  design.mjs         ЧИСТОЕ ядро: parseDesign · parseRoutes · expand · checkDesign · newDesign;
                      expand даёт ОБЕ секции data-flow.md — поток по сценариям и список юнитов по узлам
  design.test.mjs    happy + семь правил + «ни одного <module>» + тотальность + швы наряда и роли

steps/intake/       (S21) шаг 6: роль intake, рельса вопросов как у brd
  intake.md          роль (имя файла = имя роли; roleDirectories += steps/intake/); содержание —
                      навык requirements-intake из izi-flow
  order.tpl          наряд: {BRD}, {MAP}, {ANSWERS}, {FEEDBACK}, {STAGING}, {CHECK} и ДВА словаря
                      подстановкой — {DELTA_FORMS}, {SOURCES} (функция хоста frdForm, как brdForm)
  frd.mjs            ЧИСТОЕ ядро: parseFrd · checkFrd · newFrd; правила F1..F7 (docs/intake.md §5)
  map.mjs            ЧИСТОЕ: parseMap (ключи узлов, тесты, рёбра — читает и шаг 8) · mapMeasure (цена
                      карты); индекса НЕТ — §3 там же
  frd.test.mjs · map.test.mjs   тесты по формуле + швы наряда и роли

steps/weight/       (S22) шаг 7 — шаг-СКРИПТ: ни роли, ни оператора, ни токена
  weight.mjs         ЧИСТОЕ ядро: MODE_TABLE (форма → вес) · newMode({deltas}) — максимум по формам;
                      словарь форм НЕ дублируется, он берётся из steps/intake/frd.mjs
  weight.test.mjs    тест по формуле + шов «каждая форма словаря имеет вес» против расхождения слоёв

steps/plan/        (S29) шаг 10 — скрипт С ОПЕРАТОРОМ: роли нет, но ответ применяет сам скрипт
  plan.mjs           ЧИСТОЕ ядро: newPlanIndex — узлы из ШИРИНЫ (changeWidth шага 8), порядок из
                      НАПРАВЛЕННЫХ рёбер карты, команда из <test>×<suite>, ветка из веса и ключа;
                      TASK_KEY и KEY_QUESTION — константы модуля, вопрос байт в байт стабилен
  git-conventions.md навык izi-flow, приведённый к standards/role.md и УРЕЗАННЫЙ до исполнимого:
                      имя ветки и база; коммит и PR приедут со своим шагом (docs/triggers.md)
  plan.test.mjs      13 швов на фикстуре из артефактов t3 + сверка регулярки с навыком

steps/ripple/       (S23) шаг 8 — шаг-СКРИПТ: ни роли, ни оператора, ни токена
  ripple.mjs         ЧИСТОЕ ядро: DESIGN_TABLE (вес → как решается флаг) · newRipple({xml, frd, mode,
                      map, cap}) — major/minor всегда, patch по ШИРИНЕ (тронутые узлы); подграф
                      радиусом 1; карту читает steps/intake/map.mjs, второго разбора грамматики нет
  ripple.test.mjs    тест по формуле; шов грамматики — вывод читается parseDesign шага 9

steps/scope/        (S17) шаг-РОЙ: роль scout, ДВА наряда (по полю kind клетки), одно ядро
  scout.md           роль (имя файла = имя роли; roleDirectories += steps/scope/)
  order.survey.tpl   наряд клетки kind="survey": модули, их api и зависимости
  order.spine.tpl    наряд клетки kind="spine": пять ответов графа или found="no"
  part.mjs           ЧИСТОЕ ядро: parsePart · checkPart · newPart; правила C1 · S1..S5 · P1..P3
  part.test.mjs      тест по формуле + швы наряда (плейсхолдеры) и роли (запрет называет проверку)

workflows/izi.js    вся программа: четыре рельсы, бюджеты из конфига, ok/err/exit
core/               общее (answers, budgets, findings, result, form, xml) — `xml.mjs` (S17) вынут
                    из steps/design: сканер тегов один на два среза, чтобы грамматика не разошлась
bin/{answer,write-answer,cli-entry,decisions-log}.mjs  запасной канал (S13: не единственный —
                     основной путь чат-tool izi_answer), которым ОПЕРАТОР может писать ответ на
                     диск руками; write-answer.mjs — общая io-запись, использует и bin/answer.mjs,
                     и izi_answer. Воркфлоу-скрипт .agent/answers.md не читает — читает функция
                     расширения `answers`
.agent/             состояние прогона: staging/ · brd.md · answers.md · pending.json · decisions.log
```

**Установка.**
```bash
cd ext && npm install && cd ..
pi install ./ext
```
`npm install` внутри `ext/` нужен один раз (создаёт `ext/node_modules/`, каталог в `.gitignore`) —
это проверено фактом, не предположением: `pi install ./ext` (локальный путь) НЕ гоняет `npm install`
сам, и без этого шага сессия `pi -p` падает на старте с `Cannot find module
'pi-extensible-workflows'`; разбор причины — в `ext/package.json`. После `npm install`
`pi install ./ext` подключает разом функции, `steps/brd/` как источник ролей и `prompts/` (третьим
полем того же `pi`-манифеста, `"prompts": ["../prompts"]`) — отдельный `bin/install.mjs` для этого
больше не нужен.

**Почему `gilb.md`, а не `role.md`.** `pi-extensible-workflows` резолвит роль по ИМЕНИ ФАЙЛА в
объявленном `roleDirectories` (`validation.js::scanRoleFiles`: `name = basename(entry.name, ".md")`),
не по каталогу шага и не по отдельному полю конфигурации. Расширение объявляет `roleDirectories:
[new URL("../steps/brd/", import.meta.url)]` — сам срез, а не отдельный каталог `roles/`: не-`.md`
соседи (`brd.mjs`, `order.tpl`, тест) сканеру ролей не мешают, он берёт только `*.md`. Файл, названный
`role.md`, установился бы как роль «role», и `agent({ role: "gilb" })` её бы не нашёл — проверено
фактом: живой прогон с файлом, названным верно, дал `agentLaunches: 1` в `summary.json` реального
запуска (см. §3, карточка `brd`).

---

## 2. Программа

`workflows/izi.js` целиком — восемь фаз, `ok`/`err`/`exit`, бюджеты из `izi.config.json` вместо
`pipeline.json` (`LOOPS` тратят `brd`, `scope` и `intake`; `QUESTIONS` — `brd` и `intake`, см.
`docs/concept.md`, раздел «Что отложено и почему»):

```js
let LOOPS, QUESTIONS, QUESTION_ROUNDS, CHECKPOINT_RETRIES; // S16: не литералы — izi.config.json
                                                          // S21: вопросов 60, кругов 3 — разное
const b = await budgets({});                              // умолчания 3/3/2 живут в core/budgets.mjs,
if (!b.ok) exit(err("blocked", { subject: b.why }));      // сломанный конфиг = отказ, не умолчание
LOOPS = b.loops; QUESTIONS = b.questions; QUESTION_ROUNDS = b.questionRounds; CHECKPOINT_RETRIES = b.checkpointRetries;

const h = await herdrStatus({});                          // S16: наблюдаемость объявляется вслух —
log(h.available ? `herdr: on pane=${h.pane}` : `herdr: off — ${h.why}`);  // herdr при недоступности
                                                          // молчит, и прогон идёт вслепую

const ok  = (fields) => ({ track: "ok", code: 0, ...fields });
const err = (kind, fields) => ({ track: "err", kind, code: kind === "crashed" ? 2 : 10, ...fields });
const exit = (result) => { throw new Exit(result); };      // throw-to-unwind из глубины цикла

async function task() {
  const t = await checkTask({});                           // функция хоста, не shell("cat …")
  if (!t.ok) exit(err("blocked", { subject: t.why }));
}

async function brd() {                                     // на зелёном чеке RETURN, не exit —
                                                           // S15: brd больше не конец прогона
  // читает order.tpl и TASK.md функцией readText, ANSWERS — функцией answers();
  // делегирует agent(order, { role: "gilb", outputSchema: ENVELOPE });
  // на вопросе — askOperator(): setPending({subject, evidence}) ДО checkpoint() (S13 — полный
  //   вопрос лежит в .agent/pending.json ДО того, как чат увидит паузу; foreground: false, значит
  //   пауза приезжает ОБЫЧНЫМ сообщением — оператор отвечает прямо в этом чате, модель зовёт tool
  //   izi_answer, потом workflow_respond) + answers() до/после (Approve без появившегося ответа =
  //   переспрос, роль не зовётся заново — не тратит LOOPS; ответ появился → clearPending());
  // на ok — checkBrd({ path: STAGING }) ПО STAGING, зелёный → promote({ from, to }) → log + return,
  //   красный → feedback в пере-делегацию — тратит LOOPS
}

async function surveyPlan() {                              // S15: шаг-СКРИПТ — ни роли, ни вызова
  const PLAN = ".agent/survey-plan.json";                  // модели, ни оператора, ни checkpoint
  const p = await survey({ path: PLAN });                  // одна функция хоста делает весь шаг
  if (!p.ok) exit(err("blocked", { subject: p.why }));     // единственный отказ — no-files
  log(`survey-plan: files=${p.files} bytes=${p.bytes} cells=${p.cells}`);   // стоимость роя ДО роя
  log(`survey-plan: files=${p.files} bytes=${p.bytes} cells=${p.cells}`);   // стоимость роя ДО роя
}

async function weigh() {                                   // S22: шаг 7 — весь шаг в пять строк
  const w = await weight({});                              // имя локальное ≠ имя глобала хоста
  if (!w.ok) exit(err("blocked", { subject: w.why }));      // Unknown / нет дельт / форма вне словаря:
  log(`weight: mode=${w.mode} из ${w.earned}`);             //   .agent/mode стёрт функцией хоста
}

async function rippling() {                                // S23: шаг 8 — тоже пять строк
  const r = await ripple({});                              // отказ ⇒ ОБА артефакта стёрты хостом
  if (!r.ok) exit(err("blocked", { subject: r.why }));      // флаг — назначение дизайна, не радиус
  log(`ripple: design=${r.design} узлов ${r.nodes} из ${r.total} (затравок ${r.seeds})`);
}

async function designing(from) {                           // шаг 9, проход A: словарь изменения
  const gate = await design({});                           // без аргументов — ворота: читают .agent/design
  if (!gate.ok) exit(err("blocked", { subject: gate.why })); //   и стирают всё, что не зелено СЕЙЧАС
  if (gate.design === "skip") { log("design: skip"); return ".agent/design"; }
  const s = await design({ skeleton: SKELETON_PATH });      // СКРИПТ: состав словаря, 0 токенов
  // цикл до LOOPS: agent(order, { role: "valuer" }) — роль заполняет только пустые text
  // на ok — design({ path: STAGING }) ПО STAGING: зелёный → нормализация и промоут .agent/values.xml
  return ".agent/values.xml";                              // полоса ОСТАНАВЛИВАЕТСЯ: B и C не написаны
}

async function intake() {                                  // S21: шаг 6 — форма brd(), но вход другой
  const map = await graphMap({});                          // карта целиком ИЛИ отказ с числом байт:
  if (!map.ok) exit(err("blocked", { subject: map.why }));  //   индексной формы этот срез не строит
  // цикл до LOOPS: agent(order, { role: "intake" }) с {MAP} и ДВУМЯ словарями из frdForm();
  // на вопросе — askOperator(env, asked, "intake", "intake") — та же рельса, свой префикс чекпойнта;
  // на ok — checkFrd({ path: STAGING }) ПО STAGING, зелёный → promote → return, красный → feedback
}

try { phase("task"); await task(); phase("brd"); await brd();
      phase("survey-plan"); await surveyPlan(); phase("scope"); await scope();
      phase("graph"); await graph(); phase("intake"); await intake();
      phase("weight"); await weigh(); phase("ripple"); await rippling();
      phase("design"); const made = await designing(from);
      if (made !== ".agent/design") exit(err("blocked", { subject: "проход A закрыт, B и C не написаны" }));
      phase("plan"); bandEnds(await planning()); }  // конец полосы — одно место, а не десятое
catch (e) { return e instanceof Exit ? e.result : err("crashed", { subject: String(e?.message ?? e) }); }
```

Порядок остаётся КОДОМ, а не `pipeline.json`: десятая фаза — ещё одна именованная функция. Решение
принято с фактами шести шагов на руках (`docs/survey-plan.md` §5): ручные вызовы подряд всё ещё
дешевле, чем манифест плюс диспетчер плюс их тесты.

Полный файл — `workflows/izi.js`. Никакого `shell()`, `JSON.parse` или кода возврата процесса —
воркфлоу-скрипт не знает о процессах вообще, только о функциях хоста (`readText`, `answers`,
`checkTask`, `checkBrd`, `brdForm`, `frdForm`, `promote`, `setPending`, `clearPending`, `survey`,
`cells`, `digest`, `reuse`, `remember`, `checkPart`, `buildGraph`, `graphMap`, `checkFrd`, `weight`,
`budgets`, `herdrStatus`) и о встроенных глобалах pi
(`agent`, `checkpoint`, `prompt`, `log`, `phase`). `izi_answer` НЕ входит в этот список — это tool
интерактивной сессии (`pi.registerTool`, `ext/index.mjs`), а не функция песочницы воркфлоу; внутри
`workflows/izi.js` его вызвать нельзя и не нужно — его зовёт модель ЧАТА в ответ на follow-up
сообщение паузы.

Веер (шаги 4-12 — план, не реализация) сюда пока не входит: шаг 3 его только РАСКЛАДЫВАЕТ, а
батчами по `maxParallel` рой погонит уже своя программа, когда появится первый веерный срез. Тогда
же встанет вопрос, возвращать ли порядок в данные — см. `docs/concept.md`, «Что отложено и почему».

---

## 3. Шаги

Состояние: **есть** — работает и покрыто тестами · **новое** — этим концептом добавляется.

### 1. `task` — оператор кладёт требование · human · **есть**
- **вход:** — (внешний: человек)
- **выход:** `TASK.md`
- **проверка:** `checkTask({})` (функция расширения) → `steps/task/task.mjs::checkTaskText` —
  непуст, ≤300 строк
- **оператор:** кладёт файл. Красный чек — терминальный `err(blocked)`, роль не зовётся вовсе: у
  этого шага роли нет, чинит человек

### 2. `brd` — сырое требование → измеримый BRD · role `gilb` · **есть**
- **вход:** `TASK.md`, `.agent/answers.md`
- **выход:** `.agent/brd.md` (через `.agent/staging/brd.md`)
- **проверка:** `checkBrd({ path: ".agent/staging/brd.md" })` (функция расширения) →
  `steps/brd/brd.mjs::newBrd` — у каждого требования ЕСТЬ `fit` и `verify`, число в `fit` имеет
  источник (TASK.md или ЗНАЧЕНИЕ ответа оператора, не текст вопроса), 3..7 якорей, открытых
  вопросов ноль. **S16:** правило «`fit` обязан нести измеримый токен» снято решением оператора —
  живой прогон `ed1d4094` показал ложный красный на предикатном критерии (`регистронезависимое
  вхождение подстроки`) и сжёг на нём все три пере-делегации. Качество формулировки судит человек,
  гардрейл судит состав
- **оператор (S13):** пауза `checkpoint()` (`foreground: false`) приезжает в чат ОБЫЧНЫМ сообщением
  — модальный `ui.select`, который до S13 забирал ввод у всего окна и не давал оператору напечатать
  ничего (`foreground: true`, `pi-extensible-workflows/src/host.ts:686`), здесь не участвует.
  Оператор отвечает вопрос дословно ПРЯМО В ЭТОМ ЧАТЕ; модель вызывает `izi_answer({ text })`
  (ключ вопроса тул берёт из `.agent/pending.json`, не от модели), затем `workflow_respond({runId,
  name, approved: true})`. `bin/answer.mjs` остаётся запасным CLI-входом, не основным. Approve без
  появившегося в `.agent/answers.md` ответа не считается ответом и не зовёт роль заново
- **живое доказательство (S11, живой прогон в этом репозитории):** `agentLaunches: 1..3` в
  `summary.json` — роль `gilb` резолвится из `roleDirectories` и вызывается по-настоящему;
  `.agent/staging/brd.md` перезаписывается на каждой пере-делегации, `checkBrd` читает именно его
  (staging, не `.agent/brd.md`) на каждой проверке; предзаполненный `.agent/answers.md` (`node
  bin/answer.mjs --q="…" --text="4"`) даёт роли значение прямо на первой попытке, и оно доезжает до
  `fit:` требования дословно.

- **живое доказательство (S13, живые прогоны через `herdr`, тот же `TASK.md` — booking-задача).**
  Прогон `8bb23932-f368-4632-9b0b-75ea32eea95f`: оператор напечатал `4` прямо в чате в ответ на
  вопрос `gilb`, `izi_answer` записал ответ в `.agent/answers.md`, `workflow_respond` подтвердил
  паузу — весь обмен вопрос→ответ прошёл БЕЗ второго терминала. Этот прогон вскрыл живой дефект
  вызова (`clearPending()` без обязательного JSON-аргумента —
  `"clearPending requires exactly one JSON object argument"` в `result.json`, `askOperator` крашился
  на последней строке уже ПОСЛЕ того, как ответ был принят), почин в `workflows/izi.js`
  (`clearPending({})`). Повторный прогон с исправленным кодом, `0445e4cd-2667-43e4-8db8-9102679146fb`,
  дошёл до `.agent/brd.md`, `track:"ok"`, `requirements:4` — с `fit: формат времени — ISO-8601, …` в
  R1, доказывая заодно закрытие находки `numbersIn` (см. README «Долги») живым прогоном, не только
  юнитом. Чистый повтор с нуля (`.agent/` очищен, новая сессия), `79c0aa0a-e410-4172-a5cf-a1af6a78f8e8`
  — оператор ответил `давай 2 часа`, `.agent/pending.json` корректно снят `clearPending()` после
  подтверждённого ответа, прогон дошёл до `track:"ok"` за один заход, без ручного вмешательства.

### 3. `survey-plan` — раскладка разведки · script · **есть** (S15)
- **вход:** `.agent/brd.md` (только `subjects[]`), дерево файлов от cwd прогона минус список
  пропуска (сам харнес, точечные каталоги, зависимости) — не каталог `app/`: имя каталога кода
  диктовать репозиторию нельзя, разбор — `docs/survey-plan.md` §1
- **выход:** `.agent/survey-plan.json` — клетки роя: `{files, bytes, subjects, gaps, cells[{id, kind,
  subjects, bytes, files[]}]}`; `kind: "spine"` — клетка хребта (манифест сборки, CI, конфиги,
  `README`/`CONTRIBUTING`), у неё другой наряд на шаге 4
- **проверка:** клетки покрывают ВСЕ просканированные файлы без пересечений и потерь; клетка
  закрывается по 20 файлам ИЛИ ~200 КБ — что раньше. Числа клеток не ограничены: `maxParallel` — это
  размер батча шага 4, а не потолок. Оператора на шаге нет вовсе
- **вырождение:** ноль файлов вне списка пропуска → `blocked` с диагнозом, картировать нечего
- **клетка `kind: "vendor"`:** появляется только по явному пути от оператора (ответ на вопрос шага 10
  об идиоме чужой библиотеки) — граница разведки расширяется решением человека, а не догадкой о том,
  где лежат зависимости
- **якорь помечает, а не отбирает:** `subjects[]` из BRD проставляются пометкой на файле и клетке;
  якорь, не встретившийся нигде, едет в `gaps`. Разбор — `docs/survey-plan.md` §1
- **живое доказательство (S15, чужой проект `/private/tmp/quarkus-rest-json-app-v2-t1-3`,
  прогон `cffa2e65-4c68-48ae-840c-a32c6dd28bae`):** `track:"ok"`, `.agent/survey-plan.json`,
  `files=20 bytes=28549 cells=2` — читается из `journal.json` (`function/survey/1`), а не из того,
  что напечатала модель. Клетка хребта (`spine`) = `pom.xml` + `README.md`; `mvnw`/`mvnw.cmd` и `.mvn/` в план не
  попали (`SKIP_FILES` и точечный каталог), `Legume*.java` попали — под якоря они не подходят, и
  именно это доказывает, что якорь ПОМЕЧАЕТ, а не фильтрует. Цена помётки названа фактом того же
  прогона: якорь `limit` сработал на `Dockerfile.jvm` (слово в комментарии) — ложное попадание
  стоит скауту абзаца, ложный пропуск стоил бы графу узла

### 4. `scope` — рой скаутов · role `scout` **веер** · **есть** (S17)
- **вход:** клетка плана, `.agent/brd.md`
- **выход:** `.agent/graph-parts/<клетка>.xml` — ФРАГМЕНТ грамматики графа, не документ: слияние
  делает скрипт, а свободный markdown скриптом не сливается (`docs/concept.md`, «Разведка»)
- **второй выход (script, шаг 3):** `.agent/graph-computed.xml` — рёбра, http-маршруты из аннотаций
  и род внешней системы из импорта драйвера. Вычислимое считает скрипт за 0 токенов и кладёт ОТДЕЛЬНО:
  часть — то, что сказала роль, `graph-computed` — то, что посчитала машина, слияние делает шаг 5
- **проверка:** часть замкнута грамматикой; каждый узел объявил внешние точки, поверхность и тесты —
  элементом или явным `none`; **`<dep>` в части — блокер** (рёбра не её дело); непрочитанное
  объявлено `<gap>`, а не умолчано
- **кэш:** `.izi/parts/<клетка>.{xml,json}`. Клетка пересчитывается только при изменении состава,
  содержимого (`sha1`) или версии грамматики; кэшированную часть судит тот же `checkPart` ПЕРЕД тем,
  как она закроет клетку
- **два наряда:** `kind: "survey"` — «опиши модули, их api и внешние точки»; `kind: "spine"` —
  «перечисли ВСЕ сьюты тестов: род, команда, папка, форма прогона одного файла; назови команду
  сборки, механизм тоглов, соглашения об именах веток и о сообщениях коммитов, описание внешнего контракта
  (openapi/asyncapi/иное) с его валидатором — или объяви, что их нет». Наряд выбирается по полю
  `kind`, а не разбором строк
- **как:** скаут не грепает и не выбирает себе файлы — в наряде приезжает ВЫЖИМКА его клетки: путь,
  язык, пакет, вычисленные импорты/маршруты/драйверы и объявления с видимостью. Файл он открывает
  сам, когда выжимки мало. Батчи по `maxParallel`, потому что у pi потолок параллелизма, а не потому
  что так красивее

### 5. `graph` — сборка `appgraph.xml` · script · **есть** (G0–G7)
Полная карточка — `docs/graph.md`; здесь только место шага в полосе.
- **вход:** части роя (`.agent/graph-parts/<клетка>.xml`, читаются ПО ПЛАНУ — отсутствующая часть
  есть потерянное поддерево, и она названа), `.agent/graph-computed.xml`, `.agent/survey-plan.json`
- **выход:** `.agent/appgraph.xml`
- **модуль — это ФАЙЛ**, как и в части: имя (`path`), одна функция одной фразой (`<role>`),
  объявленный вход (`<api>`). Пакет и артефакт сборки модулями не являются — у них нет ни
  единственного входа, ни одной функции, — поэтому в графе они **адрес** (`pkg`, `<artifact>`), а не
  узел. Разбор трёх редакций этого решения — `docs/graph.md` §1
- **иерархия вычисляется, а не спрашивается:** `component` (связная компонента = вертикальный срез),
  `level` (слои Кана), `fanin`/`fanout` (зацепление), `<cycle>` — всё из рёбер, посчитанных шагом 3.
  Роли не задано ни одного нового вопроса
- **проверка:** один путь — одно имя (ключ узла — репо-относительный `path`, он же ID для шагов 6, 8
  и 10); ни один якорь плана не потерян (полный `subjects` КОПИРУЕТСЯ из плана, а не выводится из
  частей); есть хотя бы один `<suite>`. Всё прочее, что могло бы стать блокером, **объявляется**:
  ответ хребта не найден → `found="no"`, тест вне сьютов → `suite=""`, неразрешимый импорт →
  `<ambiguous>`, цикл → `<cycle>`, вызываемый узел без объявленного входа → `<gap>`. У скриптового
  шага нет рельсы починки, поэтому блокер остаётся только там, ради чего стоит остановить полосу
- **`<test>` сшивается по пути И по `match` сьюта:** два сьюта на одну папку (типовой maven:
  surefire `*Test.java`, failsafe `*IT.java`) без различителя отправили бы IT-тест командой юнитов,
  и та выполнила бы ноль тестов зелёным
- **ни одного `<suite>` → полоса СТОИТ здесь**, терминальный `blocked`: *репозиторий к работе не
  готов — тест-сьюта нет, нужна отдельная задача на его создание*. Это единственное «не найден»,
  которое не превращается в вопрос на шаге 10: тоглы, ветки и спека — решения оператора, а
  отсутствие тестов делает неисполнимыми и шаг 15 (закрывать узлы нечем), и шаг 16
- **импорт вне графа:** библиотека вне дерева прогона узлом не становится — это граница разведки, а
  не дефект; неразрешимый импорт объявляется `<ambiguous>` и поднимается вопросом на шаге 10
- **суть:** слияние — коммутативный моноид по пути модуля, поэтому порядок скаутов не важен
  (доказано: пересборка графа из тех же частей даёт файл байт в байт). Граф отвечает на ШЕСТЬ
  вопросов: что здесь **собирается**, как **тестировать**, как **менять**, как **выключают**, как
  **ветвятся и коммитят**, как описан **внешний контракт**. Имя транка сюда не входит: это факт
  `git`, который берёт шаг 10
- **живое доказательство (G6):** прогоны `c4fde2f3` (чистый, 4 запуска ролей, 49 143 токена) и
  `06c4172c` (по кэшу, **1** запуск, 9 333 токена) на форме `/tmp/quarkus-rest-json-app-v2-t1-3` —
  оба `track:"ok"`, 15 модулей, 2 компоненты, 5 изолированных, 4 уровня, `FruitResourceIT` привязан
  к `component-native`, `<systems/>` пуст, `subject search` — `found="no"`

### 6. `intake` — требование × реальность → FRD · role `intake` · **есть** (S21)
Полная карточка — `docs/intake.md`; здесь только место шага в полосе.
- **вход:** `.agent/brd.md`, ответы оператора и `appgraph.xml` ЦЕЛИКОМ. Потолок чтения — 32К токенов
  ≈ 115 КБ, то есть ≈306 узлов при измеренных 417 Б/узел (`docs/graph.md` §7). Выше потолка шаг
  **встаёт** с числом байт: индексной формы этот срез не строит, её цена не измерена ни разу
  (`docs/intake.md` §3)
- **выход:** `.agent/frd.xml` (через `.agent/staging/frd.xml`) — грамматика, а не проза: артефакт
  читают СКРИПТЫ шагов 7 и 8, а `steps/design/design.mjs::checkDesign` уже сегодня объявляет своим
  входом `{ scenarios, touched }`. Проза живёт внутри тегов (`<pre>`, `<post>`, `<step>`, `outcome`)
- **проверка:** `checkFrd({ path })` → `steps/intake/frd.mjs::newFrd` — семь правил F1..F7,
  объявленных один раз в `docs/intake.md` §5: состав сценария использования; `touched` резолвится в
  УЗЕЛ графа; форма дельты из закрытого словаря и её узел объявлен `touched`; сценарий различающий;
  у каждой величины назван источник из закрытого словаря, и число из него встречается в источниках
  (`numbersIn` из `steps/brd/brd.mjs` — тот же код, что судит `fit` на шаге 2); карта отказов 1:1 с
  расширениями; хотя бы одна дельта
- **оператор:** есть — та же рельса, что у `brd` (`setPending` → `checkpoint` → `answers`), но
  вопросы едут **ПАКЕТОМ**: роль собирает все пробелы и задаёт их одним нумерованным списком, оператор
  отвечает одним сообщением. Два бюджета считают разное — `questions` (сколько вопросов за прогон,
  60) и `questionRounds` (сколько выходов к оператору, 3); вопрос НЕ тратит `LOOPS`. Дорог не вопрос,
  а круг: роль перечитывает BRD и карту целиком на каждом (`docs/intake.md` §2)
- **модуль, которого ещё нет, объявляется:** `<delta form="Added" node="<путь>" new="yes"/>` —
  единственный случай, когда `node` не узел карты, и проверка идёт в обратную сторону (F3n,
  `docs/intake.md` §4). Без него «создать новый файл» выразимо только как `Unknown`, и полоса встаёт
  на шаге 7 — живой прогон `b857d4a0`, разбор в `docs/design.md` §9
- **`Unknown` — обязательная форма, а не украшение:** роль, не сумевшая классифицировать операцию
  (узла нет, два кандидата, операция вне графа), объявляет это, а не выбирает правдоподобную дельту.
  Артефакт с `Unknown` СДАЁТСЯ — отказывает шаг 7, и вопрос оператору задаёт он
- **источник содержания роли:** навык `requirements-intake`
  (`izi-flow/skills/lib/requirements-intake/SKILL.md`) — Cockburn use cases, словарь данных, карта
  режимов отказа, элицитация вместо изобретения. Его собственный запрет «не проектируй модули и не
  пиши код» стоит в `$START_FORBIDDEN` роли
- **здесь же выполнимость:** первый шаг, у которого есть оба операнда — чего хотят и что есть
- **гейт в конце шага, ПОСЛЕ зелёного чека — второй выход к оператору (D23):** узел ширины, которого
  не исполняет ни один сьют репозитория, спрашивается ЗДЕСЬ, одним словом `suite | drop | accept`
  (`docs/intake.md` §6). На красном чеке гейт молчит — там рельса роли. `accept` едет в
  `.agent/answers.md` и снимает у шага 11 блокер `unverifiable-node` про этот узел; `suite`/`drop`
  останавливают полосу, и FRD не промотируется. Цена молчания измерена прогоном `21dd9b34`: 167 805
  токенов и 5 запусков ролей между зелёным шагом 6 и той же остановкой на шаге 11

### 7. `weight` — вес по SemVer · script · **есть** (S22)
Полная карточка — `docs/weight.md`; здесь только место шага в полосе.
- **вход:** `.agent/frd.xml` — только элементы `<delta form>`; прозу `from`/`to` шаг не читает
- **выход:** `.agent/mode` (`patch | minor | major`) — одно слово
- **проверка:** `weight({})` → `steps/weight/weight.mjs::newMode` — вес есть **максимум по формам**
  (`Added → minor`, `Changed`/`Removed` → `major`, `Fixed → patch`), свёртка коммутативна, поэтому
  порядок дельт в артефакте на результат не влияет
- **форма выбирается по эффекту для СУЩЕСТВУЮЩЕГО вызова**, а не по грамматике фразы — определения
  живут один раз, в роли шага 6 (`steps/intake/intake.md`, STRATEGY §8). Пятая форма `Fixed`
  (контракт не двигается, поведение `wrong → right`) введена S22: без неё исправление дефекта
  попадает под `Changed` и весит `major`, то есть `patch` недостижим никогда, а ветка «одноузловой
  `patch` дизайна не требует» ниже — мёртвый код (`docs/weight.md` §2-§3)
- **`Unknown` → веса нет:** терминальный `blocked`, названа КАЖДАЯ неклассифицированная операция со
  своим `why`. Стоявший с прошлого прогона `.agent/mode` при этом **стирается**: `newRun` уносит в
  `.agent/prev` состояние прогона, но не артефакты, и шаг 8 прочитал бы вчерашний вес
- **оператор:** внутри шага нет — шаг без роли не открывает `checkpoint`, как `task`, `survey-plan` и
  `graph`. Ответ на `Unknown` применить некому: переписать FRD может только роль `intake`, поэтому
  оператор кладёт ответ обычным каналом и перезапускает прогон
- **следствие веса:** `minor` (аддитивно) тянет за собой узел тогла в плане — способность едет
  ВЫКЛЮЧЕННОЙ. Это выводится из веса, а не решается отдельно на шаге 10
- **живое доказательство (S22, форма `runbox/quarkus-rest-json-app-v2-t1-3`, прогон
  `682b6a71-8b8b-442f-8a1f-6e02ab2effb2`):** `track:"ok"`, `.agent/mode` = `minor`,
  `function/weight/1` → `{ mode: "minor", earned: "GET /fruits (Added)", deltas: 1 }`. На ТОМ ЖЕ
  `TASK.md`, где прогон S21 дал `form="Changed"` и, значит, `major`, роль под новыми определениями
  написала `form="Added"` — расхождение A закрыто определениями форм, а не арифметикой ядра. Шаг 7 —
  0 токенов; весь прогон 5 запусков ролей и 170 347 токенов

### 8. `ripple` — подграф изменения и нужен ли дизайн · script · **есть** (S23)
Полная карточка — `docs/ripple.md`; здесь только место шага в полосе.
- **вход:** `.agent/appgraph.xml`, `.agent/frd.xml`, `.agent/mode`
- **выход:** `.agent/design` (`needed | skip`) и `.agent/ripple.xml` — подграф изменения
- **проверка:** `ripple({})` → `steps/ripple/ripple.mjs::newRipple`. Флаг отвечает на вопрос, ради
  которого шаг 9 существует — держать контракты согласованными и отрисовать поток данных, пока работа
  не разрезана на тикеты, — а НЕ на вопрос SemVer: `major` и `minor` → `needed` всегда (контракт
  двинулся), `patch` → по ШИРИНЕ изменения. Ширина = `<touched>` ∪ узлы дельт, минус тестовые узлы
  (`docs/ripple.md` §3, расхождения A и E)
- **узел, создаваемый изменением, в подграф не входит:** вырезать вокруг несуществующего файла
  нечего; в ширину он входит, а роль шага 9 берёт его из `frd.xml`, который её наряд несёт целиком
- **подграф считается отдельно от флага:** затравки — узлы дельт ∪ `<touched>` ∪ `nodes` сценариев
  (третий источник обязателен: транзитному узлу маршрута роль шага 9 берёт контракт только отсюда),
  соседи — радиус 1 в обе стороны; в ШИРИНУ узлы сценария при этом не входят — тикета они не
  получат. Тестовые узлы в рябь не входят: тест едет в тикет со своим модулем через
  `<test path suite>` — то же правило, что F2/F3 шага 6
- **зачем второй артефакт:** подграф — вход шага 9. Весь `appgraph.xml` роли не отдаётся: рябь уже
  посчитана, а на живом репозитории граф целиком не влезет в окно
- **оператор:** внутри шага нет; любой отказ терминальный, и оба артефакта при нём **стираются** —
  иначе шаг 9 поехал бы на вчерашнем вердикте (тот же довод, что у `.agent/mode`, `docs/weight.md` §4)
- **живое доказательство (S23, форма `runbox/quarkus-rest-json-app-v2-t1-3`, прогон
  `e4ebbca9-b282-4f39-b1c7-baad5c4ee92f`, сессия `019ff332`):** `track:"ok"`, `.agent/design` = `skip`
  (по правилу того дня; под нынешним тот же FRD даёт `needed` — прогон доказал ВЫРЕЗКУ, не флаг),
  `.agent/ripple.xml` из 3 узлов. `function/ripple/1` → `{ design: "skip", mode: "minor", seeds: 1,
  nodes: 3, total: 15 }` — читается из `journal.json`, не из вывода модели. Подграф: `FruitResource`
  (`seed="yes"`, оба своих `<test>` при нём), `Fruit`, `fruits.html`; тестовых узлов ноль, `fanin`/
  `fanout` ни на одном, `cut` не понадобился — за радиусом 1 здесь не осталось ни одного соседа.
  Новое правило F4 (`nodes` сценария резолвятся в узлы карты) роль прошла с первой попытки:
  `function/checkFrd/1` зелёный, ноль пере-делегаций шага 6. Цена: шаг 8 — 0 токенов; весь прогон 4
  запуска ролей (gilb×3, intake×1) и 81 016 токенов — вдвое дешевле прогона S22, потому что рой
  шагов 4-5 целиком взялся из кэша `.izi/parts`

### 9. `design` — словарь изменения · role `valuer` · под условием · **проход A** (D32)
Грамматика и правила — `docs/data-flow.md` §4a; здесь только место шага в полосе.

**Шаг переписывается, и сегодня он кончается после прохода A.** Проходы B (граф узлов) и C
(маршруты) снесены вместе с их роями; `.agent/design-graph.xml` и `.agent/data-flow.md` не пишет
ничто. Полоса останавливается сразу после словаря и говорит об этом прямо — идти в шаг 10 нельзя:
он принимает отсутствующий дизайн как ЗАКОННЫЙ вход и молча нарезал бы тикеты с пустым `dod` на
каждом узле и без рёбер, которые дают маршруты (прогон `c6bc2e54`). Архив снесённого — `docs/attic/`.

- **условие:** `.agent/design == "needed"`. Отдельной квитанции пропуска НЕ существует: маркером
  служит сам файл `.agent/design`, который пишет шаг 8 — `skip` в нём и есть «решено пропустить»,
  отсутствие файла — «шаг 8 ещё не отработал»
- **вход:** `.agent/frd.xml` и `.agent/ripple.xml`. Больше ничего: гардрейл прохода A судит словарь
  по этим двум документам, и оба целиком доступны скрипту
- **выход скрипта (сначала):** `.agent/staging/values-skeleton.xml` — СОСТАВ словаря целиком: строка
  на каждый конец use case с готовым `closes` и стороной, строка на каждый `<api>`/`<decl kind=method>`
  узла ширины изменения, списанный из ряби дословно; текст ветки с кодом отказа — «статус код».
  0 токенов. Замер: `eddi` — 64 строки, 40 заполнено скриптом; `t2` — 13 строк, 7 заполнено
- **выход роли:** тот же файл с заполненными пустыми `text` (`eddi` — 24 строки, `t2` — 6).
  Роль не решает ни сколько строк, ни какой у них id, ни что они закрывают
- **выход шага:** `.agent/values.xml` — промоут после зелёного чека, с нормализацией: леса, которые
  читала роль (`side`, `end`, `src`, `form`), снимаются, наружу идёт `<value id text closes/>`
- **проверка:** `design({ path })` → `steps/design/values.mjs::checkValues`. Судится ЗАПОЛНЕНИЕ, а не
  состав: строка потеряна, строка дописана, `closes` переприписан, заполненный скриптом текст правлен,
  пустой оставлен пустым. Чек идёт **по staging**, до промоута
- **гейт стирает:** `design({})` без аргументов читает флаг и уносит всё, что не зелено СЕЙЧАС, — и
  безусловно уносит артефакты снесённых проходов, чтобы файл прошлой версии полосы не был спланирован
- **оператор:** рельса есть (`kind: "question"`), но вопросов у роли не бывает: фраза требования про
  каждый конец стоит в самой строке скелета
- **срез:** `steps/design/{valuer.md, order-values.tpl, values.mjs, values.test.mjs}`
- **живое доказательство (форма `runbox/eddi`, прогон `05018aba`):** скелет 64 строки / 40 заполнено
  скриптом / 24 роли; `design/values: зелен — значений 64` с ПЕРВОЙ попытки; **один** запуск роли,
  33 838 токенов, **$0.038**, 66 секунд. Тот же проход прежней конструкцией стоил на этой форме от
  $0.76 до $3.39 и корректного словаря не дал ни разу
- **почему состав отдали скрипту:** замер по двум живым словарям — `eddi` 72 строки роли против 35
  концов FRD (38 с `closes`, три дубля), шесть групп повторяющегося текста, семь дыр в нумерации.
  Все дефекты — дефекты состава, а состав вычислим из FRD и ряби

### 10. `plan` — план работ · script + вопрос оператору · **есть** (S29)
Полная карточка — `docs/plan.md`; здесь только место шага в полосе.
- **вход:** `.agent/frd.xml`, `.agent/appgraph.xml`, `.agent/mode`, `.agent/design-graph.xml` (если
  шаг 9 отработал), ответы оператора, имя транка фактом `git`
- **выход:** `.agent/plan-index.json` — DAG узлов работы: `kind`, зависимости, команда проверки,
  плюс имя ветки с базой и объявленные пробелы
- **проверка:** `plan({})` (функция расширения) → `steps/plan/plan.mjs::newPlanIndex`
- **первый скриптовый шаг С ОПЕРАТОРОМ:** роли нет, но ответ применяет сам скрипт, поэтому вопрос
  здесь возможен там, где `weight` и `ripple` отказывали терминально. Вопрос ОДИН — ключ задачи
  (`TASK-номер`), единственное значение среза, приходящее извне; всё прочее выведено. Тратит
  `QUESTION_ROUNDS`, не `LOOPS`: пере-делегировать некого
- **узлы — из ШИРИНЫ изменения** (`<touched>` ∪ узлы дельт, минус тесты), тем же кодом, что считает
  её шаг 8: `changeWidth` вынесена в экспорт `steps/ripple/ripple.mjs`, второй копии правила нет
- **порядок — из НАПРАВЛЕННЫХ рёбер карты**, а не из `<dep>`: рябь проецирует ребро в обе стороны
  (радиус 1 туда и обратно), поэтому `<dep>` дизайн-графа взаимны и топосорта не переживают —
  доказано живым дизайн-графом `t3`, два цикла из двух узлов (`docs/plan.md` §2, расхождение A)
- **команда проверки узла:** `suite.one` с подставленным `{class}`/`{path}` на КАЖДЫЙ `<test>` узла;
  `one` пуст → сьют целиком, и это записано в узле. Узел без своего `<test>` (страница, шаблон,
  создаваемый файл) команды не получает вовсе и обязан быть различён узлом `scenario` — иначе
  терминальный `uncovered-node`: закрывать его на шаге 15 было бы нечем, а «сделано» сказала бы роль
- **`kind` из решений шагов 6-8:** `doc` по расширению пути, иначе `code`; `scenario` — на сценарий
  FRD поверх ≥2 узлов кода ЛИБО поверх узла, который не закрывает себя сам; `toggle` — при `minor`
  и только если карта назвала механизм
- **«не найдено» — пробел, а не догадка и не вопрос:** механизма тоглов нет ⇒ узла `toggle` нет,
  спеки нет ⇒ узла `spec` нет; оба уезжают в `gaps[]` и в лог. Заводить первый тогл или первую
  спеку — самостоятельная работа со своим гейтом, ровно как заводить первый тест-сьют (шаг 5)
- **имя ветки — по стандарту, а не по прозе:** `<префикс>/<КЛЮЧ>`, префикс из веса (`patch` ⇒
  `bugfix`, иначе `feature`), навык `steps/plan/git-conventions.md`. База — факт `git`
  (`origin/HEAD`, иначе локальный `main`/`master`, иначе отказ `no-trunk`); ветка здесь НЕ режется
- **срез:** `steps/plan/{plan.mjs, plan.test.mjs, git-conventions.md}`
- **живое доказательство (S29, форма `runbox/quarkus-rest-json-app-v2-t3`, прогон
  `c6bc2e54-c222-4541-9f76-0eb4d7dc4113`, сессия `019ff7d4`):** `track:"ok"`, `function/plan/1` →
  `{ ok:false, ask:true }` (вопрос уехал оператору), `function/plan/2` → `{ ok:true,
  branch:"feature/IZI-3", base:"main", nodes:5, code:3, scenario:2, gaps:["toggle","spec"] }` —
  читается из `journal.json`. 15 минут, 5 запусков ролей, 164 112 токенов. Порядок
  `FruitResource → fruits.html → fruit-card.html → S1 → S2`
- **что вскрыл ПЕРВЫЙ прогон (`03b598c7`) и чего не видели юниты:** переспрос тем же текстом не
  доходит до оператора вовсе (`askOperator` судит «отвечено» по тексту вопроса — три круга сгорели
  за секунды), и два правила этого шага противоречили друг другу на узле без теста, различённом
  одноузловым сценарием. Разбор — `docs/plan.md` §12

### 11. `review` — критик судит план · role `critic` · цикл починки · **новое**
- **вход:** `.agent/plan-index.json`, `.agent/frd.xml`
- **выход:** `.agent/review.md` — `Pass | Reject(Blocker[])`
- **проверка:** каждый блокер назван с уликой (узел, файл, команда)
- **цикл:** `until(pass, fix)` до K раундов, дальше — оператору. Отрицательный вердикт есть данные:
  критик, нашедший блокер, отработал успешно

### 12. `gate1` — оператор утверждает план · human · **новое**
- **вход:** `.agent/plan-index.json`, `.agent/review.md`
- **выход:** `.agent/gate1.json` — токен акцепта
- **проверка:** присутствие токена, а не проза
- **оператор:** план показывается **в функциональном стиле** — узел, вход, выход, команда проверки.
  Ветка с базой и тогл с состоянием по умолчанию видны ЗДЕСЬ, а не всплывают после гейта: оператор
  утверждает и их тоже. `/izi-gate1 <правки>` кладёт замечания файлом, Approve без правок означает
  «беру план»

### 13. `branch` — короткая ветка от свежего транка · script · **новое**
- **вход:** `.agent/gate1.json` (токен акцепта), `.agent/plan-index.json` → `branch: {name, base}` —
  имя по соглашению **из графа**, база = транк; оба решены шагом 10, здесь не пересматриваются;
  `.agent/appgraph.xml` — список `<suite>` для базлайна
- **выход:** `.agent/branch.json` — `{name, base, baseSha, remote, baseline}`. `baseSha` нужен шагу
  16: диск изменений считается от базы ветки, а не от «как-нибудь»
- **базлайн — до первой строчки работы:** сразу после среза скрипт гоняет **все** сьюты графа.
  Ветка равна свежему транку, поэтому зелёный прогон доказывает релизопригодность транка (Git-TBD), а
  красный называет дефект чужим — без этого якоря красный сьют на шаге 16 не улика. Красный базлайн →
  `blocked` с именем сьюта и вопросом оператору: чужое молча не чиним
- **чистое ядро:** `steps/branch/branch.mjs::newBranch({gate, planBranch, git})`, где `git` — снимок
  фактов `{dirty, refs[], trunkSha, remote}` от io-функции расширения `gitFacts`. Ядро судит, но не
  режет; отказы: `no-gate` · `no-branch-name` · `dirty-worktree` · `branch-exists` · `no-trunk`
  (база плана не найдена среди рефов)
- **io:** функция расширения `cutBranch({name, base})` — `git fetch <remote> <base>`, затем
  `git switch -c <name> origin/<base>`; она же пишет `.agent/branch.json`
- **свежесть — факт, а не память:** ветвимся от `origin/<base>` сразу после `fetch`, отставший
  локальный транк в ветвлении не участвует вовсе. Нет remote → база = локальный `<base>`,
  `remote: null` в артефакте — объявлено, а не умолчано
- **оператор:** нет вовсе. Любой отказ — терминальный `blocked` с уликой: грязную рабочую копию и
  занятое имя ветки чинит человек и перезапускает. Роли на шаге нет → 0 токенов, окно SLM не тратится
- **шов:** убрать `fetch` перед `switch` → ветка режется от вчерашнего транка; юнит «`trunkSha` не
  равен голове удалённого транка ⇒ отказ» краснеет

### 14. `tickets` — наряды исполнителям по узлам плана · script · **новое**
- **вход:** `.agent/plan-index.json`, `.agent/frd.xml`, `.agent/appgraph.xml`, `.agent/branch.json`,
  `.agent/design-graph.xml` и `.agent/data-flow.md` (если шаг 9 отработал)
- **выход:** `.agent/tickets/<узел>.md` — по одному на узел плана; порядок исполнения не изобретается,
  это топосорт, уже лежащий в плане
- **как:** проекция «узел × его сценарии из FRD × `dep`-соседи из графа» + подстановка в шаблон,
  выбранный полем `node.kind` — тем же приёмом, что два наряда разведки на шаге 4. Пять шаблонов
  `steps/tickets/tpl/{code,doc,toggle,scenario,spec}.tpl`; формулировки вычитаны человеком один раз,
  а не сочиняются на каждый тикет. Роли на шаге нет — 0 токенов
- **форма тикета — под слабый тир** (`docs/concept.md`, «Тикет читает слабая модель»): секции
  `$START_<СЕКЦИЯ>`/`$END_<СЕКЦИЯ>`, как в роли и наряде; одно правило — одна секция, пересказов нет;
  `$START_EXAMPLE` несёт вырезку **из этого репозитория**, а не из чужого домена
- **секции `code.tpl`:** `$START_CONTEXT` (узел, ветка из `branch.json`) · `$START_CONTRACT`
  (antecedent/consequent, `dep`-соседи по строке) · `$START_EXAMPLE` (сосед того же рода и его тест,
  пути ИЗ ГРАФА; для импорта, не резолвящегося в узел, — вырезка **реального вызова** из этого
  репозитория) · `$START_SKILL` (только если оператор дал навык на шаге 10; **единственная секция не
  из репозитория**, поэтому путь-источник обязателен) · `$START_TASK` (дельта из FRD) ·
  `$START_TESTS` (источник — одноимённая секция `.agent/data-flow.md`, вырезка по `path`, тем же
  приёмом, что `$START_FLOW`: юниты по формуле `1 happy + Σ ветвей` — это `<dod>` тикета, отдельного
  шага «написать тесты» нет; плюс различающий сценарий; шаг 9 пропущен (`design=skip`) → секции нет,
  источником `<dod>` остаётся различающий сценарий FRD) · `$START_CHECK` (команда узла из плана) ·
  `$START_FLOW` (вырезка маршрутов шага 9, где участвует узел; шаг 9 пропущен → секции нет) ·
  `$START_FORBIDDEN` · `$START_DONE`
- **разметка кода выведена, а не сочинена:** `$START_CONTRACT` тикета ← `<contract>` узла из
  `design-graph.xml`, `MODULE_MAP` ← его `<dep>`-соседи, `$START_FLOW` ← `data-flow.md`. Разметка —
  протокол передачи знания следующему агенту (`docs/concept.md`, следствие 4), но у каждой секции
  обязан быть источник: секция без источника неотличима от выдумки модели
- **`<contract>` есть СЕЧЕНИЕ ИЗМЕНЕНИЯ, и секция тикета наследует именно это** (`docs/data-flow.md`
  §4): она говорит, что принимает и отдаёт РАБОТА, а не файл целиком. Шапка `CONTRACT` в исходнике
  описывает файл, а файл может нести и то, чего изменение не касается, — значит она из этой секции НЕ
  выводится один в один. Полная поверхность файла лежит в `appgraph.xml` (`<api>`, `<decl>`), но
  шаг 15 на вход получает ТОЛЬКО тикет, поэтому «чем заполняется шапка файла» — вопрос шага 14, а
  шага 14 ещё нет. Решение отложено вслух: `docs/triggers.md`, строка 21
- **проверка:** тикет **самодостаточен** — ни одной ссылки на артефакт, которого в нём нет («см.
  `appgraph.xml`» для слабого тира с окном 128K есть недоступное знание, а не ссылка); каждый
  `dep` тикета разрешается в другой тикет или в существующий узел графа; секции закрыты парами
  `$START_*`/`$END_*`; пути в `$START_EXAMPLE` существуют в графе — вырезка берётся с диска, а не
  сочиняется
- **бюджет окна — не рельса этого шага:** узел, не влезающий в тикет, режет шаг 10; здесь перебор —
  утверждение инварианта, и его срабатывание означает дефект шага 10, а не работу для оператора

### 15. `implement` — исполнение тикетов · role **веер** · цикл починки · **новое**
- **вход:** `.agent/tickets/<узел>.md` — **только тикет**, ничего больше: самодостаточность тикета
  проверена шагом 14 именно затем, чтобы этот шаг не подавал роли ни граф, ни план, ни FRD
- **выход:** файлы узла в рабочем дереве ветки — код и его тесты
- **git запрещён исполнителю целиком:** `$START_FORBIDDEN` тикета запрещает любую git-команду
  (`commit`, `checkout`, `branch`, `stash`, `reset`, `rebase`), права записи — только файлы узла.
  VCS-эффекты живут в двух шагах: 13 режет ветку, 17 коммитит принятое и открывает PR. **Во время
  реализации не коммитят вовсе** — работа копится в рабочем дереве, поузлового отката средствами git
  нет, испорченный узел чинится ещё одним раундом цикла
- **TDD одним тикетом:** тест по формуле и код пишет один исполнитель; отдельный узел на тест есть
  только там, где тест и так узел (`scenario`)
- **цикл — скриптовый, роль внутри него только работает:**
  ```
  until( green(checkFile(node)) or round > 6 )      // K = 6, литерал рядом с LOOPS/QUESTIONS
     implement(ticket, feedback = lastOutput)        // РОЛЬ: файлы, ничего больше
  green ? done : ask(operator, lastOutput)           // СКРИПТ: чек и пауза, коммита нет
  ```
- **`checkFile` — тест УЗЛА, а не сьют целиком:** команду собрал шаг 10 (`suite.one` с подставленным
  файлом), поэтому раунд стоит секунды. `one` в графе пуст → гоняется сьют целиком, и цена пишется в
  лог, а не умалчивается
- **зелёный → узел закрыт**, файлы остаются в рабочем дереве без коммита; красный → вывод чека
  едет в `$START_FEEDBACK` следующей пере-делегации. Роль себя не сертифицирует: «сделано» говорит
  код возврата
- **после `K = 6`** — пауза с вопросом оператору и последним выводом чека, а не падение прогона:
  прочие узлы ветки уже сделаны и закоммичены
- **веер:** батч — узлы одного уровня DAG **без общих файлов** (файлы известны из плана), остальные
  едут следующим батчем; размер батча — `maxParallel` pi. Worktree-изоляция не берётся: она меняет
  конфликт записи на конфликт мержа, решать который некому

### 16. `accept` — приёмка ветки · script · **новое**
- **вход:** `.agent/branch.json` (`baseSha`, базлайн), `.agent/plan-index.json`, `.agent/appgraph.xml`
- **выход:** `.agent/accept.json` — `{suites: {<id> → green|red}, covered[], extra[], verdict}`
- **проверка:** все `<suite>` графа зелёные, порядком `unit → component → contract` (плюс валидатор
  спеки, если был узел `spec`); файлы **каждого** узла плана присутствуют в диффе `baseSha` ⋃
  untracked — узел, по которому никто ничего не сделал, ловится здесь. Дифф берётся с рабочего
  дерева, а не из журнала: коммитов в реализации нет
- **красный сьют — адресно:** имя упавшего теста → путь → узел плана. Ровно один узел — вывод чека
  едет фидбеком в цикл шага 15 этому узлу. Двое, ни одного или тест не резолвится — регресс между
  узлами, виновника вычислить нечем: пауза с вопросом оператору и выводом чека
- **файл вне плана — не отказ:** при зелёном сьюте остаётся, но объявлен — едет в `extra[]` и оттуда
  в тело PR. Молчаливой правки за пределами плана не бывает
- **почему это улика:** базлайн шага 13 был зелёным до первой строчки работы, значит красное здесь —
  наше, а не наследство репозитория

### 17. `pr` — PR и ссылка оператору · script · **новое**
- **вход:** `.agent/accept.json` (**зелёный** — иначе шага нет вовсе), `.agent/plan-index.json`,
  `.agent/branch.json`
- **выход:** открытый PR от ветки к транку и **ссылка оператору в терминале pi** — конец полосы
- **коммит здесь, один на всю работу:** принята ветка целиком, история отражает принятое решение, а
  не путь к нему. Сообщение и заголовок PR — **по соглашению репозитория** (четвёртый вопрос графа;
  ответ пуст → фактом из `git log` транка), затем `push -u`
- **тело PR выведено из артефактов:** узлы плана с командами проверки · сьюты с результатом из
  `accept.json` · узел тогла с состоянием OFF · `extra[]` отдельным списком. Прозы без источника нет
- **хостинг — факт:** `git remote get-url` даёт хост, наличие `gh`/`glab` — способ. Нет ни того ни
  другого → `blocked` с именем ветки и готовой командой, чтобы человек открыл PR сам
- **оператор:** получает ссылку. Дальше — ревью человеком и мерж: полоса кончилась

---

## 4. Тесты — минимум, но со швом

На срез: **один тест на правило, которое может тихо деградировать**, и чистое ядро по формуле
`1 happy + Σ ветвей антецедента`. `ext/index.mjs` — io-модуль (подключает `task.mjs`/`brd.mjs`/
`core/answers.mjs` к диску) и юнитами не покрывается по той же причине, что и бывшие `bin/*.mjs`
гардрейлы: доказывает его живой прогон, а не тест, играющий роль интеграционного под именем юнита.
Тест, который ни от какой правки кода не краснеет, — комментарий; такой не пишем.

## 5. Порядок разработки

Срезы идут по одному, каждый — до зелёного прогона, прежде чем начинать следующий:

```
survey-plan → scope(рой) → graph → intake → weight → ripple → design → plan → review → gate1
             ↑ сдано S29                                                          ↑ следующий срез
             → branch → tickets(рой) → implement(рой) → accept → pr
```

Первые три дают `appgraph.xml` — после него полоса впервые знает о репозитории хоть что-то, и
дальнейшие шаги перестают быть догадкой.

**S11 разобрала генерический диспетчер (манифест `step.json`, цикл по `pipeline.order`,
универсальный `move(s)` на любой `kind`), который §1-§2 несли до этой правки** — оператор явно
велел выбросить универсальность, рассчитанную на двенадцать шагов, пока их два. Отсюда прямое
следствие для `survey-plan`: он больше не «включается в уже готовый диспетчер, знающий `script`» —
диспетчера в этом смысле не существует, есть `task()`/`brd()` по имени. Добавление `survey-plan`
означает: либо третья именованная функция-фаза рядом с `task()`/`brd()` (та же рука, тот же стиль,
ещё один литерал бюджета), либо к этому моменту цена ручного перечисления фаз превысит цену
policy-as-data и `pipeline.json`/манифест стоит вернуть — это решение принимается ТОГДА, с фактами
трёх шагов на руках, а не сейчас с фактами двух (`docs/concept.md`, «Что отложено и почему»).
