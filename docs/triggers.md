# Отложенные решения и их триггеры

Не задачи. Работы по ним сейчас нет — это решения, отложенные ВСЛУХ, с записанным условием, при
котором мы к ним вернёмся.

Правило проекта — не обобщать под то, чего не наблюдали (`CLAUDE.md`, `$START_FORBIDDEN`). Но молча
отложить нельзя: тогда «не подумали» неотличимо от «решили отложить». Поэтому у каждого решения здесь
есть событие, которое его разбудит, и цена, которую мы платим, если возьмём его раньше.

Прецедент, что это работает: `patch` был недостижим весом с самого начала, и заметил это не разбор, а
живой прогон; пятое слово `Fixed` появилось по факту, а не по гипотезе (`docs/weight.md` §2-§3).

| # | что отложено | триггер | цена, если брать вперёд |
|---|---|---|---|
| 1 | ряд `patch → by-width` в `DESIGN_TABLE` (`steps/ripple/ripple.mjs`) | первый живой прогон, где `mode=patch` и изменение трогает больше одного узла. Не случится ни разу — ряд удаляется вместе с веткой `skip` | правило, которого никто не наблюдал, живёт в коде и в четырёх доках |
| 2 | радиус подграфа ряби больше 1 (`docs/ripple.md` §4) | первый прогон, где `checkDesign` правило 1 краснеет на узле маршрута, которого нет в `.agent/ripple.xml` | окно роли `designer` оплачивается за догадку: замыкание по рёбрам равно связной компоненте |
| ~~3~~ | ~~тикет узлу, который тронут, но дельты не несёт~~ | **ЗАКРЫТО S29:** план берёт узлы из ШИРИНЫ, то есть из `<touched>` — `fruits.html` получает и тикет, и сценарий, который его закрывает (`docs/plan.md` §3, §12) | — |
| 4 | атрибут `breaking="yes\|no"` на форме `Changed` (`docs/weight.md` §3) | первый живой FRD, где существующий элемент контракта изменился и остался совместимым (`int` → `long`, enum с новым значением) | второй ФАКТ на дельту вместо смысла существующему решению |
| 5 | цикл 6↔7 на `Unknown` — вопрос оператору из шага 7 с пере-делегацией шага 6 (`docs/weight.md` §5) | живой прогон покажет `Unknown` чаще, чем раз в полосу | ещё один бюджет, петля в программе и вход в `askOperator` |
| ~~6~~ | ~~индексная форма карты выше потолка чтения~~ | **ЗАКРЫТО:** репозиторий пришёл — карта eddi 120 050 Б при потолке 117 760 (прогоны `fa8def32`, `fb57f506`), и цена формы измерена НА НЁМ: индекс 30 229 Б, четверть размера. Выше потолка шаг 6 получает индекс с `form="index" without="decl role io edge"`, отказ остался там, где сам индекс не влезает | — |
| 7 | правило «каждый `<delta node>` FRD — узел дизайн-графа с атрибутом `delta`» (`docs/design.md` §4) | первый живой прогон, где дельта шага 6 приехала в дизайн-граф транзитом, без атрибута `delta` | шестое правило поверх цепочки, которая его почти покрывает: F3 шага 6 требует, чтобы узел дельты был объявлен `<touched>`, правило 5 `checkDesign` требует `touched` в маршруте, правило 1 требует узел маршрута в дизайн-графе |
| 8 | провенанс слова `delta` в дизайн-графе: узел подграфа с `delta` обязан быть узлом `<delta node>` ИЛИ `<touched>` FRD | первый прогон, где роль пометила дельтой узел, которого FRD не объявлял тронутым, — и шаг 10 завёл на него тикет на пустом месте | прогон `ffe8cb7b` дал ровно этот случай и дал его ВЕРНО: `fruits.html` тронут FRD, дельты не несёт (страница контракта не двигает, `docs/ripple.md` §3), и `delta="Changed"` назначил дизайнер. Правило, введённое сейчас, судило бы работу, которую никто не сделал неправильно |
| 9 | расщепление `in` на «вызовы» и «возвраты» (два поля вместо одного списка альтернатив) | первый прогон, где возвратное значение подставлено как вызов В СЕРЕДИНЕ маршрута: стык `out(k) ∈ in(k+1)` сошёлся, а строка потока лжёт. На границе маршрута этот случай уже закрыт — вход называется номером (`entry`, S25), и роль обязана объявить входную альтернативу | различать типы альтернатив можно, только зная НАПРАВЛЕНИЕ перехода, а правило 3 намеренно судит ребро в обе стороны: возврат разматывается по тому же `<dep>`. Значит цена — не поле, а направленность всей грамматики маршрута |
| 10 | рёбра МЕЖДУ двумя создаваемыми модулями (`<delta new="yes">`) | первый FRD, где два новых узла зависят друг от друга: `steps/ripple/ripple.mjs` строит соседство только из рёбер существующей карты, и связать их нечем — заметит это правило 3 `checkDesign` на шаге 9, потребовав `<dep>` между ними | наблюдался ровно один новый лист-узел (прогон `b857d4a0`); механизм под связный подграф новых модулей — обобщение под ненаблюдавшееся, и цена ему не одна строка, а вторая грамматика рёбер |
| 11 | `kind` из словаря, но не тот (роль объявила `invalid`/`escalate` там, где по содержанию спрашивала) | первый прогон, где роль назвала рельсу неверно и полоса встала терминально при живом вопросе (прецедент — `fcc4c120`: `kind` вовсе отсутствовал, схема S28 это уже ловит; следующий шаг — тот же прогон с `kind` из словаря, но не `question`) | классификатор, угадывающий намерение по прозе `subject`, — ровно то умолчание, против которого весь конвейер (`standards/code.md`, правило 3: «Unknown — рельса, не умолчание») |
| 12 | узел `spec` (`kind`, ребро API-first, команда валидатора) — шаг 10 сегодня объявляет пробел `gaps: ["spec"]` | первый репозиторий, где карта отвечает `<contract found="yes">` со спекой И её валидатором | команда валидации сочиняется — тот же invented-default, что число без источника, только дороже: она либо не запустится, либо проверит не то |
| 13 | приоритет удалённо переключаемому механизму тогла (`docs/plan.md` §6) | первый репозиторий, где механизмов тоглов БОЛЬШЕ ОДНОГО | хребет несёт один элемент с одним атрибутом (`<toggles mechanism>`, `steps/scope/part.mjs::SPINE_ANSWERS`) — выбирать не из чего; цена вперёд это правка роли скаута, наряда хребта, `part.mjs` и `graph.mjs` под список |
| 14 | бюджет окна тикета и нарезка переросшего узла (`docs/concept.md`, «План, критик, гейт») | шаг 14 `tickets`, когда он появится: формула проекции живёт в ЕГО шаблонах | считать проекцию нечем — обобщение под шаг, которого нет (`CLAUDE.md`, `$START_FORBIDDEN`) |
| 15 | типизированный вопрос об идиоме чужой библиотеки — `vendor` / `skill` / пример (`docs/concept.md`, «Чужая библиотека — факт, а не память модели») | первый `<gap>`/`<ambiguous>` карты на импорт, не резолвящийся в узел и не имеющий ни одного вызова в репозитории | на всех наблюдавшихся формах таких импортов ноль: классификатор ответа, ветка `kind: "vendor"` шага 3 и секция `$START_SKILL` — механизм под ненаблюдавшийся случай |
| 17 | детали по требованию: роль запрашивает узел и получает его блок, вместо файла целиком | репозиторий, чей ИНДЕКС не влезает в потолок. Считается: ~185 Б на узел, значит индекс держит ~600 узлов, а полный eddi это 1850 | контракт шага 6 меняется с «прочитал файл» на «умеет спрашивать» — это инструмент у роли, второй проход и новый бюджет |
| 18 | `<role>` как источник непредсказуемости размера карты | прогон, где потолок роли (`MAP_ROLE_CAP`) не спас оценку. Замер: `<role>` = 11% карты, p50 108 симв., max 213 — единственное поле, которое пишет модель | сегодня потолок стоит и оценка считает по элементам; резать дальше — это резать то, ради чего роль звали |
| 19 | якорь, называющий половину дерева (`import`, `agent`, `store`) | прогон, где широкий якорь увёл фокус мимо изменения ПРИ работающем правиле аналога. Замер: одно слово определяло три четверти выбора, пока фокус не пошёл от образца | классификатор «хороший якорь / плохой» — это угадывание намерения по прозе, ровно то умолчание, против которого весь конвейер |
| 16 | коммит, тело PR и ревью в навыке `steps/plan/git-conventions.md` | шаг 17 `pr`, когда он появится | соглашение, которого никто не проверяет, есть украшение (`standards/role.md`: у каждого запрета названа его проверка) |
| 21 | чем шаг 14 заполняет шапку `CONTRACT` исходного файла, если `<contract>` дизайн-графа есть СЕЧЕНИЕ ИЗМЕНЕНИЯ (`docs/data-flow.md` §4), а шапка описывает файл целиком | появление шага 14 `tickets`. Раньше решать нечем: секции тикета живут в ЕГО шаблонах, а шаблонов нет | полная поверхность узла лежит в `appgraph.xml` (`<api>`, `<decl>`) и доступна шагу 14 бесплатно — но шаг 15 получает на вход ТОЛЬКО тикет (`docs/workflow.md` §15), значит поверхность пришлось бы класть в тикет отдельной секцией. Проектировать эту секцию сейчас — обобщение под шаг, которого нет (`CLAUDE.md`, `$START_FORBIDDEN`), а цена ошибки в ней — вес всего окна тикета |
| 22 | верифицируемость узла, который изменение СОЗДАЁТ (`<delta new="yes">`) — сегодня R6 его не судит вовсе — узла нет в `askedNodes` (`docs/review.md` §10) | шаг 16 `accept`, который меряет это ФАКТОМ: «команда была красной до и зелёной после». Раньше отвечать нечем — карта построена до того, как файл появился | угадывать, куда ляжет будущий тест и подхватит ли его `match` сьюта, — тот же классификатор «сьют подходит этому файлу», что отвергнут в D21 на одном наблюдении |
| 23 | waiver оператора для прочих кодов с владельцем `operator` (сегодня заведён ровно для `unverifiable-node`, `docs/intake.md` §6) | первый живой прогон, где оператор хочет продолжить полосу вопреки `node-not-required` | второй waiver без наблюдавшегося потребителя — механизм, который снимает остановку, никем не попрошенную снимать |
| 24 | машинный отказ R6 принимать блокер `unverifiable-node` на узле с `new: true` — сегодня такой блокер проходит: R6 создаваемый узел не судит (`askedNodes`), а R2/R3/R4 судят только форму и разрешимость улики | первый прогон, где вердикт критика несёт этот код на создаваемом узле — видно в `result.json` прогона либо в `function/review/N` журнала | дефект не наблюдался: роль про такой узел не спрашивают вовсе (`{UNCHECKED}` его не несёт, а наряд называет его отдельной строкой), и правило под ненаблюдавшийся случай — запрещённое обобщение (`CLAUDE.md`, `$START_FORBIDDEN`). Цена ожидания названа и невелика по вероятности, но велика по сумме: владелец кода — `operator`, значит `band()` берёт верхнюю ветку и прогон кончается терминальным `escalate` на находке, у которой нет предмета; на форме `t3` это ~370k токенов целиком |
| 20 | провенанс СЛОВА `delta` через три прохода шага 9: слово, которым проход B пометил узел, обязано быть словом, которым шаг 6 пометил тот же узел (соседняя половина строки 8 — та про узел без `<touched>`, эта про изменённое слово у объявленного) | второй наблюдённый случай, ИЛИ прогон, где переписанное слово доехало до шага 7/10 и изменило вес или тикет. Одного мало: сегодняшний случай объяснён целиком чужим фидбеком, и D13 убрал причину | **дефект наблюдён, и правило всё равно отложено.** Прогон `a900de7b`: проход B в круге 3 переписал `delta="Changed"` на `"Added"` у узла, который FRD объявил тронутым. Не видит этого ни одно правило — `checkGraph` (`steps/design/nodes.mjs`) судит принадлежность слова СЛОВАРЮ форм, а не совпадение со словом шага 6. Предчувствие записано в `docs/design.md:290` ещё после `ffe8cb7b`. Цена правила: `.agent/frd.xml` пришлось бы читать в гардрейле прохода B, сопоставляя `<delta node>`/`<touched>` с узлами графа, — и это тот же провенанс, что строка 8 отложила по своей причине. Цена дефекта сегодня ниже: вина адресована построчно (D13), и роль B прямо предупреждена, что `delta` не чинит ни один блокер (`designer.md` LAW 6, шов в `nodes.test.mjs`) |
