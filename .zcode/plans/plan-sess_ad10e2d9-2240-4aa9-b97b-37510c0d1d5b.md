Три правки: схема инструмента, шум, документация

1. ФИКС solo_answer (корень ошибки «Cannot read properties of undefined»):
   pi.registerTool ожидает parameters: Type.Object(...) (TypeBox), не input: {type:"object"}.
   answer-tool.ts: import { Type } from "typebox"; parameters: Type.Object({exchange: Type.String(...)}, {additionalProperties: false}); execute(toolCallId, params, ...) вместо run(input, ctx).
   Старый формат izi_answer из step9-rework — образец (ext/answer-tool.mjs:29-44).

2. ШУМ: инлайн-скрипт пишет log() на КАЖДЫЙ role/ask/say — в фоне каждый вызов = отдельная строка-запись в чат (host.ts:235, без throttling). В step9-rework был ровно ОДИН log(): say→log(i.line), шаги эмитили say ~раз на шаг.
   Убрать из инлайн-скрипта: log("→ фаза..."), log("⏸ вопросы...") — оставить ТОЛЬКО say→log (карточки и сообщения шагов). Нарратив фаз — не нужен оператору в чате, он в карточке полосы.

3. DATA-FLOW + КОНТРАКТЫ: обновить steps/data-flow.md (или новый) — вход/выход каждого шага, контракты модулей (engine, steps/*, ask, answer-tool, judges). Форма — как steps/brd/data-flow.md из step9-rework.

4. Тесты: solo_answer schema-шейп тест (parameters не undefined); линия зелёная.

5. Перезапуск на quarkus-solo-op: экран чистый (только карточки полосы + вопросы), solo_answer работает.