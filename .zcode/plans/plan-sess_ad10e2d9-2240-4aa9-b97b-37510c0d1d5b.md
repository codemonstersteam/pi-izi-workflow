T81: наблюдаемость + чат-совместимость + RESUME (прогон продолжает с места остановки)

ДИАГНОЗ: (1) полоса молчит — log()/phase() хоста не зовём (в фоне log = строка «Workflow solo: …» в чате, host.ts:223-237); (2) foreground держит сессию → chat-реле solo_answer в очереди → ответы не доезжают; (3) перезапуск начинал бы с нуля — есть наработки в step9-rework (recon), их переносим.

ПРАВКИ:
A. LOG-НАРРАТИВ: engine пишет log() на каждом ходе — «фаза plan: наряд planner (круг N)», «судья: N блокеров», «критик: APPROVE», «вопросы оператору: 5 → в чат», «ответы вписаны (5)», «переход X→Y», «execute: судьи зелёные, 3/3 строк»; phase(name) на переходах. В background это хроника в чате с первого хода.
B. /solo → BACKGROUND (foreground: false): реле оживается (followUp+triggerTurn требует свободную сессию); чат-модель = кнопка + реле пауз (~1 ход на паузу).
C. CHECKPOINT УБРАТЬ: подтверждение — ask-вопрос («да/нет») каналом solo_answer; никаких workflow_respond/runId-рисков (боль izi, runbox.md:281).
D. RESUME (образец — ext/recon.mjs из step9-rework, урок T71 «артефакт написан ≠ пласт принят»):
   - движок пишет МАРКЕР .agent/progress.json на каждом переходе: {phase, round, reachedAt, solveStart};
   - soloStart: если .agent/ есть — восстановить фазу из маркера + сверка с диском: PLAN~draft без маркера план-принят → фаза plan (круг починки с PREVIOUS); PLAN.md есть, ответы не вписаны (РЕШЕНО=0, answers.md полон) → применить и встать в confirm; pending открыт → questions; solveStart есть → execute (коммиты меряются от него);
   - вердиктам верить только по маркеру, не по существованию файла (T71).
E. Текущий прогон quarkus-solo-op: .agent НЕ чистить; answers.md починить (№4 → «Трогать страницу», №5 → «Обрезать» — слова оператора); перезапуск /solo продолжает с questions/confirm: ответы применяются к плану, карточка, подтверждение словами в чате.
F. Файлы: ext/engine.ts (нарратив+маркер+восстановление), ext/prompts/solo.md (background, без checkpoint-ветки), ext/ask.ts (инструкция ответа: чат по номерам ИЛИ answer.txt), тесты: resume-швы (перезапуск после каждой фазы продолжает: plan-красный/plan-принят/вопросы-открыты/ответы-полны/execute-маркер), confirm-as-ask, линия зелёная.
G. Коммит; перезапуск proof на quarkus-solo-op — хроника видна, прогон продолжает с места остановки.