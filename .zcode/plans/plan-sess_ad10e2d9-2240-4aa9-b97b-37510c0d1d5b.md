Доработка по контрактам: 10 правок проектирования (не тесты)

КРИТИЧЕСКИЕ:
1. applyAnswers → Result<{plan, applied, total}> — постусловие «ответы вписаны» проверяется в коде
2. outputSchema: ENVELOPE передан в agent() вызовы plan.ts и execute.ts (Raw → Validated)
3. while + явный round++ — инвариант «обрыв не тратит круг» в коде, не в комментарии
4. repairOrder только при непустых blockers — предусловие проверяется

ВАЖНЫЕ (ложная зелёность):
5. gitHead → Result; judgeSolve при since="" → блокер «git недоступен»
6. RegExp \\bФ\\d+\\b — точное покрытие (Ф10 ≠ Ф1)
7. readAt → Result<string | null> — файла нет ≠ файл пуст
8. askWithRetry .every() — все ответы непусты

ОЧИСТКА:
9. PLAN_DRAFT импортируется из paths.ts (не литерал)
10. extractPath в card.ts (согласованность с судьёй)

Каждая правка меняет контракт функции. Линия зелёная. Коммит.