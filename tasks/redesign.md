**PRIMING**  
Ты программист инженер проектировщик программ


**ЦЕЛЬ**  
Спроектировать и переписать дизайн проекта до Gate 1: построить читаемое дерево модулей workflow, детальный data-flow и AppGraph.xml так, чтобы izi стал единой точкой вызова всех модулей, а весь процесс собирался из единообразных модулей (скрипт + LLM + судья + резка + маршрутизация) в работающую «коробку».
работа с состоянием воркфлоу(история) - отдельный модуль
этот модуль выставляет конкретные АПИ, которые вызывает каждый шаг форкфлоу если нужно

необходимо писать лог выполнения воркфлоу в виде xml
тоже отдельный модуль с понятным АПИ
лог содержит трэйс исполнения процесса

что за данные поступили на каждый шаг
какие ответы пришли от ЛЛМ
какие данные куда были сохранены
по этому логу можно восстановить процесс, найти ошибку и улучшить процесс, оптимизировать
а также восстанавливать стэйт(запасной вариант) и конечно брать данные для заглушек

те если у нас в процессе 3 шага использования ЛЛМ
у нас должно быть минимум 3 теста на шаг
1 успех
2 нарушение - проверяем работу гардрейл
3 отказ ЛЛМ(обрыв связи)

все остальные инвариаенты ошибок ЛЛМ тестируем на уровне гардрейл подавая ошибочные результаты работы ЛЛМ по каждому пункту отдельно
в итоге если гардрейл контролирует 2 кейса должно быть 4 теста
2 теста - 2 правильных кейса
2 теста на ошибки - читай правильность программ и формулу тестов


**Контекст**  
- Не проектировать и не писать полное дерево модулей всего проекта — только до Gate 1.  
- Удалить все тесты. Оставить только тесты строго по формуле тестов на модуль компонентный тест с ЛЛМ
и на тест кадодго гардрейл модуля
который проверяет свое
1) happy path
2) гардрейл поймал ошибку
формулу тестов смотри в ../rationaldev-ai-sdlc

- Все модули должны вызываться исключительно через izi.  
- Модулем верхнего уровня, который вызывает izi брать существующие шаги steps/, напримет intake - это модуль, который состоит из подмодулее и реализует подшаги воркфлоу, но на своем уровне это модуль, который делает одну конкретную задачу
- Изучить и использовать подход к data-flow, который сейчас создает этот воркфлоу в eddi.  
- Изучить, какие дизайн-документы создает на этапе проектирования ../rationaldev-ai-sdlc, и решить, нужен ли отдельный дизайн контрактов между модулями.  
- Обязательно прочитать статьи:  
  https://codemonsters.team/blog/2025/12/15/program-modules/  
  https://codemonsters.team/blog/2025/12/30/program-correctness/  
- Не выдумывать архитектуру «с нуля» — переписать существующий дизайн строго по описанному процессу (проектирование → data-flow → валидация → артефакты → декомпозиция/сшивка).

**ФОРМАТ**  
1. `docs/phase/plan/plan-app-graph.xml` — полный AppGraph.xml workflow до Gate 1.  
2. `docs/phase/plan/plan-data-flow.md` — подробный data-flow в стиле harness/eddi (вход → проверки → обработка → выход → артефакты).  
3. `redesign.md` — план работ + предложение по рефакторингу izi (чтобы workflow был читаемым, все модули единообразны и подключались/вызывались только через izi). 
код должен быть аккуратным как пишет код  ../rationaldev-ai-sdlc по ROP
4. Для каждого модуля в дереве до Gate 1 обязательно указать:  
   - что делает модуль;  
   - данные на вход + как проверяется корректность входных данных;  
   - данные на выход + как проверяется корректность выходных данных;  
   - куда кладутся артефакты которые модуль выдает как результат своей работы;  
   - как в модуле декомпозируется задачи, как сшиваются результаты

**КРИТЕРИИ УСПЕХА**  
- AppGraph.xml и data-flow.md полностью описывают процесс до Gate 1 и проходят валидацию (каждый шаг — череда модулей: скрипт → LLM → скрипт-судья → скрипт-резка → LLM → скрипт-судья → скрипт-маршрутизатор).  
- izi рефакторен так, что workflow читается как последовательность вызовов единообразных модулей. 
а не спагетти код
смотри как бы код написал ../rationaldev-ai-sdlc там четко прописано требование
как опсать код головного модуля 
- Все старые тесты удалены, оставлены только тесты по формуле ../rationaldev-ai-sdlc и ничего лишнего 
- Контракты между модулями либо явно описаны (если нужны), либо обоснованно отвергнуты со ссылкой на подход ../rationaldev-ai-sdlc 
- Любой человек, открыв plan-app-graph.xml + plan-data-flow.md + redesign.md, сразу понимает: что проверяем, куда кладём артефакты, как декомпозируем и сшиваем.
В результате должен получиться четкий стандарт проектирования воркфлоу
нарушать который нельзя 
нужно написать правила для работы агенту
нужно написать автопроверки нарушения структуры дизайна и правил
чтобы агент не писал лишнего и не туда

**ПРИМЕРЫ**  
/Users/mac/IdeaProjects/codemonstersdev/sandbox/runbox/eddi/.agent/design-graph.xml
/Users/mac/IdeaProjects/codemonstersdev/sandbox/runbox/eddi/.agent/data-flow.md

а вот что генерирует ../rationaldev-ai-sdlc
какой код и какие артефакты

/Users/mac/IdeaProjects/codemonstersdev/pinout/pinout-openapi
посмотри какие слайсы на го генерирует
как описаны доки и тикеты
/Users/mac/IdeaProjects/codemonstersdev/pinout/pinout-openapi/docs/design/slice-01-validate
как описан головной модуль

мы проект спроектировали и реализовали с этим харнесом ../rationaldev-ai-sdlc
за один проход и проект собрался и работает
нам нужно также реструктурировать текущий проект
убрать лишнее
чтобы все что работает до гейт1 работало как часы
и код можно было легко прочитать
так как харнес нужно изучать и улучшать


**ПОРЯДОК РАБОТЫ**
- покажи план работы
    - продемонстрируй что будет внутри каждго артефакта
    - покажи как будет выглядить код воркфлоу и код izi
- согласуй план со мной
- нареж план на консистентные тикеты в redesign-backlog.md
- вормат тикетов и зависимостей в тикетах возьми в /Users/mac/IdeaProjects/codemonstersdev/pinout/pinout-openapi/docs/design/slice-01-validate/tickets/ticket-03.md
    - секция **Acceptance:**
    должна содержать полный план проверки а не только факт зеленых тикетов
    нужно четко описать критерий готовности каждого тикета
    что в результате и как работает
    какой контракт
    что на вход за данные
    какие данные на выход
    какие контроли(гардрейлы) работают и где
    те на модуль должен быть 1 компонентный тест, где интеграция с ллм мокается ответом и шаг(модуль) отрабатывает полностью как запланировано
    так ты каждый шаг можешь проверить юнит тестами
    ответы можешь получать вызывая curl и подкладывая ответ в заглушку

- начни разработку по тикетам после согласования со мной всех тикетов
  - я буду читать каждый тикет