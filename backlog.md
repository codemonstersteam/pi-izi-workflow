# Бэклог

Пусто: полоса `task → brd → survey-plan → scope → graph → intake → weight → ripple` закрыта, все
наряды сданы живыми прогонами. След каждого среза живёт в его доке (`docs/<шаг>.md`) и в карточке
`docs/workflow.md` §3 — здесь он не дублируется.

Отложенные решения сюда не пишутся: у них свой файл и свои условия пробуждения — `docs/triggers.md`.

Следующий срез — шаг 9 `design`. Код `steps/design/` готов и покрыт тестами, вход (`.agent/frd.xml`
и `.agent/ripple.xml`) производят шаги 6 и 8, в программу шаг не включён. Наряды пишутся здесь, когда
срез начинается.
