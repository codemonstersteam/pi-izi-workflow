# Бэклог

Текущих работ нет: шаг 3 `survey-plan` сдан (S15), гардрейл упрощён и бюджеты вынесены в конфиг
(S16). Живое доказательство — прогон `754ab1bd-e123-4e03-ad1a-d2ad1392cf7b` в чужом проекте:
`task → brd → survey-plan`, `track:"ok"`, `.agent/survey-plan.json`, `files=21 cells=2`.

Что делать дальше — `docs/concept.md` (шаги 4-12). Заводится сюда работой, а не планом.

## Долги

- **Панель агента в herdr не проверена.** Прогон печатает `herdr: on pane=… fully-inspectable`,
  но открылся ли отдельный пейн под роль `gilb` — фактом не подтверждено. Если строка `on`, а пейна
  нет, копать в `@piewf/herdr::herdrTransport`, не в харнесе.
- **README, раздел про herdr, переписать под `herdr agent start <name> --kind pi --pane <id>`** —
  сейчас там ручной экспорт `HERDR_*`, а это не тот путь: pi запускает сам herdr.
