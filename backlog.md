# Backlog

Полоса доходит до **гейта 1**: требование → `task/<КЛЮЧ>/PLAN.md` → оператор утверждает → токен.
Единица плана — модуль (класс). Устройство шага 9 — `docs/design.md`; контракт каждой фазы —
`steps/design-data-flow.md`; место каждого шага в полосе — `docs/workflow.md` §3.

Правило прохождения: наряд закрывается заменой на сохранённых артефактах (0 токенов), затем живым
прогоном на `eddi` и `t2`. Красный прогон по СУТИ алгоритма — стоп и разбор, а не следующий круг.

`node --test` — **398 из 398**. Последний прогон: `eddi` `ffdc6844` — с шага 1 до токена гейта,
4 запуска роли, 424 870 токенов.

---

## Дальше — шаг 14: тикеты

Единственная работа в очереди. Сначала проектирование (data flow + форма тикета), потом разработка.

Разделы плана уже написаны так, чтобы нарезка не требовала ни модели, ни человека:

| поле тикета | откуда берётся |
|---|---|
| `outputs` | путь в заголовке раздела |
| `blocked_by` | строки «зовёт» этого раздела |
| `inputs` | «по образцу» плюс зовомые модули |
| DoD | строка «проверка» |
| порядок | тот, в котором разделы стоят в `PLAN.md` |

Приёмка: на `eddi` 13 тикетов, у каждого непустые `outputs` и DoD, `blocked_by` без циклов;
на `t2` — 2 тикета.

## Шаг 13 — ветка

Спроектирован (`docs/workflow.md` §3.13), не написан. Вход — `.agent/gate1.json`: ключ задачи и
sha256 плана. Отказы: `no-gate` · `plan-changed` · `dirty-worktree` · `branch-exists` · `no-trunk`.
Роли нет, 0 токенов. Порядок с шагом 14 решается при проектировании: тикету нужно имя ветки.

---

## Отложено

- **Шаг 11 `review` переезжает под `intake`** (`docs/workflow.md` §3.11, TOBE). Критик судит FRD
  против `TASK.md`/`brd.md`, а не план: план идёт строго по FRD, и фаза ⑥ сверяет его машиной.
  Улика: требование нерегрессии не доезжает до FRD ничем, и не ловит никто — `checkFrd` требований
  BRD физически не получает. Что нужно решить при разработке: как выразить «ничего не менять», у
  которого носителя в грамматике FRD нет вовсе.
- **Расписка шага 9 в `bandStart`** — шаг считается закрытым по существованию пары 9B, а поставка
  теперь `task/<КЛЮЧ>/PLAN.md`. Прогон, оборвавшийся между 9B и планом партии, в шаг 9 больше не
  войдёт. Всплывёт при первом таком обрыве; чинится одной строкой.
- **Узел изменения без единой цепочки** — 9B печатает их числом (`eddi`: 3 из 13), правила нет. У
  таких узлов пустой контракт в графе и вдвое короче `dod`. На план по модулям не влияет: он берёт
  состав из FRD.
- **D41** — замер: платит ли за себя черновик `$START_DRAFT` в наряде партии. Два прогона одной
  партии, с ним и без; ≈ $0.05.
- **Снять `STOP_AFTER_DESIGN`** — шаги 10 и 11. Условие: шаг 14 показал, что тикеты режутся; тогда
  источник `dod` меняется с юнитов графа на строку «проверка» плана.
- **D35** — поля сквозным путём шаг 6 → 9 → 10 → тикет. Понижено: поля пишет роль, читая образец.

---

## Формы

| форма | состояние |
|---|---|
| `eddi` | `runbox/eddi` — прошла до токена гейта: `task/DOS-535/PLAN.md` (13 разделов), три файла партий, `.agent/gate1.json`. В `TASK.md` объявлено `task: DOS-535` |
| `t2` | `runbox/quarkus-rest-json-app-v2-t2` — прошла целиком с нуля до `PLAN.md` на 2 модуля; гейт 1 на ней ещё не гонялся |
| улики | `runbox/prev-runs/eddi-88e96bb8/`, `runbox/prev-runs/t2-20260817/` |
