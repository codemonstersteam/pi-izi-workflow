# Бэклог — шаг 3 `survey-plan`

Концепт утверждён оператором: `docs/survey-plan.md`. Итерация = задача + её тест + отметка здесь.
Красный `node --test` — результат, о котором сообщают, а не повод править тест.

Шаг 3 — чистый скрипт: оператора, `checkpoint` и разбора чисел из ответов в нём нет.

## S15-1 · `steps/survey-plan/plan.mjs` — чистое ядро
- [ ] `newPlan({files, spine, subjects, cellFiles, cellBytes})`, единственный отказ `no-files`
- [ ] клетка закрывается по `CELL_FILES = 20` ИЛИ `CELL_BYTES = 200 КБ` — что раньше
- [ ] клетки покрывают ВСЕ файлы вне хребта, без пересечений и потерь; `c0` первая, `kind:"spine"`
- [ ] артефакт несёт `subjects` целиком, `gaps`, `bytes` — их требуют шаги 4 и 5
- [ ] `plan.test.mjs`: happy · шов по байтам (файлов мало, байтов много → две клетки) · `no-files`
- **шов:** убрать проверку по байтам → юнит 2 краснеет; вернуть

## S15-2 · `ext/index.mjs` — функция `survey`
- [ ] обход от `context.run.cwd`: `SKIP`, `SKIP_FILES`, точечные каталоги (кроме `KEEP_DOTS` =
      `.github` — там CI), `MAX_BYTES`
- [ ] `SPINE` — манифесты, конфиг-слой, CI, `README*`/`CONTRIBUTING*` (четвёртый вопрос графа —
      соглашение о ветках); ни одного совпадения → `c0` нет, не отказ
- [ ] `hitsFor` — подстрока без учёта регистра, по пути И тексту; якорь ПОМЕЧАЕТ, не фильтрует
- [ ] пишет `.agent/survey-plan.json` только на зелёном плане; `no-files` → `ok:false`
- [ ] зарегистрирована в `registerWorkflowExtension.functions`
- **готово, когда:** юнитов нет по контракту io-трубы; доказывает S15-5

## S15-3 · `workflows/izi.js` — третья фаза
- [ ] `brd()` на зелёном чеке `return`, а не `exit(ok)`
- [ ] `surveyPlan()`: `survey` → ok `exit`, иначе `blocked`; без `checkpoint`
- [ ] `log(files/bytes/cells)` перед выходом — стоимость роя видна в `journal.json` до шага 4

## S15-4 · Доки
- [x] `docs/concept.md` и `docs/workflow.md` починены по шести дефектам согласованности 3→GATE #1:
      часть роя = фрагмент грамматики графа, `<dep>` обязателен, четвёртый вопрос графа (ветки),
      `Delta.Unknown`, `path` = id узла, квитанция пропуска шага 9 убрана
- [x] карточка 3 и таблица полосы приведены к автомату (клетка 20 файлов ИЛИ 200 КБ, оператора нет)
- [ ] `docs/workflow.md` §2 «Программа» несёт третью фазу — ПОСЛЕ S15-3, по факту кода
- [ ] `README.md` — состав среза `steps/survey-plan/`
- [ ] `docs/survey-plan.md` помечен как реализованный

Шаги 4-12 в этом бэклоге не заводятся: починенный концепт — это план, а не работа текущего среза.

## S15-5 · Живой прогон в чужом проекте
- [ ] `/private/tmp/quarkus-rest-json-app-v2-t1-3`: `/izi` доходит до `.agent/survey-plan.json`
- [ ] `c0` = `pom.xml` + `README.md`, `c1` = остальные 16; `Legume*` есть, `mvnw`/`mvnw.cmd` нет
- [ ] `gaps` = `search`, `backward-compatibility`
- [ ] диагноз читается из `journal.json`, не из того, что напечатала модель
