# Backlog

Шаг 9 доводит требование до `task/<КЛЮЧ>/PLAN.md`, из которого режутся тикеты.
**Единица плана — модуль (класс).** Устройство шага — `docs/design.md`; контракт каждой фазы —
`steps/design-data-flow.md`.

Правило прохождения: наряд закрывается заменой на сохранённых артефактах (0 токенов), затем живым
прогоном на `eddi` и `t2`. Красный прогон по СУТИ алгоритма — стоп и разбор, а не следующий круг.

---

## Шаг 9 закрыт: девять фаз, один вызов роли на партию

| фаза | что делает | чем закрыта |
|---|---|---|
| ①② дерево и партии | состав из FRD, факты из ряби; модуль ровно в одной партии | **D44** · `eddi` 13 модулей (7 создаётся) → 3 партии, `t2` 2 → 1, `t3` 3 → 2 |
| ③ карточка партии | проекции: модули · use case дословно · соседи · образец · сьют · черновик | **D44** · 4,9-15,3 КБ против 126 КБ карты и FRD |
| ④ план партии | РОЛЬ: как меняется каждый модуль | **D45** · `eddi` `ca2b2588` — 3 партии зелёные с ПЕРВОЙ попытки; `t2` 1 партия |
| ⑤ гардрейл партии | 5 правил: состав · чужие · use case закрыты · «проверка» и «зовёт» · адрес вызова | **D45** |
| ⑥ полнота | каждый модуль решён, каждый use case и каждый его шаг закрыт | **D46** · `eddi` 12 use case, все ветки отказа |
| ⑦ порядок | топосорт по ОБЪЯВЛЕННЫМ «зовёт» + рёбра карты; круг — отказ с именами | **D46** · `eddi` 23 объявленных вызова на 13 модулей |
| ⑧ сборка | `PLAN.md`: шапка, таблица, разделы ДОСЛОВНО в порядке работ | **D46** · `eddi` 26,9 КБ / 13 разделов, `t2` 2 раздела |
| ⑨ тикеты | 1 раздел = 1 тикет | шаг 14, отдельный наряд |

Цена шага на `eddi`: **4 запуска роли** (`router` + 3 партии), 315 822 токена — против 910 834 у
снятой конструкции с роем по use case. Полоса `t2` целиком с нуля: 9 запусков, 170 981 токен.

`node --test` — **395 из 395**.

## Что снято и почему

**Рой по use case (D39) снят.** Прогон `ce2ede24` дал 3 плана и вскрыл дефект конструкции:
`AbstractBackupService.java` понадобился и UC10, и UC12, а выразить это планировщик не мог — два
планировщика написали бы два решения одного файла. Дубль лечится не гейтом и не маршрутизацией
нехваток, а единицей плана: модуль лежит ровно в одной партии, и решает его один вызов.

Сняты вместе с роем: `planner.md`, `order-plan.tpl`, `checkPlan`, `ucplan`, грамматика
`$START_NEEDS`, круг нехваток 9f, а также `splitOf`, `cardOf`, `coreCardOf`, `checkCore`.
Улики прогонов — `runbox/prev-runs/eddi-88e96bb8/`.

**Шапка-проза в `PLAN.md` заведена и откачена.** Единственный артефакт шага, который не проверялся
по существу; человек, прочитавший уверенное введение, перестаёт читать разделы. Вместо неё машинная
строка из тех же рёбер: «Первым идёт `Glossary.java`: его зовут 3 из 13 модулей».

---

## ✅ D44 · фазы ①②③ — дерево, партии, карточка

`partsOf({ frd, ripple })` и `partCardOf({ part, frd, map, graph })` в `steps/design/card.mjs`.

**Состав изменения даёт FRD, а не рябь.** Замер: на `eddi` узлов 13, рябь знает **6** — остальные
создаются, и рябь их знать не может. Рябь ОБОГАЩАЕТ найденное: `deps` и `level`.

**Партия — по общему use case, а не по пакету.** Замер на трёх формах: по пакету UC9 на `eddi`
падает на два вызова (`templating` и `llm`), и резолвер перестаёт видеть сервис, который зовёт; на
`t3` страница и эндпоинт, который она зовёт, разъезжаются. Имя пакета — соглашение, на которое
конвейер опираться не может.

**Инвариант:** Σ модулей партий = числу модулей, пересечений нет. Это и есть невозможность дубля —
свойство разбиения, а не проверка на гейте.

---

## ✅ D45 · фазы ④⑤ — план партии и его гардрейл

Роль `core-designer.md` + наряд `order-part.tpl`; `sectionsOf` (один разбор на двух читателей) и
`checkPart` (5 правил) в ядре; `parts({})` и `part({slug, path})` в расширении; `onePart` в рельсе.

**Строка `зовёт:` сделана обязательной ДО прогона.** Замер на живых контрактах `88e96bb8`: 8
разделов дали **2 ребра**. После правила — 23 объявленных вызова на 13 разделов, и очередь работ
наконец строится.

**Номер шага копируется из FRD знак в знак.** FRD нумерует ветки латиницей (`2a`), роль писала
кириллицей (`2а`) — обе формы в одном файле прогона `e79a460e`. Нормализация в сборке была бы
сокрытием дефекта: покрыто на бумаге, набрано другой буквой на диске. Единообразие держит правило 3
гардрейла, там же, где строка пишется и где есть кому чинить. Ловушка сидела и в самом тексте роли —
схема вывода и пример показывали кириллическую `2а`, то есть учили тому, что запрещают.

---

## ✅ D46 · фазы ⑥⑦⑧ — полнота, порядок, сборка

`steps/design/plandoc.mjs`: `coverageOf`, `orderOf`, `planDoc`; хост-функция `planbook({})`.

**Отказ сборки чинится ролью, а не человеком.** Гардрейл партии не видит целого — шаг, закрытый
разделом другой партии, не его дело. Поэтому проверка стоит на сборке, но `planbook` возвращает
ВИНОВНЫЕ партии, и рельса перезывает именно их с блокером в `FEEDBACK`, бюджет `LOOPS`. Замена на
планах прогона `e79a460e` нашла 6 дыр полноты и круг `RestImportService ↔ UpgradeExecutor` — ровно
то, ради чего рельса и заведена.

**Любой отказ снимает `PLAN.md`**: вчерашний план не переживает сегодняшнюю дыру.

---

## ⏳ Ключ задачи и `task/<КЛЮЧ>/` — КОД ГОТОВ, живого прогона не было

Поставка шага живёт не в `.agent/` (оно чистится `newRun`, стирается гейтами и лежит в `.gitignore`),
а в каталоге задачи — там же, где ветка и тикеты:

```
task/<КЛЮЧ>/PLAN.md
task/<КЛЮЧ>/design/<партия>.md
```

Каталог именован ключом не для красоты: две задачи в одном репозитории делят slug партий
(`configs-glossary` в обеих), и плоский `docs/design/` дал бы второй задаче молча затереть решения
первой.

**Ключ спрашивается на шаге 1 — или не спрашивается вовсе.** Источников два, порядок один: ответ
оператора старше (он мог поправить задачу), затем объявление в `TASK.md`:

```
task: DOS-535
```

Именно ОБЪЯВЛЕНИЕ, а не поиск по тексту: задача, упоминающая соседний тикет («как в DOS-100»),
назвала бы его именем и ветку, и каталог плана — молча. Вопрос задаётся тем же дословным текстом,
которым его задаёт шаг 10, поэтому ответ служит обоим и оператора не спрашивают дважды.

**Приёмка.** Живой `eddi`: `task: <ключ>` в `TASK.md` → вопроса нет, поставка легла в `task/<КЛЮЧ>/`.
Живой `t2` без строки в задаче → один вопрос на шаге 1, дальше то же.

---

## Дальше — шаг 14: тикеты

Единственный наряд в работе. Разделы плана уже написаны так, чтобы нарезка не требовала ни модели,
ни человека:

| поле тикета | откуда берётся |
|---|---|
| `outputs` | путь в заголовке раздела |
| `blocked_by` | строки «зовёт» этого раздела |
| `inputs` | «по образцу» плюс зовомые модули |
| DoD | строка «проверка» |
| порядок | тот, в котором разделы стоят в `PLAN.md` |

Приёмка: на `eddi` 13 тикетов, у каждого непустые `outputs` и DoD, `blocked_by` без циклов;
на `t2` — 2 тикета.

## Отложено

- **Расписка шага 9 в `bandStart`** — шаг считается закрытым по существованию пары 9B, а поставка
  теперь `task/<КЛЮЧ>/PLAN.md`. Прогон, оборвавшийся между 9B и планом партии, в шаг 9 больше не
  войдёт. Всплывёт при первом таком обрыве; чинится одной строкой.
- **Узел изменения без единой цепочки** — 9B печатает их числом (`eddi`: 3 из 13), правила нет. У
  таких узлов пустой контракт в графе и вдвое короче `dod`. На план по модулям не влияет: он берёт
  состав из FRD.
- **D41** — замер: платит ли за себя черновик `$START_DRAFT` в наряде партии. Два прогона одной
  партии, с ним и без; ≈ $0.05. К движению полосы не относится.
- **Снять `STOP_AFTER_DESIGN`** — шаги 10 и 11. Условие: шаг 14 показал, что тикеты режутся; тогда
  источник `dod` меняется с юнитов графа на строку «проверка» плана.
- **D35** — поля сквозным путём шаг 6 → 9 → 10 → тикет. Понижено: поля пишет роль, читая образец.
- **И1** — критик судит FRD, а не план. Обсуждать после гейта 1.

## Формы

| форма | состояние |
|---|---|
| `eddi` | `runbox/eddi` — шаги 1-8 зелены, шаг 9 переигрывается; `PLAN.md` и `docs/design/*` от прогона `ca2b2588`; улики в `runbox/prev-runs/eddi-88e96bb8/` |
| `t2` | `runbox/quarkus-rest-json-app-v2-t2` — прошла целиком с нуля: 9 запусков ролей, 3 вопроса оператору (BRD ×2, гейт сьютов), `PLAN.md` на 2 модуля |
