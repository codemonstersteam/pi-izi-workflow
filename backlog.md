# Backlog — два судьи полосы (`IZI-JUDGES`)

Источник: `tasks/stack-help.md` (концепция) и `tasks/step-14-ticket-problem-1-2-collapse.md` (улики).
Цель набора — довести полосу до состояния, в котором ПОЛНЫЙ прогон на форме `eddi` С НУЛЯ имеет
смысл: требование судится на полноту до плана, план доказывается нарезкой до гейта.

## Состояние на 19.08.2026

**Закрыты: J1-J5, J6 (шесть нарядов), J7-J10.** `node --test` — 472 из 472 (число тестов уменьшилось: срез `steps/review/` переписан, и 29 тестов плановой эпохи заменены 16 тестами нового предмета).
**Закрыт и J6** — все шесть нарядов J6a-J6f (план на них — `tasks/j6-critic-frd.md`). Критик переехал:
предмет — FRD против `TASK.md` и `brd.md`, место — сразу за зелёным `checkFrd`, вызов ОДИН на
артефакт, словарь — четыре кода, чек-листа два (долг по требованиям BRD и обратный ход: элементы
артефакта, которых не просило ни одно требование). Срез `steps/review/` переписан, `review.test.mjs`
написан заново под новый предмет, `docs/review.md` — тоже.
**Остался J11** — полный живой прогон на eddi с нуля.
**Первым из хвоста идёт J13** (ответ оператора мимо чат-модели): он не зависит ни от чего и уже
дважды останавливал живой прогон.

Проверено ступенями, как договорились:
1. `node --test` после каждого наряда — 484 зелёных;
2. проигрыш по СОХРАНЁННЫМ артефактам четырёх форм: `F8` — один блокер на eddi (`glossaries in
   AgentConfiguration`) и ноль на t1-3/t2/t3; `F10` — три на eddi (UC6/UC7/UC8) и один на t3 (UC2
   входит через список, а канал назван карточкой — настоящая находка); правило 4a шага 9 — один
   блокер на разделе `RestImportService`; сухой прогон — красный на том же разделе через правило 15;
3. перескан карты после J1 на копии формы: объявлений `6070 → 6890`, методов `4782 → 5586`,
   `IConversationMemory` `1 → 78`, `IRestImportService` `1 → 7`;
4. нарезка по исправленному плану: `23 наряда` (22 + приёмка), волны `7 · 2 · 3 · 8 · 2 · 1`;
   в наряде 19 появился блок «How it refuses» с кодами и статусами, в границе UC6 — строка
   `before you call`, из неё ушли `key (Term)`/`value (Term)`.

## Тест SLM: справится ли qwen 27B (19.08.2026, напрямую через OpenRouter)

Прокси `localhost:4000` не поднят — оба прогона сделаны запросом к `openrouter.ai` тем же ключом;
модель `qwen/qwen3.6-27b`, вход 40.5К токенов, выход 7.9К, цена $0.05, время ~3 минуты.

**Тест A — «полон ли ПЛАН, нарезается ли по нему исполнимый набор» (FRD + PLAN + appgraph, чёткий
чек-лист из шести пунктов).** Вердикт `not cuttable`, четыре блокера, три из которых совпали с моим
разбором дословно: нет модуля под `AgentConfiguration` (наш F8), UC5 не получает границы, коды
отказа и статусы в плане отсутствуют, предусловия тестов отсутствуют. **Четвёртый — находка,
которой у нас не было:** строки `closes:` разных разделов претендуют на ОДИН шаг (`UC1/1`, `UC1/4`,
`UC3/2` названы двумя-тремя разделами). В нарезке это разрешает граф (`ownerOf` выбирает одного), но
в ПЛАНЕ владение неоднозначно, и человек на гейте читает не то, что исполнится. Кандидат в правило
шага 9.

**Тест B — «целостен ли БЭКЛОГ, допиши недостающие наряды» (FRD + PLAN + все 22 наряда).** Здесь
модель ошиблась в главном: объявила все восемь use case закрытыми и «missing steps: none» — то есть
дыру с `AgentConfiguration`, которую сама же нашла в тесте A, не увидела, когда смотрела на наряды.
Новых нарядов не написала ни одного. Зато её разбор ИСПОЛНИМОСТИ трёх нарядов точен и полезен:
- 19: нет отображения исключение → HTTP-статус, формата заголовка `Location`, правила отказа лишним
  полям, маршрутизации флага `permanent`;
- 20: нечем достать `agentId`, не назван `IAgentConfigStore`, не назван способ положить карту в
  данные шаблона, не определено условие `GLOSSARY_UNBOUND`;
- 22: не назван `IAgentConfigStore`, нет способа найти глоссарий по resource URI (в `IGlossaryStore`
  такого метода нет вовсе), не определена семантика `isMerge`.

**Вывод, который меняет план работ.** SLM силён как КРИТИК ПО СПИСКУ и слаб как судья полноты «в
целом»: спрашивать надо не «полон ли набор», а «проверь вот эти названные условия». Значит роль
критика (J6) обязана получать чек-лист строками, как её нынешний `{OWED}`, — это уже так и
устроено. А список из теста B — это следующий наряд после J9: наряду не хватает не текста, а
СОСЕДЕЙ (`IAgentConfigStore`), метода, которого ни у кого нет (`findByResourceURI`), и семантики
собственных параметров.

Живой прогон (J11) — по ранбуку `~/IdeaProjects/codemonstersdev/sandbox/pi-runbox.md`, на СВЕЖЕЙ
копии формы под `sandbox/runbox/`, с пустым `.agent/`. Перед запуском обязателен рестарт pi:
`ext/index.mjs` менялся.

---
id: J1
key: IZI-JUDGES
kind: module
status: done
wave: 1
blocked_by: []
inputs:  [steps/scope/source.mjs, steps/scope/source.test.mjs, steps/scope/computed.mjs]
outputs: [steps/scope/source.mjs, steps/scope/source.test.mjs]
verify: node --test
---
## Goal
Видимость члена — грамматика языка, а не наличие слова. `javaDecls` считает метод публичным только
при литеральном `public`, а в Java-интерфейсе его не пишут: `IConversationMemory` (20 методов) несёт
в карте ОДНО объявление, и наряд 20 получает под «What you call» строку без единого метода. Тем же
пусты `IResourceStorageFactory`, `IDocumentBuilder`, `IRestExportService`, `IRestImportService` — а
репозиторий интерфейсный насквозь. Это кормит шаги 5, 6, 9 и 14 одновременно, поэтому идёт первым.
## What you must prove
- член, объявленный в теле `interface`, получает `visibility="public"` без ключевого слова; так же
  `default`-метод и аксессор `record`
- приватный помощник в классе остаётся `internal` — фильтр `computed.mjs:187` не трогается
- шов доказан обратно: откат `javaDecls` схлопывает фикстуру-интерфейс с трёх объявлений до одного
## Done when
- `node --test` зелёный целиком
- переcканированный `eddi`: `IConversationMemory.java` несёт `getAgentId()`

---
id: J2
key: IZI-JUDGES
kind: module
status: done
wave: 1
blocked_by: []
inputs:  [ext/index.mjs, ext/index.test.mjs, steps/intake/frd.mjs]
outputs: [ext/index.mjs, ext/index.test.mjs, steps/intake/frd.mjs]
verify: node --test
---
## Goal
Шаг 6 судит FRD против БЕДНЕЙШЕЙ из двух карт. `checkFrd` читает только `.agent/appgraph.xml` (карта
роя, 88 узлов на eddi), а `.agent/graph-computed.xml` (6070 объявлений, 217 строк `<api>`) лежит
рядом и не читается никем ниже шага 5. Улика: `grep -c AgentConfiguration appgraph.xml` = 0, при том
что в вычисленном графе он резолвится в путь. Без этой проводки правила J3 и J5 невозможны.
## What you must prove
- `checkFrd.run` читает `COMPUTED_PATH` (константа объявлена, `:644`; `parseComputed` импортирован,
  `:72`) и передаёт в `newFrd` таблицы `types` (имя типа → путь) и `members` (путь → его объявления)
- параметры необязательные и пустые по умолчанию: без таблиц правила МОЛЧАТ, как молчат F5 без
  `sources` и F9 без `rewind`
- constraint 5 — потребители названы: `izi.js:819`, `:825` (staging) и `:1338` (resume)
## Done when
- `node --test` зелёный; фикстура без вычисленного графа даёт сегодняшний вердикт байт в байт

---
id: J3
key: IZI-JUDGES
kind: module
status: done
wave: 2
blocked_by: [J2]
inputs:  [steps/intake/frd.mjs, steps/intake/frd.test.mjs, docs/intake.md]
outputs: [steps/intake/frd.mjs, steps/intake/frd.test.mjs, docs/intake.md]
verify: node --test
---
## Goal
Правило F8: объявленное поле, которое никто не напишет. Формулировка ДОКАЗАНА эмуляцией на четырёх
сохранённых формах — красный ровно на известном дефекте eddi, зелёный на трёх остальных.
Текст правила: `<field name="F" in="E">` КРАСНЫЙ, если `E` резолвится в существующий путь, поля `F`
у этого пути НЕТ, и путь не заявлен ни `<delta node>`, ни `<touched path>` этого же FRD.
## What you must prove
- три условия по отдельности: сущность, которой нет нигде (`Glossary`, `Term`), — зелено; поле,
  которое у сущности УЖЕ есть (`Fruit.name` на форме `t2`), — зелено; дельта на СОСЕДНИЙ модуль не
  гасит блокер
- F8 судит ТОЛЬКО staging (`judgingStaged`) — иначе вчерашний зелёный прогон перестанет резюмиться
- блокер называет поле, сущность и путь модуля
## Done when
- `node --test` зелёный; `docs/intake.md` §4 несёт F8
- проигрыш по сохранённому FRD eddi: один блокер, `glossaries` в `AgentConfiguration`

---
id: J4
key: IZI-JUDGES
kind: module
status: done
wave: 2
blocked_by: []
inputs:  [steps/intake/frd.mjs, steps/intake/frd.test.mjs, steps/intake/intake.md, docs/intake.md]
outputs: [steps/intake/frd.mjs, steps/intake/frd.test.mjs, steps/intake/intake.md, docs/intake.md]
verify: node --test
---
## Goal
Роль ПЕРЕСМАТРИВАЕТ свой FRD на полноту до того, как его отдать, — и результат пересмотра выходит
АРТЕФАКТОМ, а не обещанием. В FRD появляется блок покрытия: по строке на каждое требование BRD —
`<carried req="R4" by="UC1/step2"/>` — и роль обязана его заполнить, пройдя требования одно за
другим. Самосертификации здесь нет (constraint 1): роль предъявляет УЛИКУ, судит её скрипт.
Улика необходимости: требование нерегрессии «существующий вызов остаётся без изменений» не доехало
до FRD ничем и не поймано никем — два прогона формы `t2` подряд, один исход.
## What you must prove
- грамматика принимает `<carried req by>`; round-trip `parse(write(x)) === x` на самом трудном
  легальном значении (standards/code.md)
- `checkFrd` краснеет, если требование BRD не имеет строки `carried`, или если её `by` указывает на
  элемент, которого в этом FRD нет
- шаблон роли (`steps/intake/intake.md`) велит пройти требования BRD ПО ОДНОМУ и назвать носителя
  каждого; пример вместо объяснения
## Done when
- `node --test` зелёный
- фикстура: FRD, потерявший одно требование BRD, отвергнут с его номером; полный — зелен

---
id: J5
key: IZI-JUDGES
kind: module
status: done
wave: 2
blocked_by: [J2]
inputs:  [steps/intake/frd.mjs, steps/intake/frd.test.mjs, steps/tickets/tickets.mjs, steps/intake/intake.md]
outputs: [steps/intake/frd.mjs, steps/intake/frd.test.mjs, steps/tickets/tickets.mjs, steps/intake/intake.md]
verify: node --test
---
## Goal
Канал входа живёт на use case, и его значение ВЫЧИСЛЕНО, а не угадано. Сегодня `via` есть только у
актёра, и все три backup/sync use case eddi получили CRUD-эндпоинт `/glossarystore/glossaries` —
канал, которого в репозитории нет вовсе (0 строк `<api>` с этим путём). Настоящие каналы лежат в
вычисленном графе: `GET /backup/export/{agentFilename}`, `POST /backup/import/sync`.
## What you must prove
- `<usecase via>` необязателен и перекрывает актёрский; round-trip на обоих уровнях
- роли подставляется СПИСОК каналов из `<api>` строк карты — она выбирает, а не сочиняет
- `checkFrd` краснеет, если шаг use case называет путь, отличный от его канала
- `viaOf` в нарезке предпочитает канал use case, падая на актёрский; потребители `viaOf` названы
## Done when
- `node --test` зелёный
- на сохранённом FRD eddi правило называет UC6/UC7/UC8

---
id: J6a
key: IZI-JUDGES
kind: module
status: done
wave: 3
blocked_by: [J4]
inputs:  [steps/review/review.mjs, steps/review/review.test.mjs]
outputs: [steps/review/review.mjs, steps/review/review.test.mjs]
verify: node --test
---
## Goal
Чек-лист критика считается из ТРЕБОВАНИЯ, а не из плана, и списков становится два (третий уже есть).
`owedItems({ requirements, task })` — строка на требование BRD; `unbackedItems({ frd })` — обратный
ход: элементы FRD, которых не называет ни одна строка `<carried by>`; `autoFindings` (открытые
вопросы) остаётся как есть — он уже машинный. `askedNodes`/`createdNodes` из среза уходят вместе с
планом.
## What you must prove
- три требования BRD → три строки долга; `plan` не участвует ни в одной сигнатуре
- use case, не названный ни одной строкой `carried`, → ровно одна строка «не заявлен»; названный
  через `carried` соседнего требования — не попадает
- обе функции тотальны: пустой вход даёт пустой список, не бросает
## Done when
- `node --test` зелёный

---
id: J6b
key: IZI-JUDGES
kind: module
status: done
wave: 3
blocked_by: [J6a]
inputs:  [steps/review/review.mjs, steps/review/review.test.mjs]
outputs: [steps/review/review.mjs, steps/review/review.test.mjs]
verify: node --test
---
## Goal
Адресное пространство вердикта — FRD, а не план: `<covers item node/>` резолвится против `frdIds`,
R5 (каждая строка ровно раз) и R7 (закрыта тем, что способно ответить) переписываются на оба списка,
R3/R4 судят адрес по FRD. R6 (узел без своей команды) уходит из среза вместе с `askedNodes`.
## What you must prove
- `covers` на несуществующий элемент отвергнут; на `UC1/2`, `S1`, узел дельты, `nfr:<subject>` принят
- незакрытая строка ЛЮБОГО из двух списков — блокер, и он называет список и строку
- `newReview` больше не требует `plan-index.json` и не отказывает `no-plan`
## Done when
- `node --test` зелёный

---
id: J6c
key: IZI-JUDGES
kind: module
status: done
wave: 3
blocked_by: [J6b]
inputs:  [steps/review/review.mjs, steps/review/review.test.mjs, steps/review/critic.md]
outputs: [steps/review/review.mjs, steps/review/review.test.mjs, steps/review/critic.md]
verify: node --test
---
## Goal
Словарь кодов: `requirement-not-carried · invented-value · goal-not-delivered · open-question`.
`CODE_CULPRIT` у всех — `frd.xml`, `CODE_OWNER` — 6, `CODE_EVIDENCE`: для `invented-value` — само
значение (оно обязано быть В артефакте), для `requirement-not-carried` — номер требования BRD.
## What you must prove
- код вне словаря отвергается до чтения блокера (R2)
- `evidence`, которого нет в артефакте, — блокер формы
- роль и `CODES` совпадают: существующий шов на расхождение остаётся красным при расхождении
## Done when
- `node --test` зелёный

---
id: J6d
key: IZI-JUDGES
kind: module
status: done
wave: 4
blocked_by: [J6c]
inputs:  [steps/review/order.tpl, steps/review/critic.md, ext/index.mjs, ext/index.test.mjs]
outputs: [steps/review/order.tpl, steps/review/critic.md, ext/index.mjs, ext/index.test.mjs]
verify: node --test
---
## Goal
Наряд роли: документы — `TASK.md`, `brd.md`, staging-FRD; плейсхолдеры `{OWED}`, `{UNBACKED}`,
`{CODES}` подставляются МАШИНОЙ (`ext/index.mjs::reviewForm`); план из наряда уходит. Текст роли
`critic.md` меняет предмет: требование — это ОБЕЩАНИЕ, судится, всё ли обещанное записано и не
появилось ли записанного сверх обещанного.
## What you must prove
- в наряде нет слова «план»; оба списка подставлены и не пусты на фикстуре живого FRD
- `reviewForm` отдаёт `owed` и `unbacked`, и его выход проходит схему хоста тем же валидатором
- наряд помещается в окно модели (на артефактах eddi — 9.5К токенов)
## Done when
- `node --test` зелёный

---
id: J6e
key: IZI-JUDGES
kind: module
status: done
wave: 4
blocked_by: [J6d]
inputs:  [workflows/izi.js, ext/index.mjs, ext/index.test.mjs]
outputs: [workflows/izi.js, ext/index.mjs, ext/index.test.mjs]
verify: node --test
---
## Goal
Место в полосе и ОДИН вызов: критик зовётся на ПЕРВОМ зелёном `checkFrd`, до `weigh()`. `Reject`
кладёт блокеры в `.agent/answers.md` и даёт `intake` ровно один круг; дальше судит только машина.
Флаг «высказался» — `.agent/review.xml`, переживает resume, стирается только при `rework` с гейта 1.
## What you must prove
- при любом числе кругов `checkFrd` роль критика вызвана РОВНО один раз (счётчик в тесте полосы)
- после `Reject` и одного круга intake второго вызова нет
- resume с зелёным `.agent/review.xml` критика не переигрывает; `rework` с гейта 1 стирает вердикт
## Done when
- `node --test` зелёный

---
id: J6f
key: IZI-JUDGES
kind: module
status: done
wave: 5
blocked_by: [J6e]
inputs:  [docs/workflow.md, docs/review.md, docs/intake.md]
outputs: [docs/workflow.md, docs/review.md, docs/intake.md]
verify: node --test
---
## Goal
Документы описывают то же, что код: §3.11 из TOBE в AS-IS (предмет, место, один вызов, словарь,
рельс), `docs/review.md` целиком на новый предмет, `docs/intake.md` §5 — блокер критика приезжает в
`answers.md`.
## Done when
- ни один документ не описывает критика как судью плана
- порядок полосы в §1 — `6 → 11 → 7 → 8 → 9 → 10 → 10б → 12 → 13 → 14`
- `node --test` зелёный

---
id: J7
key: IZI-JUDGES
kind: module
status: done
wave: 1
blocked_by: []
inputs:  [ext/index.mjs, ext/index.test.mjs, workflows/izi.js]
outputs: [ext/index.mjs, ext/index.test.mjs, workflows/izi.js]
verify: node --test
---
## Goal
Сухой прогон нарезки становится ШАГОМ полосы. Ядро уже написано и проверено на живых артефактах;
не хватает шва и трёх строк проводки. После шага 10 и до гейта 1 полоса зовёт `tickets({dry:true})`:
зелёный печатает счёт нарядов и волн, красный возвращает работу дизайнеру шага 9 его же каналом
`FEEDBACK` (`izi.js:1152-1164`), а не оператору.
## What you must prove
- шов: РЕАЛЬНЫЙ возврат `tickets.run({dry:true})` на фикстуре против РЕАЛЬНОЙ схемы тем же
  валидатором, каким судит хост (четвёртый случай `Invalid output` куплен именно этим)
- сухой прогон не требует `gate1.json` и не пишет ни одного файла
- красный сухой прогон возвращает на шаг 9 с текстом блокеров; бюджет кругов — общий с шагом 9
- шаг 14 после гейта выдаёт ТОТ ЖЕ счёт, что напечатал сухой прогон
## Done when
- `node --test` зелёный
- на копии eddi в состоянии шага 10: `22 наряда, границ 7, модулей 15, волны 7·2·3·8·2`, каталог
  `tickets/` не создан
- сухой режим ОПИСАН там, где живёт шаг: карточка шага 14 в `docs/workflow.md` и
  `steps/tickets/data-flow.md` — сегодня код лежит, а документа о нём нет

---
id: J8
key: IZI-JUDGES
kind: module
status: done
wave: 2
blocked_by: [J7]
inputs:  [steps/design/card.mjs, steps/design/card.test.mjs, steps/design/order-part.tpl, steps/tickets/tickets.mjs, steps/tickets/tickets.test.mjs]
outputs: [steps/design/card.mjs, steps/design/card.test.mjs, steps/design/order-part.tpl, steps/tickets/tickets.mjs, steps/tickets/tickets.test.mjs]
verify: node --test
---
## Goal
Имя теста принадлежит сьюту репозитория. План eddi несёт `verify: ./mvnw verify
-Dit.test=RestImportServiceIT · RestImportServiceIT` на МОДУЛЬНОМ разделе: класс нарушает шаблон
юнитового сьюта `*Test.java`, и наряд 22 получил ворота, которые не могут позеленеть, при образце
`RestImportServiceTest` в том же тексте. Сухой прогон сегодня на этом плане ЗЕЛЁН — правила нет.
Чинится в двух местах: шаг 9 не выпускает такую строку, нарезка не доверяет имени.
## What you must prove
- шаблону `core-designer` подставлены формы сьютов из карты (команды и `match` каждого)
- `checkPart` отвергает `verify`, чья команда не принадлежит карте или чьё имя ломает `match` сьюта
- `checkTickets` — правило 15: `testClass` модульного наряда обязан удовлетворять `unit.match`;
  нелегальное имя не порождает `-Dtest=`, а падает на отметку `wholeSuite`
- имя собственного файла теста берётся из того же шаблона: `outputs`, образец и ворота не расходятся
## Done when
- `node --test` зелёный
- сухой прогон по плану eddi КРАСНЕЕТ на разделе `RestImportService` и называет шаблон

---
id: J9
key: IZI-JUDGES
kind: module
status: done
wave: 3
blocked_by: [J7, J8]
inputs:  [steps/tickets/tickets.mjs, steps/tickets/tickets.test.mjs, steps/tickets/data-flow.md]
outputs: [steps/tickets/tickets.mjs, steps/tickets/tickets.test.mjs, steps/tickets/data-flow.md]
verify: node --test
---
## Goal
Наряд несёт то, что FRD уже знает, а гардрейл требует этого от ПЛАНА. Сегодня в нарядах eddi:
`<pre>` не доехало ни в один (0 совпадений); коды ошибок и HTTP-статусы живут ТОЛЬКО в граничных
нарядах, и модуль 19, который эти проверки пишет, не знает ни кода, ни статуса; UC5 не получил
границы и остался без внешней проверки; набор не имеет наряда, который прогонит внешний сьют после
последней волны. Через сухой прогон каждое из этих правил становится требованием к плану.
## What you must prove
- граничный наряд несёт `<pre>` своего use case; use case без границы отдаёт свой `<post>` владельцу
  последнего шага
- наряд, владеющий шагом с ошибкой, несёт КОД и СТАТУС из `<failure>`/`<ext>`
- краснеет: use case, чей актёр входит путём, но границы нет; набор без закрывающего прогона внешнего
  сьюта; шаг с ошибкой, чей наряд не несёт кода
- на сохранённых артефактах eddi новые правила называют РОВНО известные дефекты (UC5,
  `AgentConfiguration`, коды в границах) и ни одного лишнего
## Done when
- `node --test` зелёный; `data-flow.md` несёт новые правила один к одному с кодом

---
id: J10
key: IZI-JUDGES
kind: module
status: done
wave: 4
blocked_by: [J1, J3, J4, J5, J6, J9]
inputs:  [docs/workflow.md, docs/review.md, docs/intake.md, CLAUDE.md]
outputs: [docs/workflow.md, docs/review.md, docs/intake.md]
verify: node --test
---
## Goal
Полоса в документах совпадает с полосой в коде. §3.11 переписывается из TOBE в AS-IS (новый предмет
и место критика), шаг 10б описывается как шаг, `docs/intake.md` несёт F8 и блок покрытия.
## Done when
- ни один документ не описывает критика как судью плана
- `docs/workflow.md` §1 несёт порядок `6 → 11 → 7 → 8 → 9 → 10 → 10б → 12 → 13 → 14`

---
id: J11
key: IZI-JUDGES
kind: module
wave: 5
blocked_by: [J10]
inputs:  [~/IdeaProjects/codemonstersdev/sandbox/pi-runbox.md, sandbox/runbox/eddi (свежая копия)]
outputs: [.agent/review.xml, task/DOS-535/tickets/]
verify: полный живой прогон с шага 1
---
## Goal
ПОЛНЫЙ прогон на форме `eddi` С НУЛЯ — единственная проверка, которой этот репозиторий не может
сделать сам (constraint 7). Свежая копия формы под `sandbox/runbox/`, `.agent/` пуст, `TASK.md` тот
же, что дал прогон `DOS-535`; запуск по ранбуку (перезапуск pi; ни одного `*.md` под
`~/.pi/agent/pi-extensible-workflows/roles/`; `pi` стартует ИЗ каталога прогона).
## What you must prove
- шаг 6 закрывается с блоком покрытия и без блокеров F8
- шаг 11 (критик) выносит вердикт, и он лежит в `.agent/review.xml`
- сухой прогон печатает счёт нарядов ДО гейта 1; оператор утверждает план, про который доказано,
  что он нарезается
- шаг 14 выдаёт тот же счёт, что напечатал сухой прогон
- в нарядах: реальные backup-каналы у UC6/UC7, `RestImportServiceTest` у 22, члены интерфейсов под
  «What you call», раздел под `AgentConfiguration` существует, UC5 имеет внешнюю проверку
## Done when
- `node --test` зелёный ЦЕЛИКОМ до запуска
- `run.yaml` отмечает шаги 6, 11, 10, 12, 13, 14 сделанными
- рабочая копия `sandbox/runbox/eddi` не тронута

---
id: J12
key: IZI-JUDGES
kind: module
status: done
wave: 6
blocked_by: []
inputs:  [ext/index.mjs, workflows/izi.js]
outputs: [core/orderline.mjs, core/orderline.test.mjs, ext/index.mjs, workflows/izi.js]
verify: node --test
---
## Goal
Ход курьера не должен стоить 30 тысяч токенов. Замер сессии `01a017dc`: 22 вызова чат-модели за
четыре часа, вход 22-31 тыс. токенов на ход, `cached` — 0 или 128. Один ход дал `in=187
cached=31488` — вот как выглядит попадание кэша.

ПРИЧИНА НЕ УСТАНОВЛЕНА, и наряд начинается с замера, а не с правки. Что известно точно:

- строку лога доставляет ХОСТ: `pi.appendEntry(WORKFLOW_LOG_ENTRY, …)` (`host.ts:216`) — это запись
  сессии со своим рендерером, и она не наша;
- у хоста есть ВТОРАЯ ветка: у прогона в ФОРЕГРАУНДЕ и присоединённого лог уходит в обновление
  тул-вызова (`update(workflowToolUpdate(...))`), а записи сессии не появляется вовсе. Мы гоняем в
  фоне намеренно (CLAUDE.md, `foreground: false`): пауза приходит сообщением, редактор остаётся
  свободен — то есть ветка есть, но она стоит другого;
- гипотеза «лог рвёт кэш» ЗАМЕРОМ НЕ ПОДТВЕРЖДЕНА: промахи шли и на ходах в 16-31 секунду друг от
  друга, а единственное попадание случилось через 32 секунды после предыдущего хода. TTL провайдера
  объясняет это не лучше.
## What you must prove
- сперва ЗАМЕР: два прогона одной формы, один в фоне, один в форграунде, и таблица `in/cached/out` по
  ходам. Без него любая правка — угадывание
- если виноват лог: сокращается его ОБЪЁМ (строка «наряд NNNN симв · шаблон … · MAP …» печатается на
  каждом круге и несёт внутренности сборки — её место в журнале прогона, не в чате)
- если виноват режим доставки: решение принимается ЯВНО, вместе с ценой — форграунд отнимает у
  оператора редактор, и это плата за кэш
- что бы ни выяснилось, из пейна не должно пропасть ничего, что оператор читает сегодня
## Done when
- `node --test` зелёный; замер приложен к наряду и назван виновник

## Сделано (замер снят оператором)
`core/orderline.mjs::newOrderLine({step,chars,cap,round,over,tplChars,addends}) → {line,why}` —
чистая, юнит `core/orderline.test.mjs`. Полоса не форматирует: решение едет хост-функцией
`orderLine` (расширение 1.23.0), у полосы остались МЕРА и КРУГ.

Разбор наряда (184 симв на КАЖДОМ круге у шести шагов) сжат до 43: `intake: наряд 141879 симв из
800000, круг 3`. Слагаемые остались там, где они и есть ответ: при `order.over`, на ПЕРВОМ наряде
своего рода и всегда в `why` отказа. Круги считает РОД наряда, а не имя шага, — `scope/<cell>` и
`design/part/<slug>` печатают состав один раз, а не по разу на клетку и партию.

Сняты два пересказа: `M симв` в строке таблицы типов (то же число называет слагаемое `TYPES`) и
строка `design/parts` целиком (её суммы повторяли уже напечатанное; уникальный счёт объявленных
вызовов переехал в `design/plan`).

Швы доказаны обратно: возврат разбора на каждый круг — 3 красных; счёт кругов по имени шага вместо
рода — 1 красный. `node --test` 506/506.

---
id: J13
key: IZI-JUDGES
kind: module
status: todo
wave: 1
blocked_by: []
inputs:  [workflows/izi.js, ext/index.mjs, ext/index.test.mjs, bin/answer.mjs]
outputs: [workflows/izi.js, ext/index.mjs, ext/index.test.mjs, bin/answer.mjs]
verify: node --test
---
## Goal
ПРИОРИТЕТ. Пауза оператора перестаёт зависеть от чат-модели: полоса не пользуется чекпоинтом хоста, а
ЖДЁТ ОТВЕТА НА ДИСКЕ.

Сегодня `askOperator` (workflows/izi.js) уже считает ответ ФАКТОМ НА ДИСКЕ — `unanswered()` читает
`.agent/answers.md` и сверяет по тексту вопроса, — а `checkpoint()` служит ему только БАРЬЕРОМ:
хостовый чекпоинт возвращает `approved|rejected` и текста не несёт вовсе. Меняется, значит, ровно
барьер: вместо `checkpoint()` полоса зовёт новую host-функцию, которая ждёт, пока ответы появятся в
файле. Песочница воркфлоу остаётся без таймеров — ждёт `ext/`, доверенный код с `fs` и `await`.

Что это даёт: прогон всё время просто «running», и модель не нужна ни для паузы, ни для ответа.
`izi_answer` при этом продолжает работать без единой правки — он пишет в тот же файл, и ожидание
кончается само. Два канала сходятся на ОДНОМ факте, а чат-модель становится необязательной, а не
запрещённой.

Улики, ради которых наряд заведён: 19.08.2026 Grok вернул `at capacity` (окно 55 секунд, `in=0
out=0`) — ответ уже лежал в `.agent/answers.md`, а прогон стоял, пока курьер не ожил; TUI-путь хоста
(`answerCheckpoint`, host-navigator.ts:119) есть, но ранбук запрещает открывать TUI во время прогона:
retry-модалка поверх него убивает ввод. И то и другое — обход того, что вообще не должно было быть на
пути ответа.

Почему не «доработать хост»: `host.ts` прямо отказывает — `if (headless) fail("RESUME_INCOMPATIBLE",
"Headless CLI checkpoints are unsupported")`. Ждать чужой доработки незачем, когда своя половина
решается одной функцией.
## What you must prove
- новая host-функция ждёт, пока КАЖДЫЙ пункт вопроса получит ответ в `.agent/answers.md`; ожидание
  просыпается по изменению файла (`fs.watch`), с опросом как страховкой — прогон не должен зависеть
  от того, поддерживает ли ФС события
- ВОПРОС ВИДЕН ОПЕРАТОРУ БЕЗ ЧАТА: его текст печатается в пейн целиком (`log`), потому что сообщения
  в чат больше не будет; `pending.json` пишется, как и раньше, ДО ожидания
- ожидание ограничено бюджетом: те же `checkpointRetries` становятся окнами ожидания, каждое окно
  печатает, чего именно не хватает («без ответа 2 из 3»), исчерпание даёт прежний `err("question")` —
  прогон, который ждёт вечно, хуже прогона, который честно встал
- отмена работает: `workflow_stop` и abort хоста рвут ожидание, а не висят на нём
- `izi_answer` не тронут и продолжает работать; `bin/answer.mjs` снимает паузу самим фактом записи,
  без TUI и без модели
- шов: реальный возврат новой функции против реальной схемы хоста тем же валидатором (пятый случай
  класса `Invalid output` не заводить)
## Done when
- `node --test` зелёный
- на живой паузе прогон продолжается после `node bin/answer.mjs …` при ЗАКРЫТОЙ чат-модели

---
id: J14
key: IZI-JUDGES
kind: module
status: done
wave: 2
blocked_by: []
inputs:  [ext/index.mjs, ext/index.test.mjs, steps/intake/order.tpl, steps/intake/intake.md, steps/tickets/facts.mjs, steps/tickets/facts.test.mjs, workflows/izi.js]
outputs: [ext/index.mjs, ext/index.test.mjs, steps/intake/order.tpl, steps/intake/intake.md, steps/tickets/facts.mjs, steps/tickets/facts.test.mjs, workflows/izi.js]
verify: node --test
---
## Goal
Роль шага 6 не ищет то, что можно ПОДСТАВИТЬ. В наряд едет короткая таблица «имя типа → путь · вид ·
что объявляет», собранная скриптом из `.agent/graph-computed.xml` по заглавным идентификаторам,
которые называют `TASK.md`, `.agent/brd.md` и `.agent/answers.md`.

ТРЕТИЙ ИСТОЧНИК — ОТВЕТЫ ОПЕРАТОРА — добавлен по хронологии живого прогона 19.08.2026, и без него
наряд не выполняет собственный DoD: в `TASK.md` и `brd.md` формы eddi 20 заглавных имён, и
`AgentConfiguration` среди них НЕТ. Оператор назвал его в ПЕРВОМ ответе (06:39), а роль спросила его
путь в 06:56 — то есть с таблицей второй вопрос был бы невозможен, а без ответов её бы не случилось.

Улика, ради которой наряд заведён: роль спросила «AgentConfiguration model class path (not in
appgraph.xml)», хотя вычисленный граф несёт
`<decl at="src/main/java/ai/labs/eddi/configs/agents/model/AgentConfiguration.java" kind="class"/>`.
Карта роя покрывает только клетки фокуса (86 узлов), вычисленный граф — все 1856 файлов и 6890
объявлений. КОРЕНЬ: у полосы две карты, а язык шагов знает одну; решение БЮДЖЕТА (сузить фокус)
молча стало решением об ИСТИНЕ.

ПОТОЛОК НАРЯДА — НЕ ПОТОЛОК КАРТЫ, и первая редакция этого наряда их путала. `117760 Б` это
`MAP_CAP_BYTES` (`steps/intake/map.mjs:111`) — сколько КАРТЫ роль может прочитать. Наряд судится по
`ORDER_CAP_CHARS = 800000` (`core/budgets.mjs`), и на eddi он занимает 144598 символов — 18% потолка.
Запас был не 87 байт, а ~655 тысяч символов.

Отвергнутые варианты, чтобы к ним не возвращались: тул, который роль зовёт (субагент получает тулы
как `input.tools ∪ customTools`, `agent-execution.ts:254`, и передать `customTools` из песочницы
нечем — сперва эксперимент, потом решение); `bash` у роли (шелл в репозитории оператора у слабой
модели при контракте «роль пишет только в staging»).
## What you must prove
- имена берутся из текста задачи, BRD и ОТВЕТОВ оператора, резолвятся по вычисленному графу, и в
  наряд едет строка на каждое найденное: имя · путь · вид · что объявляет
- имя, которого в графе нет, в таблицу не попадает — и остаётся законным поводом спросить оператора
- таблица считается в бюджет наряда (`sized`) и имеет свой потолок, чтобы один жирный тип не вытеснил
  остальные
- на сохранённых артефактах eddi `AgentConfiguration` приезжает в наряд с верным путём
## Done when
- `node --test` зелёный
## Сделано (19.08.2026)
477 тестов зелёных (+4 шва). Резолвер ОДИН: переиспользован `factsOf`, рядом с ним живут
`namedTypes`/`typesBlock`, `declOf` научился отдавать `kind` вместо повторного вывода. Замер на живых
артефактах eddi: таблица 2 строки / 1220 Б, `AgentConfiguration` с верным путём, 150 членов (в наряд
идут 24, потолок таблицы 8192 Б); собранный наряд 144598 символов из 800000. Каждый шов доказан
обратно — правка снята, тест красный.

---
id: J15
key: IZI-JUDGES
kind: module
status: done
wave: 3
blocked_by: [J14]
inputs:  [steps/intake/frd.mjs, steps/intake/frd.test.mjs, workflows/izi.js, ext/index.mjs, steps/intake/intake.md]
outputs: [steps/intake/frd.mjs, steps/intake/frd.test.mjs, workflows/izi.js, ext/index.mjs, steps/intake/intake.md]
verify: node --test
---
## Goal
Узлом изменения может стать файл, которого нет в карте РОЯ, но который есть в репозитории. J14 закрыл
«роль может не спрашивать путь»; этот наряд закрывает «роль может этим путём воспользоваться».

Противоречие, найденное при сдаче J14 и подтверждённое живым артефактом: `F2`
(`steps/intake/frd.mjs`) требует, чтобы `<touched path>` был ключом узла `appgraph.xml`, а
`AgentConfiguration.java` им не является — рой его не читал. `F8` при этом требует, чтобы ровно этот
путь был заявлен `<delta>` или `<touched>`. Роль на прогоне 19.08.2026 разрешила тупик единственным
доступным способом: написала `form="Unknown"` (`.agent/staging/frd.xml:181-183`) УЖЕ ПОСЛЕ того, как
получила путь от оператора. То есть факт был, а места для него в грамматике не было.

Решение — из того же принципа, что и J14: о СУЩЕСТВОВАНИИ отвечает скрипт шага 3, о СМЫСЛЕ — рой
шага 5. Путь, которого нет в карте роя, но который есть в вычисленном графе, — законный узел
изменения; чего нет нигде — по-прежнему выдумка, и `F2` обязан её ловить как раньше.

Вторая половина наряда — рельса на промах подстановки (вариант D): у роли появляется выход
`track:"err", kind:"lookup", subject:"<имя типа>"`, полоса резолвит его скриптом за 0 токенов и
возвращает ответ в наряд тем же каналом, каким возит FEEDBACK. Вопрос ЧЕЛОВЕКУ остаётся ровно там,
где ему место: типа нет нигде.
## What you must prove
- `F2` принимает узел, известный вычисленному графу, и по-прежнему отвергает путь, которого нет ни
  там, ни в карте роя; в блокере видно, ГДЕ путь искали
- `F8` и `F2` согласованы: заявить модуль дельтой можно ровно тем же путём, который резолвит J14
- на сохранённых артефактах eddi FRD с `<delta node=".../AgentConfiguration.java">` проходит `F2`, и
  `form="Unknown"` для него больше не нужен
- `lookup` резолвится скриптом и не создаёт вопроса в `pending.json`; имя, которого нет в графе,
  превращается в честный `question` с текстом «в репозитории такого типа нет»
- круг `lookup` считается тем же бюджетом, что круги починки, и не может крутиться вечно
## Done when
- `node --test` зелёный; проигрыш по сохранённым артефактам eddi показывает `F2` зелёным на пути из
  вычисленного графа
## Сделано (19.08.2026)
481 тест зелёный. `F2`/`F3`/`F4` спрашивают `inRepo(path)` = узел карты роя ИЛИ путь, известный
вычисленному графу; выдуманный путь остаётся выдумкой (шов). Проигрыш на живых картах eddi: тот же
FRD давал 3 блокера без вычисленного графа и 0 с ним. Попутно найдена вторая слепота того же корня:
правило F3 «Changed, но ломаться нечему» судило по `entries`/`called` карты РОЯ, которая про файл вне
фокуса не говорит «его никто не зовёт» — она молчит; теперь правило молчит вместе с ней.
Рельса `lookup`: `kind:"lookup"` в конверте, ветка в полосе, режим `graphMap({resolve})` (карту не
возит — только строки таблицы), роль знает про рельсу и про запрет спрашивать человека о факте.

---
id: J16
key: IZI-JUDGES
kind: module
status: done
wave: 1
blocked_by: []
inputs:  [steps/intake/intake.md, steps/intake/order.tpl, steps/intake/frd.test.mjs]
outputs: [steps/intake/intake.md, steps/intake/order.tpl, steps/intake/frd.test.mjs]
verify: node --test
---
## Goal
Роль чинит ВСЕ блокеры круга за один ответ. Живой прогон 19.08.2026: гардрейл вернул 8 блокеров, роль
закрыла часть и сожгла три круга из шести на остальных. Причина не в переводе роли на английский —
русский оригинал говорил ровно то же: оба текста велели «правь ровно названные места» (это про «не
трогай остальное»), а ПРИМЕР показывал ОДИН блокер, и слабая модель читала его буквально. Слов
«закрой все» не было ни в роли, ни в наряде НИКОГДА.
## What you must prove
- роль несёт строку «CLOSE EVERY LINE OF THE FEEDBACK IN THIS ONE ANSWER» и велит СНАЧАЛА посчитать
  строки, потом сделать по правке на строку, потом сверить файл со счётом
- пример починки показывает НЕСКОЛЬКО блокеров и столько же правок — один блокер в примере и есть
  инструкция «чини один»
- наряд несёт «COUNT THE LINES AND CLOSE THEM ALL IN THIS ANSWER» и объясняет ПОЧЕМУ: проверка идёт по
  ВСЕМУ файлу, и закрытая одна строка возвращает тот же список кругом позже
## Done when
- `node --test` зелёный (473 из 473); шов пинает обе строки и счёт блокеров в примере

---
id: J18
key: IZI-JUDGES
kind: module
status: done
wave: 1
blocked_by: []
inputs:  [steps/focus/focus.mjs, steps/focus/focus.test.mjs, steps/focus/coverage.md, ext/index.mjs, ext/index.test.mjs, workflows/izi.js, steps/intake/order.tpl]
outputs: [steps/focus/focus.mjs, steps/focus/focus.test.mjs, ext/index.mjs, ext/index.test.mjs, workflows/izi.js, steps/intake/order.tpl]
verify: node --test
---
## Goal
Предмет, названный требованием, либо ПРОЧИТАН, либо назван непрочитанным ВСЛУХ. Data flow —
`steps/focus/coverage.md`.

Улика: `focus.json` прогона eddi 19.08.2026 — `why:"anchors"`, `chosen:["s242"]` (два узла,
расширения шаблонизатора), `dropped:{slices:30, cells:66}`. В `subjects[]` BRD стоит `agent`,
требование R3 целиком про agent config, 22 среза несут `agent` в пути — не взят ни один, и ни строки
об этом. Цена молчания: роль спрашивала у оператора путь `AgentConfiguration.java`, `F2`/`F3`/`F8`
краснели на существующем файле, шаг 8 встал `unknown-node`.
## What you must prove
- `newFocus` возвращает `covered` и `uncovered`; объединение = множество непустых предметов, ни один
  не потерян молча (юнит)
- `uncovered` различает `why: "cap"` (срезы есть, не влезли) и `why: "none"` (в репозитории нечего
  брать) — это разные факты и разные последствия
- потолок чтения не тронут: наряд про «не молчать», а не про «брать всё»
- `checkFocus` краснеет на предмете, не попавшем ни в один список, и блокер называет ПРЕДМЕТ и место
  дефекта (это гардрейл над скриптом, роли тут нет)
- непокрытые предметы доезжают в лог полосы строкой и в наряд шага 6 блоком `{UNCOVERED}` с прямым
  запретом додумывать то, чего рой не читал
- на сохранённых артефактах eddi правило называет `agent` непокрытым
## Done when
- `node --test` зелёный; проигрыш по `focus.json` прогона 19.08.2026 печатает непокрытый предмет
## Сделано (19.08.2026)
496 тестов зелёных (+4 шва). `coverageOf` и `checkFocus` — с контрактами, юниты на: каждый предмет
ровно в одном списке · `cap` против `none` · блокер называет предмет и место дефекта · пустые якоря
молчат. Проигрыш по СОХРАНЁННЫМ артефактам eddi воспроизвёл прогон точно (16 клеток, срез `s242`):
```
agent: взято 2 клеток, ОТБРОШЕНО 50 · template: 3/4 · snippet: 3/4
glossary — в репозитории нет ничего с таким именем   (тип создаётся этим изменением, это норма)
term — 2 среза, 4 клетки отброшено потолком
```
ПОПРАВКА К ПРЕМИССЕ НАРЯДА, найденная проигрышем: `agent` НЕ был выброшен целиком — две клетки
взяты, и предмет числился покрытым, пока модуль требования лежал среди пятидесяти отброшенных.
Поэтому строка несёт ОБЕ величины, а полоса печатает два РАЗНЫХ факта: «предметы НЕ прочитаны» и
«предметы прочитаны ЧАСТИЧНО». В наряд шага 6 уехал слот `{UNCOVERED}` с прямым запретом додумывать
операцию и её контракт для непрочитанного модуля.

---
id: J17
key: IZI-JUDGES
kind: module
status: done
wave: 2
blocked_by: [J18]
inputs:  [core/node.mjs, core/node.test.mjs, core/node.md, steps/intake/frd.mjs, steps/ripple/ripple.mjs, steps/tickets/facts.mjs, ext/index.mjs]
outputs: [core/node.mjs, core/node.test.mjs, steps/intake/frd.mjs, steps/ripple/ripple.mjs, steps/tickets/facts.mjs, ext/index.mjs]
verify: node --test
---
## Goal
«Что такое узел изменения» отвечается ОДИН раз на всю полосу. Data flow — `core/node.md`.

Три остановки живого прогона eddi 19.08.2026 подряд, один корень: `F2` шага 6 («touched не
резолвится» на существующем файле), `F3` шага 6 («ломаться нечему» — ребро есть только в вычисленном
графе), `unknown-node` шага 8 (полоса встала МЕЖДУ ДВУМЯ СВОИМИ ЖЕ шагами: шаг 6 узел принял, шаг 8
отверг). Каждое чинилось отдельно и по-своему.
## What you must prove
- `nodeKind(path)` различает ЧЕТЫРЕ состояния: `swarm` (прочитан роем — известны контракты) ·
  `repo` (существует, рой не читал — известен только путь) · `new` (создаётся этим изменением) ·
  `none` (выдумка)
- ответ одинаков у всех вызывающих на одних и тех же картах (юнит на согласие шагов 6, 8, 14)
- правило «ломаться нечему» судит ТОЛЬКО `swarm`: про `repo` карта роя не знает, кто его зовёт
- функция тотальна: без карт отвечает `none`, не бросает
- отказ `unknown-node` перестаёт называть две неверные причины и называет выход: путь из карты,
  справка `lookup` или `<delta new="yes">`
- в `steps/*` не осталось прямых `nodes.has(path)` там, где спрашивают о существовании файла — шов
  читает исходники срезов
## Done when
- `node --test` зелёный; проигрыш по артефактам eddi: ни один шаг не отвергает
  `AgentConfiguration.java`
## Сделано (19.08.2026)
502 теста зелёных (+6 швов). Заведён `core/node.mjs`: `nodeKind` отдаёт ЧЕТЫРЕ слова — `swarm`
(рой читал, известны контракты) · `repo` (файл есть, не читан, известен путь) · `new` · `none`;
`pathsOf` собирает пути вычисленного графа одним выражением вместо трёх разных у трёх вызывающих.
Шаги 6 и 8 переведены на него, `repoPaths` из `steps/intake/frd.mjs` удалён.

Отказ шага 8 переписан по `standards/guardrail.md`: было «frd.xml старше appgraph.xml либо путь
выдуман» — ОБЕ причины ложны для файла вне фокуса; стало «файла нет ни в карте роя, ни в
репозитории» с тремя выходами (путь из карты · справка `lookup` · `<delta new="yes">`). Шов
запрещает возврат старой формулировки.

Швы: четыре состояния · порядок проверок (`created` раньше существующего, рой раньше графа) ·
тотальность · СОГЛАСИЕ ШАГОВ (6 и 8 на одних картах отвечают одинаково и одинаково отвергают
выдумку) · репозиторный шов «о существовании спрашивают через один модуль».

Проигрыш по СОХРАНЁННЫМ артефактам прогона: `AgentConfiguration.java → repo`, шаг 6 зелен, шаг 8
зелен (29 узлов, 8 затравок) — ровно там, где прогон встал час назад.


---
id: P1
key: IZI-PASSES
kind: module
status: done
wave: 1
blocked_by: []
inputs:  [steps/intake/frd.mjs, steps/intake/frd.test.mjs, steps/intake/passes.md]
outputs: [steps/intake/frd.mjs, steps/intake/frd.test.mjs]
verify: node --test
---
## Goal
Правило знает свой ПЛАСТ и не судит чужой. `checkFrd({ pass })`: каждое из 33 правил объявляет, к
какому пласту относится — `A` требование · `B` изменение · `C` величины и отказы · `D` покрытие; без
`pass` судятся все, как сегодня (это режим resume и F9). Data flow — `steps/intake/passes.md`.

Улика: сегодня шаг 6 — ОДИН вызов роли, судимый всеми правилами сразу, и артефакт в работе всегда
красен по слоям, которых ещё нет. Живой прогон 19.08.2026: первый вердикт 15 блокеров, из них 6 про
сценарии, которых роль ещё не начинала писать.
## What you must prove
- фикстура из одних `<usecase>` зелена в проходе A и красна без пласта — разница видна на одном
  артефакте
- правило чужого пласта МОЛЧИТ, а не «мягко предупреждает»: пласта нет — судить нечего
- ни одно правило не потеряло своего теста; каждое объявляет пласт ровно один раз
- полный режим (`checkFrd({})`) даёт тот же список блокеров, что сегодня, — на сохранённом FRD eddi
  сверка байт в байт
## Done when
- `node --test` зелёный; проигрыш по артефактам eddi в полном режиме не изменился


## Сделано
`checkFrd({ pass })` фильтрует блокеры по таблице `RULE_PASS` (18 кодов, пласт объявлен ОДИН раз —
не у тридцати трёх мест `B.push`). Проход показывает правила своего пласта И всех закрытых: порча
закрытого пласта краснеет в том же круге, в котором её сделали (живой прогон 12:51, 19.08.2026).
`F9` — вне пластов, сторож перемотки стоит во всех четырёх.

Замер по трём сохранённым артефактам eddi: полный суд БЕЗ ИЗМЕНЕНИЙ (16 / 24 / 26 блокеров, байт в
байт); проход A — 0 блокеров на всех трёх. Вся масса живых блокеров лежит в пласте B, там же, где
карта.

Швы (сняты обратно → красный): пласт «свой и все предыдущие»; внепластовый F9; незнакомый код звучит
во ВСЕХ проходах, а не тонет в одном; правило без записи в таблице роняет шов по исходнику.
---
id: P2
key: IZI-PASSES
kind: module
status: done
wave: 2
blocked_by: [P1]
inputs:  [ext/index.mjs, ext/index.test.mjs, steps/intake/order-a.tpl, steps/intake/order-b.tpl, steps/intake/order-c.tpl, steps/intake/order-d.tpl, steps/intake/intake.md]
outputs: [ext/index.mjs, ext/index.test.mjs, steps/intake/order-a.tpl, steps/intake/order-b.tpl, steps/intake/order-c.tpl, steps/intake/order-d.tpl, steps/intake/intake.md]
verify: node --test
---
## Goal
Скелет и наряд на КАЖДЫЙ проход. `frdPass({ pass })` отдаёт скелет пласта и правила этого прохода;
четыре шаблона вместо одного; карта роя подставляется ТОЛЬКО в B — в A и C рассуждать о репозитории
запрещено, а в D роль лишь ссылается на уже написанное, и о репозитории за неё судит скрипт (`F8`
читает `graph-computed.xml` сам).

Цена сегодняшнего единственного наряда: 137 КБ на каждом круге, из них 107 811 Б карты. Проход A
работает с задачей и BRD, и карта в нём — чистая трата окна модели.
## What you must prove
- карта есть ровно в одном наряде — B; наряды A, C и D без неё, наряд A умещается в 10 КБ
- сумма четырёх проходов даёт ТОТ ЖЕ артефакт, что сегодня даёт один вызов: проверяется на фикстуре
  сравнением с однопроходным результатом
- слоты шаблонов и ключи полосы — одно множество для каждого прохода (шов уже существует, его надо
  расширить на четыре наряда)
- роль в каждом наряде знает, ЧТО пишет и чего НЕ пишет: пласт назван, чужие элементы прямо
  запрещены, и у каждого наряда есть задача ОДНОЙ фразой с выходом из тупика
  (`steps/intake/passes-data-flow.md`: B — спроси справку, C — оставь `<question>` вместо догадки)
## Done when
- `node --test` зелёный; замер размера каждого наряда приложен


## Сделано
Четыре наряда: `order-a.tpl` 3.7 КБ · `order-b.tpl` 6.6 КБ · `order-c.tpl` 4.4 КБ · `order-d.tpl`
2.9 КБ (было — один на 137 КБ с картой на каждом круге). Карта роя едет РОВНО в один, B; шов
`ext/index.test.mjs` это и держит. Роль одна на все проходы, и её ЗАКОН 0 велит писать только
названный пласт.

`frdPass` не заведён намеренно: хост-функция «скелет пласта» была бы вторым экземпляром грамматики
рядом с шаблоном. Обещание «наряд говорит то, чем судят» держит ШОВ — `core/orders.test.mjs` сверяет
коды из `$START_SELFCHECK` каждого наряда с `RULE_PASS`: своё не названо или названо чужое — красный.
---
id: P5
key: IZI-PASSES
kind: module
status: done
wave: 3
blocked_by: [P1]
inputs:  [steps/review/review.mjs, steps/review/review.test.mjs, steps/intake/passes-data-flow.md]
outputs: [steps/review/review.mjs, steps/review/review.test.mjs]
verify: node --test
---
## Goal
Находка критика знает свой проход. `passOf(finding, frd)` — чистая функция: на вход находка шага 11
(`code`, `node`) и разобранный FRD, на выход `"A" | "B" | "C" | "D"`. Маршрут выводится из того, в
каком ПЛАСТЕ лежит названный элемент, а не из кода находки: один и тот же `invented-value` уезжает в
A (лишний use case), в C (лишнее значение) или в B (лишняя работа). Таблица —
`steps/intake/passes-data-flow.md` §«Куда направлять доработку после критика».

Улика: критик судит требование целиком и о проходах не знает — он называет ЭЛЕМЕНТ. Без этой функции
полоса вынуждена гадать по коду, а код пласта не определяет.
## What you must prove
- `invented-value` на трёх разных элементах даёт три разных прохода — на одной фикстуре
- `requirement-not-carried` разделён: носителя нет вовсе → `A`; носитель есть, строки нет → `D`
- элемент, принадлежащий двум пластам (дельта, названная сценарием), уезжает в РАННИЙ: починка
  нижнего пласта может снять находку верхнего, обратное неверно
- `open-question` не адресуется роли вовсе — это вопрос оператору
- элемент, которого в FRD нет, не роняет функцию и не молчит: она обязана вернуть проход (`A`), а не
  `undefined`, — тотальность по `standards/code.md`
- функция чистая: ни путей, ни чтения диска, юнит гоняет её без полосы

## Done when
- `node --test` зелёный; на сохранённых находках прогона 19.08.2026 маршрут назван для каждой


## Сделано
`passOf(finding, frd)` — маршрут по ЭЛЕМЕНТУ, а не по коду: один `invented-value` уезжает в A
(лишний use case), B (лишняя работа) и C (лишнее значение). `criticEntry(findings, frd)` — ранний из
названных.

Находка: **D адресом критика не бывает**, и это следствие F11, а не решение. «Строки `<carried>` не
хватает» живёт ровно до шага 11 — F11 не выпускает пласт D, пока строка есть у каждого требования.
Значит строка ЕСТЬ и она ложна, носителя нет, и пишет его пласт A. Деление кода на A/D из плана снято
как нереализуемое.

Швы: маршрут по коду вместо элемента — 4 красных; поздний пласт раньше раннего (фикстура, где `X` —
и id use case, и id сценария) — красный; `requirement-not-carried` в D — красный.
---
id: P3
key: IZI-PASSES
kind: module
status: done
wave: 4
blocked_by: [P2, P5]
inputs:  [workflows/izi.js, ext/index.mjs, ext/index.test.mjs]
outputs: [workflows/izi.js, ext/index.mjs, ext/index.test.mjs]
verify: node --test
---
## Goal
Полоса ведёт A→B→C→D. У каждого прохода свой бюджет кругов; красный проход НЕ открывает следующий.
Спрашивать оператора умеет ЛЮБОЙ проход — незнание рождается там, где пишется пласт, — но проход не
открывает следующий, пока его собственный вопрос без ответа. Критика шага 11 адресуется ОДНОМУ
проходу — маршрут даёт `passOf` (наряд P5), полоса его не вычисляет.

Улика: на живом прогоне 19.08.2026 три вопроса и ни одного из пласта A — «в каком классе держать
ссылки и как назвать поле» (пласт B), «какой HTTP-код на провал валидации» (C), «куда внедрять
подстановку» (B). Все три прилетели ПОСЛЕ того, как роль написала FRD целиком, и ответ обесценил уже
написанные дельты, сценарии и поля.
## What you must prove
- красный B не пускает в C; зелёный A не переигрывается при возврате из B
- вопрос ЛЮБОГО прохода будит оператора один раз и не открывает следующий проход, пока не отвечен:
  фикстура с вопросом в B не пускает в C
- находка критика перезапускает проход, который назвал `passOf`, и полоса идёт от него ВПЕРЁД
  (A → B → C → D): новый элемент нижнего пласта должен получить дельты, величины и строку покрытия
- после D идёт ПОЛНАЯ проверка без пласта: правила, судящие два пласта сразу (`F3c`, `F10`, `F8`), в
  проходном режиме видят только свой, и пласты могут разойтись между собой
## Done when
- `node --test` зелёный; на фикстуре пройден весь маршрут A→B→C→D с одним красным проходом посередине


## Сделано
Полоса ведёт A→B→C→D, промоут один — после ПОЛНОГО суда без пласта; гейт сьютов там же (в проходе он
спрашивал бы о ширине, которая ещё растёт). Красный полный суд возвращает в пласт, который его
ЧИНИТ, и полоса идёт от него вперёд до D.

Две таблицы вместо одной: `RULE_PASS` — кто ВИДИТ, `RULE_FIX` — кто ЧИНИТ. Расходятся сегодня на
одном правиле: F8 загорается в D (нужны поля пласта C и дельты пласта B), а закрывается ДЕЛЬТОЙ,
которую пишет только B. Без этого роль пласта D получала бы блокер, закрывать который её наряд ей
запрещает, — тупик, прямо названный в `standards/guardrail.md`.

Полоса проверяется ИСПОЛНЕНИЕМ, а не регуляркой: срез `$START_INTAKE` из `workflows/izi.js`
запускается на стенде из 20 заглушек (`ext/index.test.mjs`). Семь тестов: порядок проходов и один
промоут; красный проход не пускает в следующий, а зелёный не переигрывается; исчерпанный бюджет
встаёт на своём проходе; вопрос задаёт любой проход и пауза названа его именем (`intake-C`), не
тратя круг починки; возврат критика входит в названный проход; красный полный суд возвращает в ранний
пласт; гейт и промоут стоят на полном суде.

Швы (сняты обратно → красный): красный проход пускает дальше · промоут внутри прохода · вход по
критику игнорируется · имя паузы без пласта · полный суд без возврата.
---
id: P4
key: IZI-PASSES
kind: module
status: todo
wave: 5
blocked_by: [P3]
inputs:  [sandbox/runbox/eddi (свежая копия), standards/runbox.md, standards/live-run.md]
outputs: []
verify: живой прогон с нуля + node --test
---
## Goal
Живой замер против однопроходного шага. Форма eddi с нуля, по `standards/runbox.md` и под
наблюдением `standards/live-run.md`.

Точка отсчёта — прогон 19.08.2026 на однопроходном шаге 6: первый вердикт 4 блокера, три круга до
зелёного, наряд 137 КБ, 5 обрывов провайдера.
## What you must prove
- блокеров НА КРУГ меньше, чем в однопроходном шаге: правило не видит пластов, которых ещё нет
- токенов на прогон меньше: проходы A и C идут без карты
- вопрос оператору задан в СВОЁМ проходе, а не после написания всего FRD: на сегодняшнем счёте три
  вопроса пришли последними, здесь вопрос про модуль должен прийти в B, до величин и покрытия
- шаг 6 закрывается зелёным, шаг 11 выносит вердикт, полоса доходит до сухого прогона нарезки
## Done when
- замер приложен к наряду: блокеры по кругам и токены по проходам против сегодняшнего счёта

---
id: M1
key: IZI-MAP
kind: module
status: done
wave: 1
blocked_by: []
inputs:  [steps/graph/graph.mjs, steps/graph/graph.test.mjs, steps/intake/map.mjs, steps/intake/map.test.mjs]
outputs: [steps/graph/graph.mjs, steps/graph/graph.test.mjs, steps/intake/map.mjs, steps/intake/map.test.mjs]
verify: node --test
---
## Goal
Тест — строка карты, а не блок. Тестовый файл перестаёт быть `<module>` с объявлениями, api, io и
ролью; остаётся `<test path suite>` — внутри модуля, который он проверяет, и строкой верхнего уровня,
если предмет теста в карту не попал.

Замер по карте прогона 19.08.2026 (`prev-runs/eddi-passA-green/appgraph.xml`): **34 модуля из 85 —
тестовые, и они стоят 19 189 Б, 18% всей карты** при потолке 117 760 Б и занятых 107 642 Б. Модуль и
так называет свои тесты строкой `<test>`; тестовый модуль рядом — второй экземпляр того же факта
(CLAUDE.md: одно правило — одно место).

Цена молчания: свободных байт 10 118, а клетка `configs/agents` (13 файлов, 220 объявлений, ≈4-6 КБ)
из-за них не влезает — и предмет требования `agent` уходит в прогон непрочитанным.
## What you must prove
- ни один `<module>` карты не несёт `kind="test"`; каждый тестовый путь всё равно резолвится
  `parseMap → map.tests` — на нём стоят F2/F3 («тест это `<dod>` изменения, а не изменение»)
- `nodeTests` шага 10 отдаёт те же пары «узел → тест · сьют», что и сегодня: проигрыш по
  сохранённой карте eddi сверяет их поимённо
- `blindNodes` гейта шага 6 на тех же входах даёт тот же список узлов без сьюта
- тест, чей предмет в карту НЕ попал, не исчезает: он строка верхнего уровня, иначе `map.tests`
  беднеет молча и правило F2 замолкает на живом артефакте
- замер приложен: сколько байт освободилось на карте eddi
## Done when
- `node --test` зелёный; карта eddi пересобрана из тех же частей и стала меньше на замеренную величину

## Сделано (19.08.2026)
Карта eddi пересобрана из тех же 15 частей: **107 624 → 92 010 Б, освободилось 15 614 Б.** Модулей
51 вместо 85, тестов строкой 34. Шапка карты получила `tests="N"` — она считает написанное.

Замер, которого не было в наряде и который решил его форму: строк `<test>` в старой карте **ноль** —
привязки «модуль → его тест» скаут не дал ни одной, то есть полные тестовые блоки не несли даже её.
Поэтому все 34 выходят строкой ВЕРХНЕГО уровня, а не детьми модулей.

`parseMap.tests` собирается из обеих форм: старой `kind="test"` (карты прошлых прогонов читаются) и
новой строки. `KEEP` индексной формы дополнен `test` — иначе выше потолка чтения тесты пропадали бы.

Долг наряда, обнаруженный проигрышем и закрытый здесь же: `MAP_PRICE.test = 95` и учёт его в оценке
фокуса. Без этого освободившиеся 15 КБ до якорей не доходили — оценка продолжала считать тест узлом с
объявлениями.

Швы (сняты обратно → красный): тест снова блоком · сирота не написан · сирота написан дважды ·
`parseMap` не читает строку · тест снова оценивается как узел.

---
id: M2
key: IZI-MAP
kind: module
status: done
wave: 2
blocked_by: [M1]
inputs:  [steps/focus/focus.mjs, steps/focus/focus.test.mjs]
outputs: [steps/focus/focus.mjs, steps/focus/focus.test.mjs]
verify: node --test
---
## Goal
Бюджет фокуса делится по ЯКОРЯМ, а не по дешевизне клеток. Фаза 3 (`cellsByCost`) идёт по кругу:
каждому якорю его самая дешёвая невзятая клетка, затем второй круг, третий — пока жив бюджет. Клетка,
названная несколькими якорями, засчитывается тому, кто взял меньше. Клетка по-прежнему берётся
целиком: её состав — ключ кэша `.izi/parts`.

Улика: живой прогон 19.08.2026. Фаза 3 сортирует ВСЕ названные клетки по размеру, якорь при этом не
учитывается — один якорь съедает бюджет, другой получает ноль. Предмет `agent` (требование R6 целиком
про ссылку в конфигурации агента) доехал двумя случайными клетками из 52: `engine/rest/RestAgentSetup`
и тестом `readiness/AgentsReadinessTest`. Ни одного файла `configs/agents/` в карте, при том что
`AgentConfiguration.java` — носитель требования.
## What you must prove
- фикстура: у якоря A три дешёвые клетки, у якоря B одна дорогая. Старый порядок даёт B ноль, новый —
  одну. Шов снимается обратно
- ни один якорь, у которого есть хоть одна влезающая клетка, не остаётся с нулём, пока другой взял
  вторую
- клетка, названная двумя якорями, засчитывается ОДИН раз и тому, кто отстаёт
- порядок внутри якоря прежний: дешёвая первой (и это единственное, что осталось от старого правила)
- проигрыш на настоящих входах eddi: клетка `configs/agents` входит в фокус, карта остаётся под
  потолком
## Done when
- `node --test` зелёный; на входах прогона 19.08.2026 `agent` перестаёт быть предметом с 2 клетками
  из 52, и это видно в `focus.json`

## Сделано (19.08.2026)
Проигрыш на настоящих входах eddi (план 284 клетки, вычисленный граф, якоря BRD):
```
было:  клеток 15 · файлов 91  · agent 2/52 · configs/agents — 0 файлов
стало: клеток 18 · файлов 118 · agent 4/52 · configs/agents — 13 файлов, среди них
                                             AgentConfiguration.java — носитель требования R6
```
Одной квоты НЕ ХВАТИЛО, и это главный результат наряда: она честно раздала `agent` пять клеток, и все
пять оказались упоминаниями. Понадобились ещё три решения, каждое найдено замером:

1. **цена теста в оценке** (долг M1) — иначе освободившееся место не доходит до якорей;
2. **ход дома до фазы аналога** — фаза аналога выбирала бюджет раньше, чем якоря получали ход.
   Порядок фаз при этом НЕ тронут: «аналог первым» куплен замером 9/10 против 2/10, и вперёд
   пускается ровно ОДНА клетка на якорь, а не все названные — на eddi у `agent` таких три из 52;
3. **дом = каталог, НАЗВАННЫЙ предметом** (с точностью до множественного числа), и берётся
   ПЛОТНЕЙШИЙ. Две ложные ветки по дороге, обе с уликой: `docs/your-first-agent/` считался домом по
   вхождению подстроки, а `engine/runtime/client` (6 файлов, под `agent/` один) побеждал дешевизной
   настоящий дом `configs/agents` (13 файлов, все под каталогом).

Швы (сняты обратно → красный): «дешёвая первой по всем якорям» · дом по вхождению подстроки · дом по
дешевизне вместо плотности · ход дома после фазы аналога · тест оценивается как узел.

---
id: M3
key: IZI-MAP
kind: run
status: todo
wave: 3
blocked_by: [M1, M2]
inputs:  [sandbox/runbox/eddi, standards/runbox.md, standards/live-run.md]
outputs: []
verify: живой прогон + node --test
---
## Goal
Живой прогон формы eddi с нуля после M1+M2. Точка отсчёта — прогон 19.08.2026: карта 107 642 Б,
85 модулей (34 теста), `agent` 2 клетки из 52, `configs/agents` в карте ноль файлов.
## What you must prove
- карта меньше на величину, замеренную в M1, и по-прежнему под потолком
- `AgentConfiguration.java` в карте есть — предмет требования читан, а не назван непрочитанным
- роль пласта B не спрашивает оператора о том, что лежит на диске: её вопросы — только решения о
  продукте
- шаг 6 закрывается зелёным по всем четырём пластам
## Done when
- замер приложен: байты карты, состав по якорям, вопросы роли по пластам

---
id: M4
key: IZI-MAP
kind: module
status: done
wave: 1
blocked_by: []
inputs:  [steps/intake/frd.mjs, steps/intake/frd.test.mjs, steps/intake/intake.md, steps/intake/order-b.tpl]
outputs: [steps/intake/frd.mjs, steps/intake/frd.test.mjs, steps/intake/intake.md, steps/intake/order-b.tpl]
verify: node --test
---
## Goal
Артефакт сначала ЧИТАЕМ, потом судим. Сырой `<` внутри значения атрибута обрывает элемент в сканере
(`core/xml.mjs`), элемент исчезает из разбора целиком, и правила судят артефакт, в котором его нет —
обвиняя роль в том, чего она не писала (`standards/guardrail.md`).

Улика — живой прогон eddi 19.08.2026, третий запуск, дошедший до шага 11 и вставший там:
```
пласт B  <scenario id="S6" uc="UC6" before="… <code>{{glossary.<term>}}</code> …"/>
         → 2 блокера: «у UC6 нет сценария» (ЛОЖЬ, он написан) и «дельта GlossaryService без сценария»
пласт A  <question why="… <code>…</code> …"/>  → открытый вопрос исчез из артефакта молча
пласт C  <field name="terms"      type="array<Term>"/>   → поле пропало
         <field name="glossaries" type="array<URI>"/>    → поле пропало
шаг 6 закрылся ЗЕЛЁНЫМ с 5 полями вместо 7
шаг 11  R7 <covers item="R8" node="field:glossaries"/> — «node не элемент FRD»
         критик назвал носителя ВЕРНО; его отвергли за то, чего нет в разборе. Круги критика сожжены
```
## What you must prove
- `unreadable(xml)` называет строку, атрибут и ВЫХОД; чистый артефакт молчит; функция тотальна
- блокеры F0 не проходят через `forPass`: пласта у них нет, испорченная строка слепит любой проход
- роль и наряд пласта B предупреждают о значении атрибута ДО того, как оно стоит круга: правило,
  НЕВЕРНАЯ строка и верная — рядом
- пример в тексте роли НЕ из живого домена (шов `ext/index.test.mjs` — слабая модель вернёт заготовку)
## Done when
- `node --test` зелёный; на сохранённом артефакте прогона правило называет оба поля

## Сделано (19.08.2026)
543 теста. `F0` в `steps/intake/frd.mjs::unreadable`, вызывается из `newFrd` ДО `checkFrd` и мимо
фильтра пластов. На промоутнутом артефакте прогона даёт ровно два места — `array<Term>` и
`array<URI>`, те самые поля, ради которых делалась задача.

Правило-текст добавлено В ОДНОМ месте на все проходы (закон 7a роли) и в наряд пласта B, где оно
сгорело: форма «WRONG → RIGHT» строкой, без объяснений. Пример на чужом домене (`{{parcel.FIELD}}`) —
иначе шов «ни одного слова живого домена в роли» краснеет, и он прав.

Замечание на будущее, не наряд: роль ЧИНИТ такое сама, если ей сказать. В круге 2 пласта B она
переписала значение словами, а не добавила второй сценарий, — беда была в том, что никто не говорил.

---
id: Q1
key: IZI-ASK
kind: module
status: done
wave: 1
blocked_by: []
inputs:  [core/ask.mjs, core/ask.test.mjs, ext/index.mjs, ext/index.test.mjs, workflows/izi.js]
outputs: [core/ask.mjs, core/ask.test.mjs, ext/index.mjs, ext/index.test.mjs, workflows/izi.js]
verify: node --test
---
## Goal
Разговор с оператором становится АРТЕФАКТОМ шага. `.agent/ask.xml` копит по записи на обмен: шаг,
проход, размер черновика на момент вопроса, вопросы и ответы дословно. Замысел — `docs/ask.md` §3а.

Сегодня вопрос живёт в `.agent/pending.json`, который стирается после ответа: узнать после прогона,
о чём роль спрашивала на шаге 6, неоткуда — а именно это первым делом ищет разбор.
## What you must prove
- запись появляется ПОСЛЕ ответа оператора и несёт номер, вопрос и ответ дословно — их копирует
  машина, не роль (CLAUDE.md, ограничение 4)
- пишется РАЗМЕР черновика, а не его тело: тело лежит в staging, копия артефакта в артефакте — два
  места для одного факта
- запись не теряется при следующем обмене: файл копит, а не перезаписывается
- `newRun` уносит `ask.xml` в `.agent/prev` вместе с остальным состоянием прогона
## Done when
- `node --test` зелёный; на прогоне видно пять обменов подряд с их проходами


## Сделано (19.08.2026)
`core/ask.mjs::askEntry` — чистая, юнит из пяти случаев. `clearPending` из «стереть pending» стал
«закрыть обмен»: сперва пишет запись в `.agent/ask.xml`, потом стирает. Вопросы берёт из
`pending.json`, ответы из `answers.md` — обе стороны копирует машина; полоса называет только то, чего
на диске нет: шаг, проход и путь к черновику.

Запись несёт `draft="0"` против `draft="5217"` — это разные факты о работе: роль спросила ДО того, как
что-то написала, или уже имея черновик, и от этого зависит, было ли ей что исправлять.

`newRun` уносит `ask.xml` в `.agent/prev` вместе с ответами: разговор — их след.

Швы (сняты обратно → красный): запись не ведётся · в обмен попадают чужие вопросы · текст не
экранируется (ответ живого прогона нёс `{{glossary.<term>}}`).
---
id: Q2
key: IZI-ASK
kind: module
status: done
wave: 2
blocked_by: [Q1]
inputs:  [workflows/izi.js, steps/intake/order-a.tpl, steps/intake/order-b.tpl, steps/intake/order-c.tpl, steps/intake/order-d.tpl, steps/brd/order.tpl, core/orders.test.mjs]
outputs: [workflows/izi.js, steps/intake/order-a.tpl, steps/intake/order-b.tpl, steps/intake/order-c.tpl, steps/intake/order-d.tpl, steps/brd/order.tpl, core/orders.test.mjs]
verify: node --test
---
## Goal
Новые ответы едут ОТДЕЛЬНЫМ блоком `{ANSWERED}` с приказом применить, а не растворяются в накопленной
истории `{ANSWERS}`. Замысел — `docs/ask.md` §3б.

Улика: на пятом обмене прогона 19.08.2026 история весила 1 466 символов, и три новых ответа ничем в
ней не выделялись. Роль применила один из трёх.
## What you must prove
- блок несёт РОВНО последний обмен: вопрос и ответ парой, с номерами
- обмена не было — блока НЕТ вовсе; пустой блок роль читает как «ответов не было»
- в блоке стоит приказ: это единственное новое знание с твоего прошлого черновика, примени каждый
  ответ
- слоты и ключи по-прежнему одно множество (шов `core/orders.test.mjs`)
- {PREVIOUS} рядом: черновик, как он лежал в момент вопроса (пусто — если роль спросила до записи)
## Done when
- `node --test` зелёный; на фикстуре наряд после ответа несёт ANSWERED и PREVIOUS


## Сделано (19.08.2026)
`answeredBlock(seen, asked)` в полосе + слот `{ANSWERED}` во всех четырёх нарядах шага 6. Блок несёт
РОВНО последний обмен парами «ВОПРОС → ОТВЕТ ОПЕРАТОРА» и приказ: это единственное новое знание с
прошлого черновика, примени каждый ответ, непримененный станет блокером F13.

Обмена не было — блок говорит это СЛОВАМИ («вопросов ты ещё не задавал»), а не пустотой: пустой блок
роль читает как «ответов не было».

Швы: обмен не запоминается · пустой блок вместо слов — оба красные при снятии.
---
id: Q3
key: IZI-ASK
kind: module
status: done
wave: 3
blocked_by: [Q1]
inputs:  [steps/intake/frd.mjs, steps/intake/frd.test.mjs, ext/index.mjs]
outputs: [steps/intake/frd.mjs, steps/intake/frd.test.mjs, ext/index.mjs]
verify: node --test
---
## Goal
`F13` — ОТВЕТ ОПЕРАТОРА ОБЯЗАН БЫТЬ ПОТРАЧЕН. У ответа берутся твёрдые знаки — путь с методом
(`GET /glossarystore/glossaries/{id}`), код (`422`, `TERM_KEY_INVALID`), имя с точкой
(`ai.labs.glossary`); хоть один обязан встретиться в артефакте. Знаков нет — правило МОЛЧИТ.
Замысел и пруф — `docs/ask.md` §3в, §5.

Улика: прогон 19.08.2026, пласт A закрылся ЗЕЛЁНЫМ, потеряв два ответа из трёх. Ни одно правило не
сверяет артефакт с ответами: F1 судит состав use case, F6c — концы, F11 — покрытие требований.
## What you must prove
- пруф воспроизводится: на `answers.md` и `frd.xml` того прогона правило даёт БЛОКЕР на
  `GET /glossarystore/glossaries/{id}` и молчит на трёх ответах без знаков
- блокер называет ВОПРОС, ОТВЕТ и знак, которого нет в файле, — и выход: применить ответ либо
  вернуться к оператору, если ответ устарел
- правило вне пластов (`*`): ответ теряется одинаково в любом проходе
- потолок назван вслух в контракте: закрывает ~40% обменов, остальное — работа критика
## Done when
- `node --test` зелёный; проигрыш по артефактам прогона печатает ровно один блокер


## Сделано (19.08.2026)
`core/answers.mjs::hardTokens` + `steps/intake/frd.mjs::spentAnswers` (F13, пласт `*`). Проигрыш по
артефактам прогона: пять обменов → ОДИН блокер, ровно тот потерянный
`GET /glossarystore/glossaries/{id}`; `422` признан потраченным; три ответа без твёрдых знаков —
молчание.

Потолок назван вслух в контракте функции и в `docs/ask.md` §6: знаки нашлись у двух ответов из пяти,
то есть правило закрывает ~40% обменов, остальное судит критик прозой.

Швы: правило судит ответы без знаков · у блокера нет второго выхода · путь с методом перестал быть
знаком — все три красные при снятии.
---
id: Q4
key: IZI-ASK
kind: run
status: todo
wave: 4
blocked_by: [Q1, Q2, Q3]
inputs:  [sandbox/runbox/eddi, standards/live-run.md]
outputs: []
verify: живой прогон + node --test
---
## Goal
Прогон ровно с того места, где роль спрашивает: шаг 6, пласт A. Точка отсчёта — прогон 19.08.2026:
три вопроса, три ответа, два потеряны, пласт закрылся зелёным.
## What you must prove
- роль получает черновик, свои вопросы и ответы одним нарядом
- ответ с твёрдыми знаками потрачен либо назван блокером F13
- `.agent/ask.xml` несёт все обмены шага после прогона
## Done when
- замер приложен: сколько ответов потрачено, сколько поймано правилом, сколько ушло к критику

---
id: L0
key: IZI-LOOP
kind: module
status: done
wave: 1
blocked_by: []
inputs:  [ext/index.mjs, ext/index.test.mjs, core/runlog.mjs]
outputs: [ext/index.mjs, ext/index.test.mjs]
verify: node --test
---
## Goal
`newRun` уносит состояние прошлого прогона ТОЛЬКО у прогона С НУЛЯ. Вход с шага N>1 — это
ПРОДОЛЖЕНИЕ: ответы оператора, открытый вопрос и разговор принадлежат текущей работе.

Улика: после гейта 1 луп читает слова оператора из `.agent/answers.md`. Сегодня `newRun` первым
действием уносит их в `.agent/prev`, и перезапуск ради реворка теряет и решение гейта, и все ответы,
на которых стоит FRD. Прогон 19.08.2026: пять обменов, каждый — пауза человека.
## What you must prove
- лестница спрашивается ТОЙ ЖЕ функцией, что ведёт полосу (`core/runlog.mjs::resumeAt`), а не вторым
  разбором журнала
- вход с шага 1 — уносит, как сегодня; вход с шага N>1 — оставляет ответы, `pending.json` и `ask.xml`
- staging чистится в обоих случаях: недописанный черновик прошлой попытки не наследуется
## Done when
- `node --test` зелёный; строка лога различает «убрано» и «продолжение — состояние оставлено»


## Сделано (19.08.2026)
`newRun` спрашивает `core/runlog.mjs::resumeAt` — ту же функцию, что ведёт полосу, — и различает два
случая. Вход с шага 1: уносит ответы, вопрос и разговор, как раньше. Вход выше первого шага:
оставляет их на месте, потому что это ТА ЖЕ работа. Staging убирается в обоих случаях — недописанный
черновик прошлой попытки не наследуется.

Ответ хост-функции получил поле `kept`, и полоса печатает другую строку: «продолжение — ответы и
разговор оставлены на месте». Числа `answers`/`pending` теперь значат УНЕСЕНО, а не «было»: иначе лог
сказал бы «убрано 5 ответов» о файле, который остался.

Шов (снят обратно → красный): продолжение снова теряет ответы.
---
id: L5
key: IZI-LOOP
kind: module
status: done
wave: 1
blocked_by: []
inputs:  [steps/intake/frd.mjs, steps/intake/frd.test.mjs, ext/index.mjs]
outputs: [steps/intake/frd.mjs, steps/intake/frd.test.mjs, ext/index.mjs]
verify: node --test
---
## Goal
`F14` в пласте B: предмет требования, у которого В РЕПОЗИТОРИИ ЕСТЬ СВОЙ ПАКЕТ, обязан иметь в
изменении хотя бы один модуль из этого пакета. Ни одного списка слов: `subjects[]` пишет шаг 2,
пакеты знает вычисленный граф шага 3, модули изменения — дельты и `touched`.

Улика: `subjects[]` несёт `agent`, репозиторий несёт `configs/agents` (13 файлов, среди них
`AgentConfiguration.java`), изменение не трогает ни одного. Требование R11 закрылось ложной строкой
`<carried req="R11" by="UC5/1"/>`, где шаг ЧИТАЕТ ссылку, а не создаёт её; критик написал `Pass`;
`AgentConfiguration` не попал ни в дельты, ни в план, и гейт 1 забраковал работу.
## What you must prove
- предмет со своим пакетом и без модуля изменения — блокер с тремя выходами (дельта · `touched` ·
  `<question>`, если предмет к работе не относится)
- предмет БЕЗ пакета молчит: его создаёт это изменение, трогать нечего
- предмет, у которого модуль есть, молчит
- аналог из `analogue:` не судится: его копируют, а не меняют
- цена названа вслух в контракте: на живом артефакте 3 блокера на 6 предметов, один шумный
## Done when
- `node --test` зелёный; проигрыш по артефактам прогона называет `agent`


## Сделано (19.08.2026)
`F14` в пласте B, на трёх фактах, добытых полосой раньше: якоря `subjects[]` (шаг 2), каталоги
вычисленного графа (шаг 3), дельты и `touched` (пласт B). Ни одного списка слов.

Проигрыш по артефакту забракованного плана: **3 блокера на 6 предметов** — `agent` (тот самый
дефект), `descriptor` (похоже, тоже настоящий: R19 про `{id}.descriptor.json`) и `configuration`
(шум: каталог `engine/runtime/client/configuration` совпал именем). Цена названа в комментарии
правила: один лишний круг против потерянного требования и забракованного плана.

Молчит там, где судить нечем: у предмета нет своего пакета (его создаёт это изменение), модуль уже
есть, предмет — аналог из `analogue:` (его копируют, а не меняют).

Наряд пласта B получил пункт самоповерки 5a. Швы (сняты обратно → красный): аналог тоже судится ·
предмет без пакета краснеет.
---
id: L1
key: IZI-LOOP
kind: module
status: done
wave: 2
blocked_by: []
inputs:  [steps/planreview/plan-critic.md, steps/planreview/order.tpl, steps/planreview/planreview.mjs, steps/planreview/planreview.test.mjs, ext/index.mjs]
outputs: [steps/planreview/plan-critic.md, steps/planreview/order.tpl, steps/planreview/planreview.mjs, steps/planreview/planreview.test.mjs, ext/index.mjs]
verify: node --test
---
## Goal
Второй судья: план против требований. Роль `plan-critic` читает `brd.md`, `frd.xml` и `PLAN.md` (плюс
слова оператора, когда луп включён после гейта) и возвращает находки СТРОКАМИ:

```
R<n> | ПЛАН ПОТЕРЯЛ | <адрес: модуль плана> | <что дописать>
R<n> | НЕ НАПИСАНО  | <что добавить в FRD>  | <почему план без этого соврёт>
```

Улика: наш критик требования написал `Pass` на артефакте, где второй судья за ОДИН вызов нашёл
4 находки и сам их промаршрутизировал — 4 из 4 верно. План против требований не судит никто: шаг 10б
проверяет лишь, режется ли он.
## What you must prove
- `findingsOf(text)` разбирает строки в структуру, мусорная строка её не роняет, пустой вердикт легален
- род находки — закрытый словарь из двух слов, и он объявлен ОДИН раз
- наряд несёт ровно три документа и приказ отвечать строками, без пересказа
## Done when
- `node --test` зелёный; разбор воспроизводит находки эксперимента дословно


## Сделано (19.08.2026)
`steps/planreview/` — новый срез: роль `critic.md` (судит ПЛАН, находка несёт номер требования и род),
`planreview.mjs::findingsOf` и закрытый словарь `KINDS` из двух слов.

Форма находки — четыре поля через `|`, а не JSON: слабая модель теряет скобки и кавычки, разделитель
держит. Проверено экспериментом: два вызова, восемь строк, ни одной битой.
---
id: L2
key: IZI-LOOP
kind: module
status: done
wave: 3
blocked_by: [L1]
inputs:  [steps/planreview/planreview.mjs, steps/planreview/planreview.test.mjs]
outputs: [steps/planreview/planreview.mjs, steps/planreview/planreview.test.mjs]
verify: node --test
---
## Goal
Маршрут находки — чистая функция над ФАКТАМИ артефактов, не над прозой:

```
адрес — раздел, который в PLAN.md ЕСТЬ   → "plan"    фиксер правит план
адрес — модуль, которого в плане НЕТ     → "design"  пересборка 7-9: у нового раздела нет места
                                                     ни в дизайн-графе, ни в plan-index.json
род «НЕ НАПИСАНО»                        → "frd"     фиксер правит FRD, затем пересборка
```
## What you must prove
- по случаю на каждую ветку, на настоящих находках эксперимента
- тотальность: пустая находка и незнакомый род дают самый дорогой маршрут, а не самый дешёвый
## Done when
- `node --test` зелёный


## Сделано (19.08.2026)
`routeOf(finding, { plan })` — три маршрута по факту, а не по прозе: адрес есть в `PLAN.md` → `plan`;
адреса нет → `design` (у дописанного патчем раздела не будет ни места в дизайн-графе, ни строки в
`plan-index.json`); род «НЕ НАПИСАНО» → `frd`.

Незнакомая находка едет в САМЫЙ ДОРОГОЙ маршрут. Швы (сняты обратно → красный): маршрут не смотрит,
есть ли раздел · род «НЕ НАПИСАНО» не уводит в FRD.
---
id: L3
key: IZI-LOOP
kind: module
status: done
wave: 4
blocked_by: [L2]
inputs:  [steps/planreview/plan-fixer.md, steps/planreview/order.fix.tpl, steps/planreview/planreview.mjs, steps/planreview/planreview.test.mjs, ext/index.mjs]
outputs: [steps/planreview/plan-fixer.md, steps/planreview/order.fix.tpl, steps/planreview/planreview.mjs, steps/planreview/planreview.test.mjs, ext/index.mjs]
verify: node --test
---
## Goal
Фиксер правит ПО ЯКОРЮ, а не переписывает файл: `ЗАМЕНИТЬ <строка>` и `ДОБАВИТЬ ПОСЛЕ <строка>`.
Применяет правку МАШИНА: якоря нет в файле — отказ, а не молчаливая вставка не туда.

Улика эксперимента: фиксер закрыл четыре находки за два круга, файлы остались валидными, и его
единственный промах (`F3c` — дельта без сценария) поймал наш же гардрейл.
## What you must prove
- `applyPatch({ text, patch })` — чистая: заменяет по точному якорю, отказывает на ненайденном,
  не трогает ничего кроме названного места
- после правки гардрейл обязателен: правили FRD → `checkFrd` · правили план → сухая нарезка 10б
- фиксер не создаёт разделов: новый модуль — это маршрут "design", а не правка
## Done when
- `node --test` зелёный; проигрыш применяет патчи эксперимента и даёт зелёный полный суд


## Сделано (19.08.2026)
`applyPatch({ text, patch })` + хост-функции `planReview`, `planFix`, `planRoute`. Правка по якорю:
`ЗАМЕНИТЬ:` и `ДОБАВИТЬ ПОСЛЕ:`, применяет машина, якоря нет — ОТКАЗ без записи.

Роль `fixer.md` прямо запрещает создавать разделы: модуль, которого нет в плане, — чужой маршрут.

Швы: промах якорем проглатывается молча · файл тронут при отказе.
---
id: L4
key: IZI-LOOP
kind: module
status: done
wave: 5
blocked_by: [L1, L2, L3]
inputs:  [workflows/izi.js, ext/index.mjs, ext/index.test.mjs]
outputs: [workflows/izi.js, ext/index.mjs, ext/index.test.mjs]
verify: node --test
---
## Goal
Шаг 10в в полосе: критик плана → маршрут → правка → критик, бюджет 2 круга. Включается ДВАЖДЫ —
перед гейтом 1 сам по себе и после гейта со словами оператора на входе критика. Прежняя ветка
«rework всегда откатывает на шаг 6» снимается: полный откат остаётся крайним случаем маршрута.
## What you must prove
- чистый план проходит луп без единого вызова роли
- находка рода "plan" чинится фиксером и не трогает шаги 7-9
- находка рода "design" уводит в пересборку, "frd" — в правку FRD с последующей пересборкой
- слова оператора после гейта доезжают до критика ВХОДОМ, а не вторым разборщиком прозы
- бюджет исчерпан — полоса встаёт с последними находками, а не идёт к гейту молча
## Done when
- `node --test` зелёный; срез полосы исполняется на стенде


## Сделано (19.08.2026)
`planLoop(note)` в полосе, шаг 10в, бюджет два круга. Включается ДВАЖДЫ: перед гейтом сам по себе и
после гейта — со словами оператора на входе критика. Старая ветка «rework всегда откатывает на шаг 6»
снята: полный откат стал одним из трёх маршрутов, а не единственным ответом.

Найдено и починено по дороге, чего никто не видел: **`workflows/izi.js` был синтаксически сломан** —
правка `answeredBlock` записала в `.join("…")` настоящий перевод строки вместо `\n`. 568 тестов
оставались зелёными, потому что файл целиком никто не разбирал; полоса умерла бы на загрузке. Заведён
шов «полоса — валидный JavaScript, а не набор строк для регулярок».

Швы: род `frd` не уводит на шаг 6 · слова оператора не доезжают до критика · после правки нет
гардрейла · сырой перенос в строке.
---
id: L6
key: IZI-LOOP
kind: run
status: todo
wave: 6
blocked_by: [L0, L1, L2, L3, L4, L5]
inputs:  [sandbox/runbox/eddi, standards/live-run.md]
outputs: []
verify: живой прогон + node --test
---
## Goal
Прогон с ГОТОВОГО плана: лестница входит в 10в, шаги 1-9 не переигрываются, рой не зовётся.
Точка отсчёта — прогон 19.08.2026: план из 11 модулей, гейт забракован оператором, три требования
потеряны планом и два не написаны в FRD.
## What you must prove
- луп закрывает `resourceUri`, `glossaryRefs` и отказ 422
- гейт видит план БЕЗ находок критика
- замер приложен: вызовов роли, кругов, что закрыл фиксер, что ушло в пересборку
## Done when
- замер в наряде; план дошёл до гейта чистым

---
id: N1
key: IZI-RAISE
kind: module
status: done
wave: 1
blocked_by: []
inputs:  [steps/plan/raises.mjs, steps/plan/raises.test.mjs, ext/index.mjs, ext/index.test.mjs]
outputs: [steps/plan/raises.mjs, steps/plan/raises.test.mjs, ext/index.mjs, ext/index.test.mjs]
verify: node --test
---
## Goal
Отказ, поднятый цепочкой, ДОПИСЫВАЕТ СБОРЩИК плана, а не вспоминает роль. `planbook` уже склеивает
карточки в `PLAN.md`; рядом лежит `.agent/data-flow.md`, развёрнутый тем же шагом. Поднявший
вычисляется точно: шаг цепочки, где код стоит СПРАВА и не стоит слева, — остальные его пронесли.

Улика: в цепочках прогона 19.08.2026 стоят `400 TERM_KEY_INVALID`, `404 GLOSSARY_NOT_FOUND`,
`422 GLOSSARY_DELETED`. В `PLAN.md` этих кодов НОЛЬ. Шаг 14 режет тикеты из разделов плана, значит
исполнитель получит наряд без единого отказа, напишет happy path, и `verify` тикета сойдётся.
## What you must prove
- `raisesOf({ flow })` различает ПОДНЯВШЕГО и пронёсшего; модуль без кодов не получает блока
- блок помечен как ПРОИЗВОДНЫЙ и несёт ссылку на цепочку и шаг — иначе следующий читатель решит,
  что это обещала роль
- проигрыш по сохранённым артефактам: три кода доезжают до разделов тех модулей, что их поднимают
- план после дописи по-прежнему режется (сухая нарезка 10б)
## Done when
- `node --test` зелёный; пересобранный PLAN.md несёт три блока `raises:`

## Proof
Пересборка PLAN.md по сохранённым карточкам и цепочкам прогона 19.08.2026: 11 разделов, 2 блока
`raises:`, три кода (TERM_KEY_INVALID, GLOSSARY_NOT_FOUND, GLOSSARY_DELETED) доехали до разделов
поднявших модулей. Швы доказаны обратно: пронёсшего засчитали поднявшим — красно.
---
id: N2
key: IZI-RAISE
kind: module
status: done
wave: 2
blocked_by: [N1]
inputs:  [workflows/izi.js, steps/planreview/order.fix.tpl, ext/index.mjs, ext/index.test.mjs]
outputs: [workflows/izi.js, steps/planreview/order.fix.tpl, ext/index.mjs, ext/index.test.mjs]
verify: node --test
---
## Goal
Величины расставляет МОДЕЛЬ, один вызов после сборки плана. У `<nfr>` нет адреса, и вычислить его
нечем: связь «glossary-cache-ttl → GlossaryService» видна только по смыслу цепочки. Поэтому роли
дают величины и `data-flow.md`, а она находит модуль и пишет правку по якорю.

Роль прежняя — `plan-fixer`; наряд прежний — `order.fix.tpl`. Новых ролей не заводим.
## What you must prove
- в FEEDBACK едут строки `nfr:` со своим источником, наряд объясняет, что с ними делать
- `data-flow.md` едет документом: по нему роль ищет модуль, через который проходит величина
- правку применяет МАШИНА по якорю; якоря нет — отказ, файл не тронут
- гардрейл после: план режется И величина доехала (`measuresOf(fit)` встречается в плане);
  твёрдых знаков у величины нет — правило молчит
- лупа нет: один вызов, остальное подберёт критик плана, который стоит следом
## Done when
- `node --test` зелёный; на плане прогона величина `5 minutes` появляется в разделе GlossaryService

## Proof
Живой вызов фиксера (qwen3.6-27b) на плане того же прогона: наряд 31113 символов, ответ
`INSERT AFTER:` по якорю в разделе GlossaryService, `applyPatch` применил, `5 minutes` в плане.
Знаки величины считает `measuresOf`, не `hardTokens`: `hardTokens("5 minutes") === []` и правило
молчало бы о плане, где величины нет. Шов доказан обратно: величина по голому числу — красно.
---
id: N3
key: IZI-RAISE
kind: run
status: done
wave: 3
blocked_by: [N1, N2]
inputs:  [sandbox/runbox/eddi]
outputs: []
verify: живой прогон + node --test
---
## Goal
Проигрыш по сохранённым артефактам, затем живой прогон с шага 9.
## What you must prove
- пересборка PLAN.md из тех же карточек и цепочек даёт три блока `raises:`
- вызов фиксера по N2 ставит Caffeine с TTL в раздел GlossaryService
- живой прогон доходит до гейта с планом, несущим отказы и величины
## Done when
- замер приложен: кодов доехало, величин доехало, вызовов роли потрачено

## Proof
Проигрыш по снимку `prev-runs/eddi-gate1-rejected-plan` через ХОСТ-функции, 20.08.2026 (живой
прогон с шага 9 — отдельно, по команде оператора). Роль — qwen3.6-27b, 9 вызовов: критик 3, фиксер 6.

    planbook              11 разделов, 7 use case → 2 блока raises, 3 кода отказов
    величины              не хватало 1 (`5 minutes`) → 1 вызов → в разделе GlossaryService
    критик круг 1         2 находки (обе в план) → 1 вызов → легли; ВЕЛИЧИНА СТЁРТА правкой
    гардрейл круга        величина возвращена 1 вызовом (дефект найден и починен здесь же)
    критик круг 2         4 находки → 3 план, 1 требование; требование чинится первым
    фиксер FRD            заход 1 → F3c (дельта без сценария); заход 2 по блокерам → ЗЕЛЕНО
    критик круг 3         1 находка → маршрут «дизайн»: модуля в плане нет
    шаг 9 после правки    модулей 11 → 12; planbook ОТКАЗЫВАЕТ, пока раздела нет
    нарезка               11 нарядов, волны 1 · 3 · 5 · 2 — после каждой правки

Найдено и починено проигрышем (каждое со швом и обратным доказательством):
1. величина — инвариант плана, а не разовая фаза (`placeValues` после каждой правки круга);
2. красный полный суд — инструкция: блокеры едут фиксеру вторым заходом (`FRD_TRIES`), а не
   перемоткой на шаг 6 ценой десяти вызовов;
3. F13 засчитывал знак ответа в `<!-- комментарии -->` — комментарий вырезан до поиска;
4. наряд фиксера прямо запрещает закрывать находку комментарием.
