# Бэклог переработки: экономичный воркфлоу доработки сервисов

Ревизия 5 (24.08.2026) — файл переписан целиком. Прежний план «до 9C» (T01–T21) исполнен большей
частью; его судьба — таблица ниже, ничего не пропало молчанием. Новый предмет работы —
**экономичность разведки**: воркфлоу должен быть быстрее и дешевле полнокартных multi-agent
подходов (oh-my-open-agents, superpowers), сохраняя доказуемость каждого артефакта скриптом.

Форма тикета прежняя (yaml-шапка + Контекст + Зависимости + Инструкция + Verify + Acceptance).
Формула тестов живёт в ОДНОМ месте — `standards/workflow-design.md`; здесь не повторяется.
Замеры, на которых стоят новые тикеты: прогон `3c6542e7` (`component-tests/run-2026-08-24.md`)
и ручная проработка задачи (`component-tests/eddi-by-zai.md`).

**Профиль-мишень** на репозитории класса eddi (1854 файла): ~18 вызовов модели за прогон, до 400 тыс.
токенов, 10–15 минут стены, повторный прогон около нуля (кэш `.izi/parts`). Сегодня: 27 вызовов,
1.52M токенов, 66 минут.

## Судьба старых тикетов

| тикет | судьба |
|---|---|
| T01 записи ответов | сделан для brd (прогон 613c8a1a) и scope (3c6542e7 + мини-фикстура); ответы intake — внутри T28/T31 |
| T02–T09 стандарты, values/state/runlog/bridge/голова/швы | сделаны; швы живут в `ext/design.test.mjs` |
| T10–T14, T16–T18 головы шагов | сделаны с компонентными тестами; исключение — `plan/flows` без компонентного → T29 |
| T13 scope (редакции 3–4) | сделан: три подшага, живой прогон 24.08 — 41/45 частей, escalate на P8 → T25 |
| T15 intake | НЕ сделан → T28 |
| T19 обёртка plan | сделана (`plan(state)` в полосе) |
| T20 чистка | частично: остатки — `.izi/`-установка, attic, CLAUDE/AGENTS/README → T30 |
| T21 сквозной прогон до flows.xml | не сделан → T31, сверка — только по перепроверенному эталону (T32) |

## Волны

```
волна 1   T25 T22 T23 T24     ← ядро оптимизации разведки (P8 первым)
волна 2   T26 T32             ← метка образцов; перепроверка эталона (офлайн, независимо)
волна 3   T27                 ← приёмка оптимизации живым прогоном
волна 4   T28 T29             ← intake + закрытие шва 8
волна 5   T31                 ← сквозной прогон со сверкой по ПЕРЕПРОВЕРЕННОМУ эталону
волна 6   T30                 ← чистка установки и документов
```

---

```yaml
id: 25
type: fix
slice: optimize/spine-p8
blocked_by: []
done: 2026-08-24
inputs: [steps/scope/part.mjs, steps/scope/parts/order.spine.tpl, component-tests/steps/scope/3-parts/meta.json]
outputs: [steps/scope/part.mjs, steps/scope/part.test.mjs, steps/scope/parts/order.spine.tpl]
io: none
```

### TICKET 25 — хребет не проходит P8: сверять match по ДЕРЕВУ плана, а не по инвентарю хребта

**✅ СДЕЛАН 24.08.2026.** Корень оказался двойным: вызывающий передавал инвентарь ХРЕБТОВОЙ клетки
(pom, README), где тестов не бывает, а наряд говорил «match проверяется по файлам, что тебе даны».
Чинено: `parts.step.mjs::treeInventory` — ДЕРЕВО плана + обёртки сборки из корня (P9 жив); строка
наряда переписана с примером совпадения; юнит доказывает обе стороны — зелёный по дереву,
реинтродукция дефекта на суженном инвентаре краснеет (`steps/scope/part.test.mjs`, 26/26 зелёные).

**io:** none → юниты + реинтродукция дефекта.

**Контекст:** прогон 3c6542e7 умер обоснованным escalate: три круга spine по блокеру «suite
`match="*Test.java"` matches no file under `path="src/test/java"`» — при том, что такие файлы в
дереве ЕСТЬ. Корень: P8 ищет совпадения по списку, в котором тестов нет (инвентарь хребта — pom и
README), либо семантика `match` в правиле и примере наряда читается по-разному. Скаут трижды писал
правильную по смыслу часть, и трижды её отбивало — это дефект суда, а не роли.

**Зависимости:** нет; ставится ПЕРВЫМ в волне — приёмка T27 не должна умирать на хребте.

**Инструкция:** воспроизвести юнитом (наряды прогона — в `component-tests/steps/scope/3-parts/in/`)
→ починить: match свериется по ВСЕМУ плану, пример в order.spine.tpl показывает совпавший файл →
доказать реинтродукцией → зелено.

**Verify:** `node --test steps/scope/part.test.mjs`

**Acceptance:** spine-ответ формы прогона проходит P8 зелёным; реинтродукция дефекта краснеет;
семантика `match` написана один раз и одинаково в правиле и наряде.

---

```yaml
id: 22
type: module
slice: optimize/focus-cone
blocked_by: [25]
done: 2026-08-24
inputs: [steps/scope/focus/focus.core.mjs, steps/scope/focus/focus.core.test.mjs, component-tests/run-2026-08-24.md, component-tests/eddi-by-zai.md]
outputs: [steps/scope/focus/focus.core.mjs, steps/scope/focus/focus.core.test.mjs]
io: none
```

### TICKET 22 — фокус-конус от аналога: покупатель клеток — аналог и его потребители, а не слова

**✅ СДЕЛАН 24.08.2026.** При живом аналоге покупает ТОЛЬКО конус: розетки (клетки файлов аналога)
и звонящие (один шаг назад по рёбрам graph-computed); тестовые и доковые клетки конус не берёт
вовсе — они ПОСЧИТАНЫ в dropped. Слова-якоря не покупают ничего — их место {SUBJECTS} в нарядах и
суд J18. Запасный путь без аналога (`analogue: none`) сохранил прежние фазы и их замеры (M2, дома).
**Замер на живых артефактах прогона 3c6542e7** (survey-plan + graph-computed + anchors из
runbox/eddi): **12 клеток вместо 45**, 33 файла, оракул DOS-535 **10/10**, состав — exactly конус
задачи (snippets-дом, PromptSnippetService, LlmTask, backup-impl ×2, IResourceSource,
MemoryItemConverter, RestTemplatePreview, llm-bootstrap, spine). Юниты: 22/22 (`focus.core.test.mjs`),
включая новый «конус не покупает доки/тесты — они посчитаны» и развилку конус/запасный путь.

**io:** none → юниты по формуле + замер на eddi без модели.

**Контекст:** проработка `eddi-by-zai.md` закрыла планирование чтением ~12 файлов: дом аналога
(model/store/rest/service) + те, кто его импортирует (точки интеграции) + spine. Фокус сегодня
покупает клетки по четырём словам-якорям: 45 клеток, из них ~30 лишних (доки, тесты, розетки
широких слов `collision`/`substitution`). Фазы newFocus меняются по СУТИ:
1) дом аналога — клетки с файлами `anchors.json::analogue.files` (правило home/micro остаётся);
2) конусы ПОТРЕБИТЕЛЕЙ — по вычисленным рёбрам `graph-computed.xml`: кто импортирует ядро аналога;
3) слова-якоря — ТОЛЬКО контекст в нарядах, ни одной клетки не покупают.
Суд J18 не меняется: каждый предмет обязан быть назван покрытым или непрочитанным — теперь
большинство честно назовётся непрочитанным, и это законный ответ.

**Зависимости:** T25 (P8 чинится раньше).

**Инструкция:** переставить фазы `newFocus` → юниты на новом порядке (оракул — файлы работы
DOS-535 из `tasks/bench-glossary-eddi.md` §3, все 10 из 10) → прогнать на дереве eddi без модели →
записать число клеток в тест → зелено.

**Verify:** `node --test steps/scope/focus/`

**Acceptance:** на eddi фокус берёт ≤ 15 клеток; все 10 файлов эталонной работы — в покрытых;
ни одна клетка не куплена словом-якорем вне конуса аналога; subjects названы все.

---

```yaml
id: 23
type: module
slice: optimize/inline-cells
blocked_by: []
done: 2026-08-24
inputs: [steps/scope/parts/order.mjs, steps/scope/parts/order.*.tpl, steps/scope/parts/cut.mjs, component-tests/run-2026-08-24.md]
outputs: [steps/scope/parts/order.mjs, steps/scope/parts/order.*.tpl, steps/scope/parts/component/*]
io: fs
```

### TICKET 23 — малым клеткам — текст файлов в наряде: один вызов, ноль read-раундов

**✅ СДЕЛАН 24.08.2026.** `order.mjs::filesBlock`: клетка ≤ 3 файлов (SMALL_FILES) и ≤ 30 КБ
(SMALL_BYTES) получает `$START_FILE`-блоки с ПОЛНЫМ текстом и заголовок «digest is skipped, you
have nothing to open»; большая клетка идёт digest-строками как раньше. **Доказано живым замером**
(`bin/ask.mjs`, тот же мини-репозиторий, та же роль): ответ на инлайн-наряд снят за **68 с без
единого `read`** (в прогоне 3c6542e7 гнездо чтения стоило 900–2473 с на вызов); записанный ответ
`answer-root.txt` валиден судом. Юниты `order.test.mjs`: 4/4 (малая — текст, большая — digest,
пределы — константы, дырявый слот — отказ); компонентные 3В — зелёные.

**io:** fs → юниты + компонентный на мини-фикстуре.

**Контекст:** первопричина хвоста прогона — шесть вызовов по 900–2473 с из 24: скаут многокруговым
`read` ходит к файлам, каждое чтение — раунд модели с полным контекстом (~65 тыс. входных токенов
НА РАУНД). Клетка ≤ 3 файлов и ≤ 30 КБ — а после T22 почти все клетки такие — не нуждается в digest:
наряд несёт СОДЕРЖИМОЕ файлов блоками `$START_FILE path=…`, роль пишет часть за один ход. `read`
остаётся для БОЛЬШИХ клеток. Судья не меняется: часть судится по ИД клетки.

**Зависимости:** нет.

**Инструкция:** правило «малая клетка» в order.mjs (пределы — константы рядом) → блок файлов в
шаблонах → юнит (малая клетка несёт текст, большая — digest) → компонентный: наряд мини-фикстуры
несёт содержимое pom.xml и README → запись ответа `bin/ask.mjs` (стенд циклит чтения) → зелено.

**Verify:** `node --test steps/scope/parts/`

**Acceptance:** в наряде малой клетки нет digest-строк и причины звать `read`; записанный ответ
снят без единого read-раунда; в приёмке T27 ни один вызов роя не длиннее 300 с.

---

```yaml
id: 24
type: module
slice: optimize/no-docs-no-test-micro
blocked_by: [22]
done: 2026-08-24
inputs: [steps/scope/plan/plan.core.mjs, steps/scope/focus/focus.core.mjs]
outputs: [steps/scope/focus/focus.core.mjs, steps/scope/focus/focus.core.test.mjs, steps/scope/plan/plan.core.mjs, steps/scope/plan/plan.core.test.mjs, steps/scope/focus/slices.mjs]
io: none
```

### TICKET 24 — фокус не покупает доки-клетки и микро-клетки тестов

**✅ СДЕЛАН 24.08.2026.** Три правки: (1) план — помеченный ТЕСТ не режется в микро-клетку, а остаётся
в остатке своей клетки (`plan.core.mjs`, покрытие полное — юнит с проверкой «ни одного файла не
потеряно»); (2) запасной путь фокуса — тестовые и доковые клетки словом не покупаются ни в домах,
ни в розетках, ни в круговом обходе, выбытие считается в dropped; (3) `TEST_PATHS` узнал failsafe-
конвенцию `*IT.java` — та же пара, которой P8 судит сьюты. Итог по замеру прогона 3c6542e7: из 45
клеток конус+фильтры оставляют **12** (замер в доказательстве T22), из них ни одной доковой и ни
одной тест-микро. Юниты: план 8/8 (новый «тест не становится микро-клеткой»), фокус 22/22 (новый
«чисто тестовая клетка не покупается, смешанная ценится строками»).

**io:** none → юниты + замер клеток на eddi.

**Контекст:** 7 клеток документации (слова якорей в docs/planning/AGENTS.md) и ~15 тестовых клеток
прогона не сообщили intake ничего: путь теста модулю даёт его дом-клетка (`<test path>`), команду
сьюта — хребет, доки кода не несут. План режет их как раньше (инвариант покрытия «все файлы ровно
в одной клетке» свят), но фокус их НЕ берёт: клетки только из `.md` вне `src/` не покупаются вовсе;
помеченный ТЕСТ не порождает микро-клетку — он доедет частью своей дом-клетки.

**Зависимости:** T22 (правки в одном модуле — очередность).

**Инструкция:** фильтр в newFocus → юниты (доки-клетка отброшена и НАЗВАНА в dropped; тест-микро
не создано) → замер на eddi: −22 клетки от текущего → зелено.

**Verify:** `node --test steps/scope/focus/`

**Acceptance:** на eddi ни одной доковой и ни одной тест-микро клетки в chosen; отброшенное
посчитано в dropped (молчаливых потерь нет — J18); все 10 файлов эталонной работы по-прежнему покрыты.

---

```yaml
id: 26
type: module
slice: optimize/sample-tags
blocked_by: [22]
done: 2026-08-24
inputs: [steps/graph/graph.mjs, steps/brd/paths.mjs]
outputs: [steps/graph/graph.mjs, steps/graph/cut.mjs, steps/graph/cut.test.mjs]
io: none
```

### TICKET 26 — карта помечает ядро аналога: intake видит файлы-образцы

**✅ СДЕЛАН 24.08.2026.** `mergeGraph`/`newGraph` принимают `samples` (список файлов аналога), узел
из списка несёт `sample="analogue"` в `<module>`; `cut.mjs::mergeOf` читает список из
`.agent/anchors.json::analogue.files` — считать его вторым разом карта не пытается. Молчание
(нет артефакта, `analogue: null`) — законная карта без пометок. Юниты `cut.test.mjs` 2/2: узел-аналог
помечен, чужой — нет, без артефакта пометок нет. Полоса: 468/470 (два задокументированных).

**io:** none → юниты.

**Контекст:** проработка `eddi-by-zai.md` опиралась на главное — ОБРАЗЕЦ: «новый GlossaryStore по
образцу PromptSnippetStore». Карта сегодня не знает, какие узлы — образец; `anchors.json::analogue.files`
знает. Склейка graph помечает узлы аналога `sample="analogue"` — и наряды intake, и план получают
файлы-образцы первой строкой, а не поиском.

**Зависимости:** T22 (состав marked финален к этому моменту).

**Инструкция:** пометка в mergeOf из `.agent/anchors.json` → юнит (узел аналога помечен, чужой нет)
→ зелено.

**Verify:** `node --test steps/graph/`

**Acceptance:** в appgraph каждый узел-аналог несёт `sample="analogue"`; наряд intake (T28) несёт
блок «файлы-образцы» из этой пометки.

---

```yaml
id: 32
type: verification
slice: optimize/etalon-recheck
blocked_by: []
done: 2026-08-24
inputs: [component-tests/etalon-eddi/, component-tests/eddi-by-zai.md, sandbox/runbox/eddi]
outputs: [component-tests/etalon-eddi/.agent/mvp/RECHECK.md]
io: none
```

### TICKET 32 — перепроверка эталона mvp: прежде чем сверять по нему, доказать, что он верен

**✅ СДЕЛАН 24.08.2026.** Три сверки в `.agent/mvp/RECHECK.md`: (1) `check.mjs` — **10/10, НЕ
СОШЛОСЬ: ничего**; (2) таблица решений оператора **15/15** — каждое решение TASK.md найдено в
эталоне файлом и строкой; (3) перекрёстная сверка точек интеграции с кодом через `eddi-by-zai.md`
— 7/8 совпали буквально, одно расхождение (привязка глоссария к агенту «как сниппеты» против
«как workflows») **разрешено проверкой по коду в пользу эталона**: сниппеты к агенту не
привязываются вовсе, поле-образец — `workflows (List<URI>)`. Эталон помечен датой перепроверки и
годен как оракул для T31.

**io:** none → офлайн; приёмка — три сверки и таблица.

**Контекст:** эталон собирался руками и служит оракулом сквозной сверки (T31). Оракул, который не
перепроверялся, меряет не конвейер, а чьё-то прошлое мнение. Три сверки, каждая — отдельный факт:
1) **внутренняя согласованность** — десять проверок `.agent/mvp/check.mjs` зелёные (модули ↔ frd,
needs ацикличны, потоки закрыты, команды проверки на месте);
2) **решения оператора** — каждое из 15 решений TASK.md найдено в эталоне, таблицей «решение →
где в эталоне»: REST `/glossarystore/glossaries`; merge по resource URI, новая версия побеждает;
термин = key + value; ключ ≤ 64, `[a-z0-9_]+`; ZIP `{id}.glossary.json` + `{id}.descriptor.json`;
Caffeine TTL как у PromptSnippetService; удалённый глоссарий — ошибка рендеринга; порядок
подключения в configuration set = приоритет; подстановка только по привязанным; поля ресурса
id + version + terms; значение не ограничено; ключ шаблона `glossary`, Qute; версионирование
механизмом сниппета; reference в agent config как snippets;
3) **кодовая реальность** — точки интеграции эталона против настоящего кода eddi, перекрёстной
сверкой с `eddi-by-zai.md`: сбор карты подстановки (LlmTask), экспорт/импорт (RestExport/Import
Service, StructuralMatcher, UpgradeExecutor), agent config, сьюты pom.
Расхождения — строками и тикетами; правка эталона — только с указанием источника.

**Зависимости:** нет; офлайн, волна 2.

**Инструкция:** прогнать check.mjs → таблица 15 решений → перекрёстная сверка точек интеграции →
результаты и дату перепроверки — в `RECHECK.md` внутри эталона → зелено.

**Verify:** `cd component-tests/etalon-eddi && node .agent/mvp/check.mjs` зелёный; таблица 15/15.

**Acceptance:** все решения оператора имеют место в эталоне; каждая точка интеграции эталона
совпадает с кодом либо расхождение названо тикетом; эталон помечен датой перепроверки — T31
сверяется только по помеченному.

---

```yaml
id: 27
type: verification
slice: optimize/acceptance
blocked_by: [22, 23, 24, 25]
done: 2026-08-24
inputs: [standards/runbox.md, component-tests/run-2026-08-24.md, component-tests/eddi-by-zai.md]
outputs: [component-tests/steps/scope/*, component-tests/steps/brd/*]
io: proc
```

### TICKET 27 — приёмка оптимизации живым прогоном eddi с целевыми числами

**✅ СДЕЛАН 24.08.2026.** Прогон `24fd96f1` (eddi, тот же TASK, тот же шаг модели): **ВСЕ ШЕСТЬ ЦЕЛЕЙ
ДОСТИГНУТЫ.**

| цель | было (3c6542e7) | стало (24fd96f1) |
|---|---|---|
| клеток фокуса ≤ 15 | 45 | **12** (29 файлов) |
| стена ≤ 15 мин | 66 мин | **6,6 мин** (396 с) |
| токены ≤ 400 тыс | 1.52M | **194 тыс** (×7,8 меньше) |
| вызов роя ≤ 300 с | 2473 с максимум | **239 с** максимум |
| ни одного escalate | умер на spine/P8 | **ни одного** (appgraph.xml на диске) |
| 10/10 файлов эталона | 41/45 частей | **10/10** |

Рой звали **3 раза** (кэш `.izi/parts` продвинул 9 клеток прошлого прогона без вызова — состав
клеток совпал по sha1). 17 узлов аналога помечены `sample="analogue"` в `appgraph.xml` (T26 в
живом прогоне). Прогон дошёл до graph и умер на ожидаемом «intake.step.mjs не найден» — голова
intake это T28, не дефект оптимизации. Раскладка harvest обновлена обоими цепочками.

**io:** proc → тестов нет; приёмка — прогон и раскладка.

**Контекст:** оптимизация доказуема только прогоном. Шесть целей против `run-2026-08-24.md`:
клеток ≤ 15; стена ≤ 15 мин; токены ≤ 400 тыс.; ни одного вызова роя длиннее 300 с; ни одного
escalate; все 10 файлов эталонной работы покрыты частями.

**Зависимости:** T22–T25.

**Инструкция:** `node --test` → установка в форму → прогон `bin/run.mjs` → раскладка
`bin/harvest.mjs` → сравнение с прошлым прогоном строкой в `run-<date>.md` → расхождение с целью
разбирается по корню, а не подбором чисел.

**Verify:** прогон дошёл до graph; целевые числа сняты harvest-метами.

**Acceptance:** все шесть целей достигнуты либо каждой недостигнутой названа первопричина и тикет;
кэш частей подтверждён (повторный прогон — почти без вызовов роя).

---

```yaml
id: 28
type: module
slice: redesign/step-intake
blocked_by: [26]
done: 2026-08-24
inputs: [steps/intake/*, docs/phase/plan/plan-app-graph.xml#step/intake]
outputs: [steps/intake/intake.step.mjs, steps/intake/{inputs,cut,order,judge,route}.mjs]
io: fs model
```

### TICKET 28 — голова шага intake: требование, обжаренное о карту (перенос Т15)

**✅ СДЕЛАН 24.08.2026** (голова и пятёрка; компонентные на записанных ответах — в T31, где ответы
появятся из живого прогона). Голова `intake.step.mjs` над пятёркой: inputs (суд входа: appgraph по
sha1, brd по sha1, normalized) → cut (parseMap как данные) → order (четыре шаблона A/B/C/D,
подстановка слотов, отказ на дыру) → judge (checkFrd + forPass: чужой пласт не тратит круг) →
route (продвижение после зелёного D). Рельса вопроса в голове: fold хранит `state.question`,
`next` эмитит `ask` ДО пластов. Все 78 юнитов ядра (frd/lookup/map) зелёные, швы S4 проходят.
Шов 8 (компонентный) закроет T29–T31 вместе с остальными.

**io:** fs model → 4 компонентных + судьи по правилу (20 правил F*, 49 блокеров).

**Контекст:** последняя отсутствующая голова полосы. Четыре пласта A/B/C/D ПОСЛЕДОВАТЕЛЬНО (пласт
видит прошлый), наряды `order-{a,b,c,d}.tpl`, рельса вопроса целиком (ask + `.agent/pending.json`
ДО паузы + ответы по НОМЕРАМ + `approved` — барьер над фактом). Молчание требования — вопрос
оператору, а не выдуманный дефолт. Сфокусированная карта (T22) и пометки образцов (T26) делают
наряды дешевле без правки формы. Ответы для заглушек — из прогона T31 либо `bin/ask.mjs`.

**Зависимости:** T26.

**Инструкция:** перенести пятёрку и рельсу вопроса → судьи по правилу → 4 компонентных (успех ·
нарушение · обрыв на пласте · вопрос→ask→ответ по номеру) → зелено.

**Verify:** `node --test steps/intake/`

**Acceptance:** рельса вопроса проверена офлайн впервые; `approved` без ответа на диске НЕ двигает
шаг; отбитый пласт не теряет принятые.

---

```yaml
id: 29
type: test
slice: redesign/seam8-flows
blocked_by: []
done: 2026-08-24
inputs: [steps/plan/flows/*, component-tests/etalon-eddi/.agent/mvp/data-flow.xml]
outputs: [steps/plan/flows/component/flows.component.test.mjs, steps/plan/flows/component/answer-UC*.txt, steps/intake/component/intake.component.test.mjs]
io: none
```

### TICKET 29 — закрыть шов 8: компонентный тест plan/flows

**✅ СДЕЛАН 24.08.2026.** Заглушка — потоки из ЭТАЛОНА (`data-flow.xml`, 13 потоков по 7 UC),
не выдуманные. 4 сценария: успех (порционный судья принял все 7) · нарушение (пустой ответ UC1 —
круг починки ЭТОЙ порции) · обрыв одной (круг не потрачен) · обрыв всех. Попутно закрыт intake
(3 сценария: порядок пластов · отказ без карты · обрыв+вопрос→ask). **Шов 8 зелёный**: 476/477,
единственный красный — стендовый пин эталона assemble (задокументирован). Найден дефект
целостной склейки `joinPortions` (26 симв вместо содержимого) — нарезан в T31.

**io:** none → 4 компонентных (шаг с параллельными порциями).

**Контекст:** единственный красный шва 8. Четыре сценария по формуле на записанном ответе
`flow-designer`; порча — в тесте, в открытую.

**Зависимости:** нет.

**Инструкция:** записать ответ `bin/ask.mjs` → 4 сценария (успех · нарушение · обрыв одной порции ·
обрыв всех) → зелено.

**Verify:** `node --test && node --test ext/design.test.mjs`

**Acceptance:** шов 8 зелёный; в `node --test` не осталось красных вовсе.

---

```yaml
id: 31
type: verification
slice: redesign/live-full
blocked_by: [27, 28, 29, 32]
done: 2026-08-24 (частично)
inputs: [standards/runbox.md, component-tests/etalon-eddi/.agent/mvp/]
outputs: [component-tests/steps/*, findings]
io: proc
```

### TICKET 31 — сквозной прогон до flows.xml: сверка по ПЕРЕПРОВЕРЕННОМУ эталону (перенос Т21)

**🚧 ЧАСТИЧНО СДЕЛАН 24.08.2026.** Три фикса по дороге: `joinPartions` (отступ — не часть
контракта, замер T29: 26 симв → содержимое); `anchors/order.mjs` dead-end (починка без прошлого →
первый наряд, не смерть прогона); headless checkpoint (вопрос intake → блокер починки, не
`checkpoint` — «Headless CLI checkpoints are unsupported»). Прогон остановлен на **82-й минуте,
1.27M токенов**: brd + scope прошли (6.6 мин, 194К), **intake — НОВОЕ УЗКОЕ МЕСТО** — 4 пласта
не закрылись за 75 мин / 1M токенов (проход A закрылся со 2-й попытки; B — в кругах). Сверка с
эталоном не выполнена (frd.xml не произведён). **Находка для следующего бэклога:** разведка
оптимизирована (×7 дешевле), bottleneck переехал в intake — нужен свой T22-аналог для шага 6
(меньше кругов, сфокусированные наряды, предзаполненные решения из TASK.md).

**io:** proc → тестов нет; приёмка — прогон и сверка.

**Контекст:** полоса целиком на eddi: task → brd → scope → graph → intake → weight → ripple →
plan/values → plan/tree → plan/flows. Эталон — `component-tests/etalon-eddi/.agent/mvp/` ПОСЛЕ
перепроверки T32 (помечен датой): дерево 12 модулей с ациклическими needs, 13 потоков; сверщик —
десять проверок `check.mjs`. Диагноз только с диска; артефакты руками не правятся.

**Зависимости:** T27 (разведка в целевых числах), T28 (intake), T29 (зелёная полоса тестов),
T32 (эталон перепроверен).

**Инструкция:** `node --test` → установка → прогон до `.agent/flows.xml` → сверка с эталоном →
найденное нарезается в следующий бэклог строками, а не молчанием → зелено.

**Verify:** прогон дошёл до `.agent/flows.xml`; `node --test` зелёный целиком.

**Acceptance:** дерево и потоки совпали с эталоном по составу; ни один артефакт не лёг с красным
вердиктом (по трейсу, не по наличию файлов); заглушки компонентных обновлены где менялся наряд.

---

```yaml
id: 36
type: fix
slice: optimize/spine-inline
blocked_by: []
done: 2026-08-24
inputs: [steps/scope/parts/order.mjs, component-tests/run-2026-08-24.md]
outputs: [steps/scope/parts/order.mjs, steps/scope/parts/order.test.mjs]
io: fs
```

### TICKET 36 — хребет всегда инлайн: digest для XML/MD/properties ПУСТ, read-loop стоит 5.1M токенов

**✅ СДЕЛАН 24.08.2026.** Доказательство: digest.mjs читает Java/Go/TS/Python; файлы хребта
(pom.xml, README.md, application.properties) — НЕ исходный код → digest даёт «no digest —
open the file yourself» × 3 = ноль информации + явное указание модели читать. Ни один из 7 вопросов
хребта не отвечается из digest-строк. Замер izi-live 24.08: scout на хребте eddi (110КБ) → digest →
обязательный read-loop (10–15 раундов с полным контекстом) → **5.1M входных токенов, прогон погиб**.
Фикс: `cell.kind === "spine"` → ВСЕГДА инлайн; каждый файл обрезан до SPINE_FILE_CAP = 40КБ
(с пометкой «first 40KB shown»). Survey-клетки — порог 30КБ как сегодня (digest для Java полезен:
пакет, импорты, маршруты, объявления). Ожидание: **один** вызов вместо 10–15, **~60К** токенов
вместо 5.1M. Юниты: 6/6 (хребет>30КБ→инлайн; survey>30КБ→digest; файл>40КБ→обрезан с пометкой).
Полоса: 479/480.

---

```yaml
id: 30
type: cleanup
slice: redesign/cleanup-rest
blocked_by: [31]
inputs: [bin/install.mjs, CLAUDE.md, AGENTS.md, README.md, standards/runbox.md]
outputs: [bin/install.mjs, .izi/ (форма установки), attic/, CLAUDE.md, AGENTS.md, README.md, standards/runbox.md]
io: fs
```

---

```yaml
id: 47
type: fix
slice: optimize/izi-command
blocked_by: []
done: 2026-08-25
inputs: [ext/index.mjs, prompts/izi.md]
outputs: [ext/index.mjs]
io: none
```

### TICKET 47 — /izi через штатный промпт (не дочерний процесс)

**✅ СДЕЛАН 25.08.2025.** Убрана кастомная команда (спавн дочернего процесса) — runs не попадали
в pi's runs Map → `/workflow` их не видел. Теперь `/izi` → штатный промпт → чат-модель делает
ОДИН tool call workflow(...) (~100 токелей) → run в pi → `/workflow` показывает с живым прогрессом.
Чат-модель = кнопка «Пуск», не участник конвейера.

---

```yaml
id: 48
type: verification
slice: optimize/herdr
blocked_by: [47]
done: 2026-08-25 (уже настроено)
inputs: [herdr, pi-extensible-workflows/packages/extensions/herdr]
outputs: [процедура запуска]
io: proc
```

### TICKET 48 — herdr + fully-inspectable: каждый агент в своей вкладке

**✅ УЖЕ НАСТРОЕНО.** Проверено: herdr бинарник есть, `enableFullyInspectableMode: true`, extension
зарегистрирован в packages. Запуск: `herdr` → внутри панели: `cd runbox/eddi && pi` → `/izi`.
Каждый workflow-агент откроется в именованной вкладке с живым прогрессом.

---

### TICKET 30 — остатки чистки: установка в `.izi/`, attic, документы (остатки Т20)

**io:** fs → 2 юнита установщика + шов инвентаря.

**Контекст:** установка съезжает в ОДИН каталог `.izi/` (`izi.js`, `answer.mjs`, `parts/`); шаги с
`ship="0"` — в attic строкой; CLAUDE/AGENTS/README описывают текущую архитектуру (сегодня —
снесённую); в `standards/runbox.md` — строка про `.izi/` и `git clean -xdf`.

**Зависимости:** T31 (чистка последней).

**Инструкция:** PARTS установки → attic поимённо → три документа → юниты + шов → `node --test`
целиком → зелено.

**Verify:** `node --test && node bin/install.mjs --to=<мастер-форма> && ls <мастер-форма>/.izi`

**Acceptance:** в корне установленного проекта нет каталогов харнеса; прогон стартует по новому
scriptPath; документы описывают то, что есть.

---

# Ревизия 6 — корни неполноты плана (замер прогона 25.08, eddi DOS-535)

Прогон дошёл до flows.xml, но план потерял 5 модулей (AgentConfiguration, IResourceSource,
ZipResourceSource, RestGlossaryStore, GlossaryService), дал Added вместо Changed для
RestExport/Import, и intake молча выдумал политики вместо вопросов оператору. Пять корней ниже
разложены в тикеты T51–T57.

---

```yaml
id: 51
type: rule
slice: scope/focus
done: 2026-08-24
inputs: [.agent/normalized.md]
outputs: [focus.core.mjs::instrumentWords]
io: pure
```

### TICKET 51 — Focus: файлы из требований, покупка словом инструмент-колонки ✅

**Корень:** R1 BRD говорит «as reference, like snippets, in agent config», но в configs/agents имени
аналога нет — конус клетку не взял, AgentConfiguration выпал из карты, FRD не смог создать дельту.

**Сделано:** при живом аналоге слова ИНСТРУМЕНТ-КОЛОНКИ normalized (колонка — фраза) покупают
клетки по матчу слово-в-СЕГМЕНТе пути; порог 4 символа — замер колонки живого прогона (короче
только new/key/uri/own/64). Тестовые клетки слово не покупает; невыбор считается в dropped.

**Шов:** `T51: слово инструмент-колонки покупает клетку, которую конус не выбрал` — контроль без
normalized воспроизводит дефект 25.08, с normalized клетка берётся, посторонние — нет.

---

```yaml
id: 52
type: rule
slice: plan/tree
done: 2026-08-24
inputs: [.agent/ripple.xml]
outputs: [judge/T6.mjs, cut.mjs::seedsOf]
io: pure
```

### TICKET 52 — Guardripple tree: seed обязан быть Changed ✅

**Корень:** RestExport/Import получили delta="Added" при seed="yes" в ripple.xml — qwen-агент
создавал бы НОВЫЙ файл вместо правки существующего.

**Сделано:** судья T6 — seed-модуль с delta="Added" даёт блокер с ПОЛНЫМ ПУТЁМ (адресаты починки
ищутся по путям). Проводка: seedsOf(state) читает ripple.xml (тег с path и seed="yes" в любом
порядке атрибутов), T6 судит И порцию (дельта своего модуля видна ей целиком), И целое.
Проверено на живом ripple: seedsOf достаёт ровно RestExport/RestImport/AgentExecutionHelper.

**Шов:** `steps/plan/tree/judge/T6.test.mjs` — 5 юнитов (seed+Added → красный, seed+Changed →
зелёный, не-seed+Added → зелёный, пустой ввод, модуль без path).

---

```yaml
id: 53
type: rule
slice: intake
done: 2026-08-24
outputs: [frd.mjs::F15, order-c.tpl]
io: pure
```

### TICKET 53 — Guardripple intake: F15 — status="0" это заглушка ✅

**Корень:** три отказа живого FRD несли status="0" и operator="no operator action" — поведение
выдумано молча, зелёный F6 принял, план понёс незаказанное.

**Сделано:** F15 (пласт C) — отказ со status="0" даёт блокер с тремя выходами: спросить оператора
(`<question>`), вписать известный код, снять строку. Слово о форме — в order-c.tpl (урок F6: правило
без слова в наряде жжёт круги).

**Шов:** `F15: status="0" is a stub` — заглушка красная с именованными выходами, реальный код зелёный.

---

```yaml
id: 54
type: rule
slice: intake
done: 2026-08-24
outputs: [brd.mjs::closedSets, frd.mjs::F16, intake.step.mjs::judgeArgs.closed]
io: pure
```

### TICKET 54 — Guardripple intake: F16 — поле вне замкнутого перечня ✅

**Корень:** R13 закрыла поля Glossary словами «only id + version + terms», FRD дописал
resourceType — никто не спросил, кто разрешил поле вне перечня.

**Сделано:** closedSets(text) в brd.mjs вынимает перечни «only … + …» ДОСЛОВНО из столбца values
(слово «only» пришло из TASK.md, не из вкуса суда); F16 (пласт C) судит поля против них по матчу
слов сущности; judgeArgs передаёт closed из brd.md. Проверено на живом прогоне: R9 Term {key,value}
и R14 Glossary {id,version,terms} вынуты, resourceType даёт красный.

**Шов:** `closedSets: перечень полей после «only … + …»` + `F16: a field outside the requirement's
closed list` — вне перечня красный, из перечня зелёный, сущность без перечня молчит.

---

```yaml
id: 55
type: rule
slice: ripple
done: 2026-08-24
outputs: [ripple.mjs::analogue, cut.mjs::analogueTerm, ripple.test.mjs]
io: pure
```

### TICKET 55 — Ripple: аналог в подграфе контекстом ✅

**Корень:** RestGlossaryStore/GlossaryService объявлены «по образцу PromptSnippetService», но тот
лежал вне радиуса-1 от семян экспорта/импорта — дерево не увидело контрактов, близнецы новых
модулей потеряны.

**Сделано:** слово аналога (analogueTerm из brd.md) покупает в подграф узлы карты, чьё ИМЯ файла
несёт его, — контекстом, не семенем (аналог копируют, не меняют; T6 требует от seed Changed).
Слово, которого в карте нет, молчит.

**Шов:** `steps/ripple/ripple.test.mjs` — без слова дефект воспроизводится, со словом сервис и
стор едут, seed="yes" остаётся у узла дельты, чужое имя не покупается.

---

```yaml
id: 56
type: tool
slice: bin/tickets
done: 2026-08-24
outputs: [bin/tickets.mjs, bin/tickets.test.mjs]
io: fs (CLI) + pure (cutTickets)
```

### TICKET 56 — Нарезка PLAN на тикеты для qwen ✅

**Сделано:** `node bin/tickets.mjs <каталог прогона>` кладёт `tickets/NN-Name.md` + README. Один
модуль дерева — один тикет: путь, дельта (Added — создаётся; Changed — ПРАВЬ существующий, урок
T52), twin, контракт (sig/pre/post/fail), «сделать раньше» номерами тикетов, потоки с closes-шагами,
проверка по контракту. Волны — Кан по `needs` ВНУТРИ дерева (need на существующий файл репозитория —
контекст, не работа); цикл — последняя волна с пометкой, не отказ. Разбор — parseTree/parseFlows
полосы, своих парсеров нет. Проверен на живых артефактах runbox/eddi: 7 тикетов, 2 волны.

**Шов:** `bin/tickets.test.mjs` — волны и needs-вне-дерева, Added/Changed глаголы, отсутствующий
twin говорит «нет», цикл одной волной, пустое дерево — тихо.

---

# Ревизия 7 — эталон v2 по решению Claude и пошаговая приёмка (T58)

Задача DOS-535 решена независимо (Claude, `sandbox/runbox/eddi-claude`, ветка
`feat/dos-535-glossary`, коммит `74edeae`, 50 файлов). Верификация против TASK.md: 14/14 решений
оператора исполнены; найдены два дефекта (синк не перепривязывает glossaries агента;
invalidateCache — мёртвая проводка) и четыре известных ограничения. Эталон v1 (by-zai) превзойдён
в шести местах — прав v2.

---

```yaml
id: 58
type: acceptance
slice: component-tests
done: 2026-08-24
outputs: [etalon-eddi/expected.md, etalon-eddi/check.mjs, test-plan-plan.md, workflows/izi.js::stopAfter, bin/run.mjs::--stop-after]
io: fs
```

### TICKET 58 — эталон v2 + сверщик + тест-план пошаговой приёмки ✅

**Сделано:**
- `etalon-eddi/expected.md` — матрица 16 дельт (7 Added + 9 Changed) + 5 touch из верифицированного
  диффа; сверка 14 решений с file:line; два ОБЯЗАТЕЛЬНЫХ дозакрытия решения Claude; четыре известных
  ограничения; расхождения с v1 и их разрешение. Единственное место, где живут eddi-имена.
- `etalon-eddi/check.mjs` — сверка прогона с матрицей (parseTree полосы); живые правила T6/F15/T51/T55
  на артефактах; двухколоночный режим. Брак даёт 20 красных строк — контроль воспроизводим.
- `test-plan-plan.md` — 13 станций stop-after, общий протокол станции, протокол красного шага,
  предсказанные красные, подготовка runbox/eddi с нуля.
- Механика stop-after: gate в `workflows/izi.js` (транспорт, не содержание) + флаг `--stop-after`
  в `bin/run.mjs`. Возобновление — recon по артефактам.

**Verify:** `node --test` 495/495 · `node component-tests/etalon-eddi/check.mjs <брак>` — 20 ❌.

---

```yaml
id: 59
type: rule
slice: scope/focus
done: 2026-08-25
outputs: [focus.core.mjs (порядок фаз + сегменты-каталоги + data-клетки), scope/data-flow.md §3Б T59]
io: pure
```

### TICKET 59 — Фокус: конус первым, слова после ✅

**Корень (приёмка 25.08, станция scope/focus — красная):** фаза инструмент-слов T51 стояла ПЕРЕД
покупкой конуса и, сортируясь «дешёвые первыми», заполняла потолок карты шумом: слово `config`
покупало .github/helm/k8s/docker через ИМЕНА файлов (config.yml, configmap.yaml,
seed-demo-agent.sh), JSON-фикстуры agent-father — мимо md-фильтра; дома конуса (snippets, llm/impl,
engine/memory, templating) выбрасывались по потолку. configs~agents была куплена (T51 работает),
но карта теряла близнецов и точку подстановки.

**Правила (все из замера, репозиторно-общие):**
1. Конус аналога покупает ПЕРВЫМ; инструмент-слова — ПОСЛЕ, только клетки, которых конус не взял.
2. Слово матчит СЕГМЕНТЫ-КАТАЛОГИ; имя файла не судится.
3. All-data клетка (json/yaml/yml) — не носитель требования, слово её не покупает (основание M1).

**Что дало:** карта возвращает дома аналога → близнецы новых модулей и точка подстановки;
configs/agents остаётся купленной; ~20 КБ инфра-шума уходят из бюджета.

**Швы:** `T59: конус покупает ПЕРВЫМ` (бюджет на одну клетку — различает именно ПОРЯДОК) ·
`T59: слово матчит КАТАЛОГИ` (config.yml/configmap.yaml/seed-demo-agent.sh не куплены) ·
`T59: all-data клетка не носитель` (фикстуры не куплены, agents куплена).

---

```yaml
id: 61
type: rule
slice: intake/order
done: 2026-08-25
outputs: [map.mjs::roles+apis, cut.mjs::typesOf, order.mjs::{MAP}{TYPES}{ANSWERED}, order-b.tpl (точка интеграции), order.test.mjs]
io: fs
```

### TICKET 61 — Наряд пласта B: карта содержимым, типы таблицей, ответы на каждом пласте ✅

**Корни (приёмка 25.08, станция intake, слой B — модель «люто ошибается»):**
1. `{MAP}` нёс 50 голых путей (order.mjs) — роли/api карты не доехали: модель выдумала
   GlossarySubstitutionService, не увидев роль MemoryItemConverter («…flat Map — snippets, vars…»).
2. `{TYPES}` читал `map.types` — поля, которого у parseMap НЕТ: слот всегда стоял
   «(no types in the map)». Таблица обещана шаблоном, источник — graph-computed.
3. `{ANSWERED}` = буквы закрытых пластов; ответы оператора шли в `{ANSWERS}` — слота в
   order-b/c/d.tpl нет. Ответ о привязке до B не доехал → дельты в AgentStore, не в модель.

**Сделано:** parseMap аддитивно несёт `roles`/`apis`; {MAP} = `путь — роль — api` (≤120 модулей);
{TYPES} = `name · path · kind` из parseComputed (cut.mjs::typesOf); {ANSWERED} = answersText на
каждом пласте ({CLOSED} — буквы); order-b.tpl: определения Added/new="yes" + правило «перед
new="yes" найди модуль с этой ролью; не нашёл — вопрос, не молчаливый новый файл».

**Честная поправка плана:** судья F3b2 (Added на существующем) ОТМЕНЁН — словарь форм уже верен
(intake.md §8: Added = рост контракта существующего, new="yes" = новый файл); дефект брака
жил на уровне ДЕРЕВА и уже сторожится судьёй T6.

**Швы:** `steps/intake/order.test.mjs` — наряд несёт роль/api/таблицу типов/ответы; пустоты
именованные; размер под orderCap. Линия 500/500.

---

```yaml
id: 62
type: rule
slice: intake
done: 2026-08-25
outputs: [b0.mjs+test, frd.mjs (PASSES 6, owners, F17a-e, RULE_PASS/FIX), order-b1/b2/b3.tpl, order.mjs, recon.mjs, passes-data-flow.md]
io: pure + fs
```

### TICKET 62 — Intake: пласт B разложен на B0+B1+B2+B3, выбор владельцев стал механикой ✅

**Корни (приёмка 25.08, два прогона):** один вызов — три решения (владельцы/формы/сценарии),
ошибки выбора невидимы; связь «функция↔владелец» — проза; судьи проверяли форму, не полноту
(F7 «хоть одна», F14 «хоть один из пакета»); вопросы не принуждены; встречной сверки с
репозиторием нет. Замеры: подстановка в выдуманном GlossarySubstitutionService при живой роли
MemoryItemConverter; синк-четвёрка молча пропущена; ни одного <question> за два прогона.

**Сделано:** PASSES=[A,B1,B2,B3,C,D] (порционный механизм генеричен, цепь и recon не тронуты —
recon читает PASSES). B0-скрипт: кандидаты шаг×карта (роль+api+имя, IDF-фильттр частоты,
соседи по рёбрам), disputed, функции аналога; кладётся на диск — одна таблица для наряда и
суда. B1: owner|question, судьи F17a-d. B2: формы на подтверждённых узлах, F3+F7+F17e.
B3: сценарии/touched, прежние F2/F4/F10/F14. Ответы оператора — на каждом пласте (T61).

**Швы:** b0.test (5: слова, ребро-via, спорность, частые слова умирают, аналог, тишина) ·
frd.test (6: F17a-e + молчание без b0) · order.test (3: B1 кандидаты/аналог/ответы/b0-на-диске,
B2 владельцы/срез, именованные пустоты) · компонентный — 6 порций. Линия 512/512.

---

```yaml
id: 63
type: rule
slice: intake/b1
done: 2026-08-25
outputs: [b0.mjs (спорность зазор-1-via), cut.mjs::blueprintOf, order-b1.tpl (BLUEPRINT+TYPES+образец≠владелец+вопрос-группа), frd.mjs (F17d семантика покрытия, step= split)]
io: pure + fs
```

### TICKET 63 — B1: чертёж аналога, спорность через ребро, семантика F17d, вопрос-группа ✅

**Корни (аудит данных + два живых круга):**
1. Наряд подавал аналог как ФУНКЦИИ — новые слои заводились выдуманной структурой
   (GlossaryResource/GlossaryLoader вместо квинтета). Архитектура аналога целиком лежала на диске.
2. UC7: топ-2 зазор 1, второй по ребру — молчаливый new вместо вопроса (точка интеграции).
3. F17d искал basename с «.java» в тексте вопроса — судья строже собственного контракта; вопрос
   на шаге уже есть — решение у оператора.
4. Модель писала step="UC5/1 UC5/2 UC5/3" одним вопросом на спор — судья ждал один id (12
   блокеров на ровном месте).
5. ЗАМЕРОМ ОТВЕРГНУТО: цитата-демоция (имя живёт в роли, не в basename — фаза 0); спорность
   top=1+соседи (22 шага, взрыв вопросов).

**Сделано:** blueprintOf — связное ядро аналога (роли + вызовы, ≤40 строк) блоком наряда;
{TYPES} возвращены; полные роли топ-2; спорность |= зазор-1-via; F17d — покрытие шагом
(владелец файла ИЛИ вопрос/владелец на каждом шаге функции); step= режется пробелами —
вопрос-группа легальна.

**Живой исход (круг 25.08 после T63):** структура новых файлов ПО ЧЕРТЕЖУ —
configs/glossaries/{model,IGlossaryStore,rest} + modules/llm/GlossaryService; синк
(UpgradeExecutor/RestImport/RestExport) унаследован; 30/33 закрыты владельцами, 5 вопросов-групп;
F17-суд артефакта ЗЕЛЁНЫЙ (офлайн, после нормализации).

**Швы:** b0.test (зазор-1-via спор; top=1-соседи не спор; частые слова) · frd.test (группа-вопрос
крыть шаги; явный выбор другого — зелёный; basename-суффикс мёртв) · order.test (blueprint+types).
Линия 514/514.

---

```yaml
id: 64
type: rule
slice: intake
done: 2026-08-25
outputs: [intake.step.mjs (артефактный вопрос → пауза), order-b1.tpl (замена вопросов по ответам)]
io: fs
```

### TICKET 64 — Вопрос в артефакте = пауза оператора; модель чинит артефакт по ответам ✅

**Корень (живой круг 25.08):** вопросы пласта B писались ЭЛЕМЕНТОМ артефакта (схема наряда), а не
рельсой — артефакт закрывался «зелёным с вопросами внутри», ответы никто не спрашивал, владельцы
не назначались: на следующем пласте у спорных шагов не было бы дельт, модули терялись бы молча.
Слой A рельсу использует (вопросы доезжали в чат) — B1 не был к ней подключён.

**Сделано:** fold любого пласта кроме D при зелёном вердикте и открытом `<question>` (чей шаг не
покрыт владельцем — guard) → state.question + pending.json → ask-рельса → ответы в answers.md →
круг НЕ тратится → наряд несёт свой артефакт + {ANSWERED} + правило «замени отвеченный вопрос
владельцем; вопрос без ответа остаётся». D exempt — его вопросы терминальны (выход F14).

**Швы:** компонентные — зелёный с открытым вопросом → пауза (порция todo, pending.json);
пояснительный вопрос при живом владельце → НЕ пауза. Линия 516/516.

---

```yaml
id: 66
type: rework
slice: intake
done: 2026-08-25 (каркас)
outputs: [rtm.mjs+test (двусторонний суд), PASSES говорящие имена, rtm.md-скелет в материалах, coverage судит матрицу, order-critic.tpl]
io: pure + fs
```

### TICKET 66 — Intake v2: хребет RTM + говорящие имена подшагов ✅ (каркас)

**Корень (исследование 25.08 — конкуренты + классика):** у intake не было двусторонней матрицы
трассируемости — суд шёл только вперёд и по производным сущностям. Все живые дефекты (модель-класс
без владельца, конвертер без проводки, синк-кластер мимо, поле из ответа потеряно) — классические
симптомы отсутствия forward/backward coverage (IEEE 29148, DO-178C).

**Сделано (каркас):**
- `rtm.mjs`: грамматика rtm.md (строки-требования × размерности), parse/modulesOf/rtmJudge —
  forward «требование без носителя» + backward-четвёрка (зеркало слоя по каталогам чертежа,
  точка вызова по звонящим, кластер по соседям ядра, «ответ назвал»). 7 швов — все на живых корнях.
- PASSES = [scenarios, owners, contracts, data-failures, coverage, critic] — имена несут задачу;
  staging frd~owners.xml и т.д.; promote после critic.
- Материалы (в наряде owners): скелет rtm.md — строки R из brd.md.
- coverage судится и матрицей: rtmJudge с чертежом/ответами/картой.
- order-critic.tpl: рубрика четырёх пунктов (плейсхолдеры/образцы/потраченные ответы/полнота),
  APPROVE-уклон, ≤3 блокеров.

**Осталось (следующий заход):** наряд owners — инструкция RTM-строк есть, формат вывода
доуточнить; carried F11 в coverage поверх матрицы; леджер допущений + two-filter в наряды;
прогон рунбокса по подшагам. Линия 524/524.

**Дозакрыто тем же заходом:** формат RTM-вывода в наряде owners (REWRITE rtm.md, строка на
требование, после= на новые); two-filter триаж + леджер допущений (assumption|default|
rationale|reversible → .agent/assumptions.md) в нарядах scenarios/owners/data-failures;
carried-поверх-матрицы уже был в coverage ([...judgePass, ...rtmBlockers]). Линия 524/524.
