{
  "id": "task",
  "kind": "human",
  "title": "оператор кладёт сырое требование",
  "in": {},
  "out": { "task": "TASK.md" },
  "check": { "cmd": "node steps/task/validate-task.mjs", "args": ["{{artifact}}"] },
  "//check": "≤ 300 строк. Больше — задача делится: одна задача = один вход. Предел объявлен в скрипте и проверяется им; роль его не пересказывает, иначе правило живёт в двух местах и расходится. {{artifact}} резолвит workflows/izi.js — у этого шага нет staging (kind=human — оператор пишет прямо в свой единственный out), поэтому резолвится в TASK.md.",
  "receipt": "task",
  "//receipt": "Контракт: шаг закрывает КВИТАНЦИЯ .agent/receipts/task.json (bin/receipt.mjs), а не факт существования TASK.md на диске — существование out больше ничего не значит (standards/protocol.md «Квитанция закрывает шаг»)."
}
