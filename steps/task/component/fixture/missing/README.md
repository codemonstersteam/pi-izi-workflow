Фикстура сценария 2 [no-task]: каталог прогона, в котором **нет `TASK.md`**.

Отсутствие файла и есть содержание этой фикстуры. Файл лежит здесь по одной причине: git не хранит
пустых каталогов, и после свежего клона фикстура исчезла бы, а тест упал бы словами «фикстуры
missing нет» — то есть на своём стенде, а не на шаге.

Шаг читает только `TASK.md`; этот README ему не виден.
