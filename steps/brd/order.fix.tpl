$START_TASK
Проверка вернула твой BRD. Находок: {COUNT}. Сделай ровно это и ничего больше:

{TASKLIST}

В квадратных скобках — АДРЕС правки: номер требования `R<N>` либо имя строки артефакта
(`subjects[]`, `analogue`, `open-questions`). Найди это место и поправь то, что названо после адреса.

ГЛАВНОЕ ПРАВИЛО ЭТОГО НАРЯДА: требование, к которому находок НЕТ, копируй БУКВА В БУКВУ — вместе с
его `fit:` и `verify:`. Остальное в файле проверка уже приняла; переписывая его заново, ты теряешь
принятое.
$END_TASK

$START_DATA

$START_WORK_DOCUMENT
path: {STAGING}
ЭТО ТВОЙ ФАЙЛ — тот, что ты написала в прошлый раз. Его и правь, целиком записывая обратно.
$START_WORK_CONTENT
{PREVIOUS}
$END_WORK_CONTENT
$END_WORK_DOCUMENT
{SOURCES}
$END_DATA

$START_CONSTRAINTS
- Правь только то, на что указала находка; остальные требования копируются буква в букву.
- Нумерация `R` не меняется: номер требования — его адрес, и он уже назван в находках.
- Новых требований не добавляй. Находка «нет способа проверки» просит `verify:`, а не ещё одно `R`.
$END_CONSTRAINTS

$START_OUTPUT
path: {STAGING}
check: {CHECK}
СДЕЛАЙ ЭТО СЕЙЧАС: пройди по находкам сверху вниз, поправь названные места и запиши файл целиком по
staging-пути инструментом `write`; только после этого вызови `workflow_result` по OUTPUT_FORMAT своей
ROLE.
$END_OUTPUT
