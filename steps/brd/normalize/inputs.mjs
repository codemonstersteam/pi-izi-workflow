// MODULE_CONTRACT: inputs — СУД ВХОДА подшага 2A
// Purpose:    одно решение: годится ли заказ как вход нормализации. Вход подшага 2A — это вход
//             всего шага 2 (`TASK.md` плюс ключ задачи), и правило про него уже написано ОДИН раз:
//             `steps/brd/inputs.mjs`. Здесь оно подставляется, а не переписывается — второй текст
//             того же требования разъехался бы молча (standards/code.md, ограничение 1).
// io:         fs (через ../inputs.mjs)
// EXTERNAL_DEPENDENCY: steps/brd/inputs.mjs — сам суд и его классы отказа.
// Invariants: ТОТАЛЕН. Отказ несёт КЛАСС и текст.
// Interface:  CLASSES, inputs
//
// ФАЙЛ ЖИВЁТ ЗДЕСЬ, А НЕ ОДНОЙ СТРОКОЙ ИМПОРТА В ГОЛОВЕ, потому что «шаг судит СВОЙ вход»
// (standards/workflow-design.md, правило 11) сторожится швом по КАТАЛОГУ подшага: подшаг без
// `inputs.mjs` — это подшаг, у которого суда входа нет вовсе, и шов обязан это видеть.
export { CLASSES, inputs } from "../inputs.mjs"
