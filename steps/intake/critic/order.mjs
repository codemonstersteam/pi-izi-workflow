// MODULE_CONTRACT: critic — слайс пласта critic наряда intake (последний взгляд, D)
// Purpose:    одно решение: ЧТО видит модель на критике — ВЕСЬ артефакт целиком (общий слот
//             {PREVIOUS} головы) и рубрику шаблона; своих данных пласт не несёт, поэтому
//             {BRD} здесь ПУСТ — пустота законна: повторять BRD значит дублировать то, что
//             артефакт уже воплощает.
// io:         none
// EXTERNAL_DEPENDENCY: нет — данные критика приносит голова (order.mjs, {PREVIOUS}).
// Invariants: слот приходит ВСЕГДА пустой строкой; тотальность наряда проверяет голова
//             (order.mjs) — подписанный пустотой слот не оставляет дыры.
// Interface:  orderSlice

// FUNCTION_CONTRACT: orderSlice — слоты пласта critic
//   Input:        state — состояние прогона; prev — staging прошлого слоя, здесь не нужен
//   Antecedent:   нет — ветвлений нет
//   Consequent:   success: { "{BRD}": "" } — BRD в наряде критика пуст, данные — артефакт
//   Purity:       pure
// V2-4 — КРИТИК: последний взгляд перед планом; рубрика в шаблоне, данные — артефакт целиком.
export function orderSlice(_state, _prev) {
  return {
    "{BRD}": "",
  }
}
