# Тестовый прогон izi: quarkus-rest-json-app-v2-t1-3-27-08-test-plan (FRUIT-1, fast-трек)

Дата: 27.08.2026, 12:43–14:05. Форма: `sandbox/runbox/quarkus-rest-json-app-v2-t1-3-27-08-test-plan`
(чистая копия мастера), интерактивный pi в tmux (`/izi`), модель ролей и курьера
`qwen3.8-max` (xhigh). Задача: «UI тянет весь список фруктов, чтобы найти один. Нужен поиск
по части имени, с ограничением на размер ответа. Существующие вызовы ломать нельзя.»

Проверялось: укороченный трек intake (T76 «маленькая задача»: один вызов вместо шести пластов)
со всеми четырьмя фиксами дня — перекалибровка порога, T77 (круги починки one), T78
(carried-by-nfr), T79 (строки-ограничения на нормализации).

## Результат: ЗАДАЧА РЕШЕНА ПОЛНОСТЬЮ, fast-трек доказан живьём

`.agent/frd.xml` (2,7 КБ, полный двор зелёный, офлайн-суд зелёный):

| пункт заказа | чем закрыт |
|---|---|
| поиск по части имени | UC1 (GET /fruits/search, фильтрация по подстроке) + поле `name` (source: normalized.md) |
| ограничение размера ответа | поле `limit` (integer, optional) + постусловие «count does not exceed limit» |
| существующие вызовы ломать нельзя | UC2 «existing operations continue unchanged» (GET/POST/DELETE идентичны) + сценарий S2 (до/после сосуществования) + `<nfr subject="backward-compatibility" source="brd.md">` |

Качество ВЫШЕ базы 26.08: там ограничение было только нфр-строкой — здесь оно получило
use case, сценарий проводки, нфр и владельца. RTM: R1, R2 → FruitResource.java (все 5 шагов
UC1 и 3 шага UC2 у правильного модуля). Дельта одна — `GET /fruits/search` Added на
FruitResource: существующий контракт не тронут (совместимость по построению и по гарантии).

## Хронология: три прогона, четыре фикса — все пойманы живым прогоном

| прогон | время | чем кончился | что поймал |
|---|---|---|---|
| 1 | 12:43–12:49 | остановлен | **порог 12 не сработал**: isSmall читает УЗЛЫ КАРТЫ (24), а калиброван по java-файлам (9) → полный путь. Фикс: 12→32 по живым картам (quarkus 24 / eddi 71) |
| 2 | 12:57–13:33 | **тупик, остановлен** | 44 красных круга, 24 агента, 269k токенов: R3-ограничение без владельца, артефакт честно нёс `carried by="nfr:…"`, а forward-суд видел только owners/questions. Фиксы: T77 (красный one чинится на месте — падение 1+6 хуже полного пути) и T78 (carried-by-nfr — носитель) |
| 3 | 13:52–14:05 | **frd.xml продвинут** | первый круг one красный (R2 без владельца) → **T77-круг починки** → второй вызов зелёный → promote. T79 живьём: normalized.md нёс `preserve | existing calls | … — «Существующие вызовы ломать нельзя»` |

Сбоев инфраструктуры и лупов в финальном прогоне нет; вопросы оператора не понадобились.

## Метрики финального прогона (по state.json, per-agent)

| шаг | вызовов | время | токены in / out / cached |
|---|---|---|---|
| task | 0 (скрипт, ключ из TASK.md) | 0 | 0 |
| brd/normalize+clean+anchors | 3 | 92 с | 8,8k / 3,0k / 15,4k |
| scope/parts (рой scout ×10, параллельно) | 10 | ~160 с (макс параллельных) | 55,1k / 17,5k / 321k |
| graph | 0 (скрипт) | 0 | 0 |
| **intake (fast-трек: красный круг + T77-починка)** | **2** | **382 с** | **31,6k / 14,5k / 159k** |
| plan/values (за рамкой, остановлено) | 1 | 49 с | 5,8k / 1,8k / 13,4k |
| **Итог до frd.xml** | **15** | **~18 мин** | **105k / 33k / 502k; биллинговых 138k** |

### Против базы 26.08 (полный путь, тот же TASK)

| метрика | 27.08 fast-трек | 26.08 полный | цель T76 |
|---|---|---|---|
| вызовов LLM intake | **2** | 9 | ≤2 ✅ |
| intake токены (in+out) | **46k** | 99k | ≤⅓–½ ✅ |
| весь прогон до frd.xml | **15 агентов, 138k, ~18 мин** | 16 агентов, 203k, ~45 мин | — |
| полнота плана | **3/3 пункта** | 3/3 (ограничение — только нфр) | — |

Скрытая цена дня: два сорванных прогона (269k токенов тупика + ~75k прогона порога) — это
плата за калибровку живыми прогонами; каждый закрыт тикетом с реинтродукцией.

## Живые доказательства фиксов

- **Перекалибровка**: развилка сработала (say «маленькая задача: один вызов»), 24-узельная
  карта прошла порог 32.
- **T77**: красный `one` (rtm:forward R2) → круг починки с FEEDBACK → зелёный. 2 вызова
  вместо 1+6 — ровно экономика «красный-чинимый».
- **T78**: не понадобился как запасной выход (R2 закрыт OWNERS через UC2 — сильнее), суд
  зелёный; тупик прогона 2 закрыт навсегда.
- **T79**: `preserve`-строка с дословной цитатой в normalized.md; constraint-row судья
  держит класс (юниты + компонентный сценарий 10 + эталон eddi чист).

## Артефакты на диске (итог фазы планирования)

`.agent/`: normalized.md (2 строки, с preserve) · brd.md (R1-R2 + аналог fruit) ·
anchors.json · hits.txt · focus.json · survey-plan.json · appgraph.xml (24 узла, задвоение
путей — отложенный тикет дедупа) · graph-computed.xml · rtm.md (R1, R2 → FruitResource) ·
staging/frd~one.xml · **frd.xml**.

Коммиты дня: 5bd45f8 (порог 32) · 4c2aac9 (T77) · 33ca419 (T78) · 5dc7884 (T79); линия 560/560.

## Осталось за рамкой

- eddi-регресс (полный путь на большой задаче, isSmall=false) — форма
  `runbox/eddi-27-08-test-plan` подготовлена, старт после приёмки оператора.
- Отложенные тикеты: дедуп путей карты (24→12, затем порог ужать до ~16); T71 (resume
  existence≠green); T72 (разрезы вывода: критик — только вердикт).
